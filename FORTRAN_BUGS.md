# Bugs and suspected errors found in the Fortran sources

One entry per finding. Include file:line, WRF/WRF-SFIRE commit, what the code does, what it should do, and how it was noticed (which test).

| # | Repo @ commit | File:line | Description | Impact on .esm | Reported upstream |
|---|---|---|---|---|---|
| N1 (note) | NCAR/MMM-physics @ 550b5b4 | `bl_ysu.F90:1` | `#define NEED_B4B_DURING_CCPP_TESTING 1` makes `bl_ysu_run`'s `ttnp` a temperature tendency (θ tendency × `pi2d`) and `dtsfc` use `del/pi2d`; the WRF wrapper compensates. | Standalone consumers and tests must divide by `pi2d` (Spike A reference is `ttnp/pi2d`). | no (documented behaviour) |
| N2 (note) | NCAR/MMM-physics @ 550b5b4 | `bl_ysu.F90` parameters and literals | `kind_phys` parameters (`h1=0.33333335`, `h2=0.6666667`, `afac/bfac=6.8`, `d1..d3`, `tmin`) and in-line literals (15.9, 0.15, 1.746, 1.286, 2.1, 0.16, 1.e-7, −0.18) are default-real, so a real64 build runs with single-precision roundings (`h1 = 0.3333333432674408 ≠ 1/3`); with the real32 module constants this limits real64 agreement to ≈1e-7 relative. Same in `mp_wsm6.F90` (bvtr, avtr, den0, .104, 9.8, .55, .78, .31 are default-real; `t0c` arrives as real32 273.1499939 K so melting rates ∝ (T − T0) lose 4e-6–6e-5 near freezing; `pfrz2` = 0.6600000262 in the Bigg exponential; `dimax` real32 gives roqimax 4.3e-7; `rgmma`'s `euler` literal is default-real). | Spike A tolerances 1e-6 rather than 1e-9. | not yet |
| B1 | NCAR/MMM-physics @ 550b5b4 | `sf_sfclayrev.F90` | Not kind-generic: `alog/amax1/amin1` and default-real literals passed to `kind_phys` dummies; fails to compile with `kind_phys` = real64. | Fixed in the ctessum-claude fork (52cfbd7) so a real64 driver can be built. | not yet |
| B2 | wrf-model/WRF v4.8.0 | `phys/Makefile` target `submodules` | Tests `run/NoahmpTable.TBL` relative to `phys/` (should be `../run/`), so the test always fails and every `./compile` runs `git submodule update --init --recursive`, silently resetting instrumented submodule checkouts. | Commit the submodule pointer before compiling. | not yet |
| N3 (note) | NCAR/MMM-physics @ 550b5b4 | `mp_wsm6.F90` | Process rates are dt-dependent by construction: limiters scale with `1/dtcld` (`min(rate, q/dtcld)`, `satdt`), and pigen/pcond/melting/freezing are instantaneous adjustments whose increment is dt-independent. | WSM6 tests compare the dumped `<rate>_raw` rates pointwise against the dumped evaluation state instead of a small-dt replay. | n/a |
| N4 (note) | NCAR/MMM-physics @ 550b5b4 | `mp_wsm6.F90:459-460, 519-540` | `xl`, `cpm` are evaluated once per call and `qsat`, `rh` once per sub-step from the start-of-sub-step `t`, `q`, while the process rates use `t` after sedimentation, melting and freezing (deliberate; comment "neglect the changes ... Emanuel (1994)"). | `WSM6Saturation` is a separate component whose outputs feed `WSM6WarmRain` as inputs. | n/a |
| N5 (note) | NCAR/MMM-physics @ 550b5b4 | `mp_wsm6.F90` `rgmma` (≈line 1599) | Γ(x) via a Weierstrass product truncated at 10000 factors: `g3pbr = 4.690786833363863` vs Γ(3.8) = 4.694174205740421 (7e-4 low); `g5pbro2 = 1.8265869519788835` vs 1.8273550806240353. | `wsm6_parameters.esm` carries the rgmma values as the scheme's effective constants. | n/a |
| N6 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_sw.F:499-521` | The renormalisation (`TRANS0 < 1` → `FF = 99/…`) rescales the fractions applied to `SDOWN(K+1)` and `TTEN`, but `DSCLD/DSCA/DABS` were already accumulated with the RAW fractions, so below a renormalised layer `SDOWN ≠ SDOWN(1) − DSCLD − DSCA − DABS` and `XABS = (…)(SDOWN(1) − DSCLD − DSCA)/SDOWN` can go negative (clamped to 0): a thick layer switches off all vapour absorption beneath it. | The component's closed-form bookkeeping is exact only when no depleting layer lies below a renormalised one (needs a coupled recurrence otherwise; format gap (k)). | not yet |
| N7 (note) | NCAR/MMM-physics @ 550b5b4 | `bl_ysu.F90:843-847` | The two Bolton saturation evaluations of the cloud-top branch use different reference temperatures, 273.16 then 273.15. | Parameters `t0_bolton_first`/`t0_bolton` kept apart. | not yet |
| N8 (note) | NCAR/MMM-physics @ 550b5b4 | `bl_ysu.F90:866-867, 875-876` | Two `bfx0 = max(… − radsum/rhox2/cp, 0.)` assignments are immediately overwritten (dead code); the surface part of the branch's `wm3` uses the FINAL `hpbl` and drops the `sfcflg` gate, unlike `wstar3` (first-guess `hpbl`). | Transcribed as written (`wm3` branch form). | n/a |
| N9 (note) | NCAR/MMM-physics @ 550b5b4 | `bl_ysu.F90:891` | `hgamq` is recomputed in the branch without the positivity clip; nothing reads it afterwards. | Not transcribed (documented in `hgamq`). | n/a |
| N10 (note) | wrf-model/WRF v4.8.0 | `phys/module_sf_slab.F:441,449` | `OLTG` uses `P1000mb*0.001/PSFC` while `THG` (the one that enters HFX) uses the literal `100./PSFC`; identical only because P1000mb = 1e5 Pa. `OLTG` is computed and never used. | `theta_g` uses `wrf.p1000mb`. | n/a |
| N11 (note) | wrf-model/WRF v4.8.0 | `phys/module_sf_slab.F:445,456` | The force-restore term `HM = 1.18 EOMEG (TG0 − TMN)` is computed for every point but enters the tendency only when num_soil_layers = 1; `TMN` therefore has no effect on the 5-layer scheme, whose bottom boundary is the never-updated deepest layer `TSLB(num_soil_layers)`. | `hm` diagnosed only; bottom BC = fixed reservoir `T_deep`. | n/a |
| N12 (note) | wrf-model/WRF v4.8.0 | `phys/module_sf_slab.F:463-506` | Explicit Euler with `nsoil = 1 + int(4·1.25·DIFSL·dt/DZS(1)²)` sub-steps and surface fluxes re-evaluated at `TSLB(1)` from the second sub-step; the per-step ground-temperature change at dt = 60 s (nsoil = 2) differs from the instantaneous tendency by 0.5–11 %. At dt ≤ 0.04 s nsoil = 1 and the step is exactly dt × derivative. | Tests use the 0.01 s real64 replay as an exact derivative. | n/a |
| N13 (note) | wrf-model/WRF v4.8.0 | `phys/module_sf_slab.F:350-351` | `DATA CSW/4.183E6/` and `LAMG(4)` are declared and never used (dead constants). | none | n/a |
| N14 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F:2347` | WRF's `OLR = TOTUFLUX(kte)` is the upward flux at the MODEL TOP (p_top ≈ 178 hPa), not at the top of the RRTM column `TOTUFLUX(NLAYERS)` (commented out); the two differ by 3.1–7.5 W/m² in the reference run. | `olr` transcribed as the model-top flux, documented. | not yet |
| N15 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F:971` | `HEATFAC = 8.4391` uses g = 9.8066 and cp = 1.004e3 (not WRF's 9.81/1004.5) and is the rounded literal (formula 8.43915). | `heatfac` kept as the literal in `rrtm_parameters.esm`. | n/a |
| N16 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F:4094-4103, 4177-4185` | MM5ATM sets `WKL(3,L)` from the model-column ozone climatology, then (kte ≠ NLAYERS always) overwrites it with `O3PROF2` recomputed on the extended column; the dumped `o3prof` therefore is NOT the ozone RRTM uses (33 % different). N2O/CH4 are multiplied by amdn/amdc and divided out again. | `colo3` deferred (needs O3DATA as a pressure integral); coln2o/colch4 written directly as coldry·vmr. | n/a |
| N17 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F:4110-4126` | Dead `IF (1 .EQ. 0)` block (old single-buffer-layer extension). | none | n/a |
| N18 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F:3790-3797` | `PLWP/PIWP = (RO·QR)^0.75·DZ·1000` take a non-integer power of a dimensioned quantity (implicitly kg/m³). | `rho_ref = 1 kg/m³` parameter makes the power dimensionless. | n/a |
| B3 | NCAR/MMM-physics @ 550b5b4 | `sf_sfclayrev.F90:712` | `dx_factor = varf(i)/(dx(i)*dx_factor/32000.+7./8.)` reads the uninitialised local `dx_factor` on its own right-hand side (KIM turbulent orographic form drag, `if_kim_tofd`); the result is then unused, but the statement is undefined behaviour. | The tofd branch is not transcribed (`cf = 0`); flag upstream before transcribing. | not yet |
| N19 (note) | NCAR/MMM-physics @ 550b5b4 | `sf_sfclayrev.F90:43-49, 1016-1054` | The psi lookup-table nodes are `float(n)*0.01` (real32) and the closed forms use default-real literals (`1./2.5`, `sqrt(3.)`, `ATAN(1.)`, `0.33`); with the real32 WRF constants (svpt0 = 273.1499939 dominates through θ_v1 − θ_vs) this bounds real64 agreement at ≈4e-6 relative in Ri_b-derived quantities. | `SfclayRev` tolerances 2e-5/1e-6 rather than 1e-9. | not yet |
| N20 (note) | NCAR/MMM-physics @ 550b5b4 | `sf_sfclayrev.F90:883-887` | `lh` (intent out) is assigned only when `isfflx` is true and `scm_force_flux` is false; otherwise it is returned uninitialised. | The component defines `lh = xlv qfx` always; `lh` is not asserted in those two regimes. | not yet |
| N21 (note) | NCAR/MMM-physics @ 550b5b4 | `sf_sfclayrev.F90:720-777, 534` | `ck`/`cka` are computed before the `iz0tlnd` thermal-roughness block, so `cka` uses the pre-modification `psiq`; `psiq10` is never updated for `iz0tlnd`; `depth_dependent_z0` ignores its `z0` argument. | Transcribed as written (`psiq_pre` for `cka`; `psiq10` land form). | n/a |
| N22 (note) | NCAR/MMM-physics @ 550b5b4 | `mp_wsm6.F90:598-602, 977-979` | `xni` is computed before the ice fallout and again inside the cold-rain block from the current qi; the instrumented dump (W1) captures the stale first value. | `hydrometeor_slopes.esm` computes N_i from the rates state; it cannot be asserted against the dump directly. | n/a |
| N23 (note) | NCAR/MMM-physics @ 550b5b4 | `mp_wsm6.F90:1116-1121` | `pgacs` (accretion of snow by graupel) is set to 0 unconditionally since V3.0 (common snow/graupel fall speed); the surrounding if-block is dead. | Transcribed as `pgacs = 0` with the reason. | n/a |
| N24 (note) | NCAR/MMM-physics @ 550b5b4 | `mp_wsm6.F90:496, 683, 698` | `mstep(i) = 1` always (legacy sedimentation sub-stepping); psmlt/pgmlt are stored as dtcld-increments `min(max(rate*dtcld, -q), 0)`, not rates. | `melting_freezing.esm` exposes rates and increments separately. | n/a |
| N25 (note) | NCAR/MMM-physics @ 550b5b4 | `mp_wsm6.F90:690-702` | `pgmlt` is evaluated at the temperature already updated by psmlt in the same layer, but with the ventilation factor work2 from the un-updated temperature; the freezing block then evaluates supcol and xlf once per layer and reuses them for pimlt, pihmf, pihtf, pgfrz although pimlt changes t. | Transcribed as written (`T_s`, `T_m`, `supcol_f`, `xlf_f`). | n/a |
| N26 (note) | NCAR/MMM-physics @ 550b5b4 | `mp_wsm6.F90:1142-1204` | The deposition limiters are sequential and order-dependent (prevp, pidep, psdep, pgdep, pigen share satdt via supice and the ifsat flag), so the applied rates depend on the evaluation order, not only on the state. | `ice_deposition.esm` reproduces the chain pointwise; the unlimited rates are the order-free physics. | n/a |
| N27 (note) | NCAR/MMM-physics @ 550b5b4 | `mp_wsm6.F90:1530-1560` | The saturation adjustment uses the WATER saturation mixing ratio at all temperatures (`qsat(:,:,1)`), so below 0 °C it adjusts vapour to water saturation even when only ice is present, and it reuses the start-of-call `xl` and `cpm` for the latent heating; the result is an instantaneous increment stored as `pcond = dq/dtcld`. | `saturation_adjustment.esm` takes `xl`, `cpm` as inputs and uses `sat.qsat_w` only; exposes the increment, the kernel rate and a τ-relaxation rate. | n/a |
| N28 (note) | NCAR/MMM-physics @ 550b5b4 | `mp_wsm6.F90:632-655, 737` | `fall(:,k,:)` mixes the post-fallout density with the pre-fallout speed and is overwritten at k = 1 by the PLM surface precipitation; `fallc` exists only at k = 1. Only the k = 1 values are read afterwards (rainncv/sr). | Reference fluxes taken as the dt→0 limit of `fall·delz`; ice interior flux formula-only. | n/a |
| N29 (note) | NCAR/MMM-physics @ 550b5b4 | `mp_wsm6.F90:625-631, 1118` | Snow and graupel sediment at the common mixing-ratio-weighted speed `worka` (nislfv_rain_plm6), zeroed when qs+qg ≤ 1e-15; `workr = 0` where qr ≤ 0; the fall speeds use slopes from the START-of-sub-step state (not the rates state). | `W_sg` shared by snow and graupel; `m = ρ max(q,0)`; `sd` evaluated on the input state. | n/a |
| N30 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F` RTRN loop 3000 | The upper-level Planck emission of layer LEV, `BGLEV`, is carried from the previous downward iteration and therefore uses `PFRAC(IPR, LEV+1)` (the Planck fraction of the layer above) with `PLVL(NGB, LEV)`; only the top layer uses its own PFRAC. | `bglev_up[g,l] = pfrac[g, min(l+1, NLAYERS)] · plvl[ngb, l+1]`. | n/a |
| N31 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F` RTRN loops 2100/3500 | The clear-sky downward radiance is copied from the total until the first cloudy layer (`ICLDDN`), then propagated with the clear formula; this equals the clear recurrence applied throughout. | `rad_dn_clr` is one uniform recurrence. | n/a |
| N32 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F` RRTMINIT loop 1000 | The comment says TF = tau/6 below 0.01; the code tests `TAU < 0.1`. RTRN never uses TAUG directly: the optical depth is quantised through ITR to 1/5000 in Padé-transition space. | `tf_linear_threshold = 0.1`; `tau_gas` from ITR, not TAUG. | n/a |
| B5 | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F:4189-4202` (MM5ATM) | The extended-column ozone is off by one layer: `PZR(L) = PZ(NLAYERS-L+1)` for L = 1..NLAYERS drops the surface level PZ(0), so INIRAD/O3DATA returns in O3PROF2(L) the climatological mean over [PZ(L), PZ(L+1)] — the layer ABOVE RRTM layer L — and `WKL(3,L) = O3PROF2(L) amdo` assigns it to layer L; the surface layer's own ozone is never computed and the top layer gets the constant 6.135E-6. Verified: the shifted mean matches the dumped WKL(3) to 2.4e-6, the own-layer mean differs by 27 % of the column maximum. | `o3_clim` reproduces the shift (documented); `o3_clim_own_layer` shows the intended value. | not yet |
| N33 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F` SETCOEF | SELFFAC, SELFFRAC, INDSELF are assigned only for layers ≤ LAYTROP; above they hold stale memory (the dump shows 4.7e30 etc.), harmless because only the lower-atmosphere branches read them. | Defined on every layer; tested through `*_lower` views. | n/a |
| N34 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F` TAUGB2 loop 1800 | The reference-pressure search `DO IFRAC = 2,12 … GO TO 1900` falls through with IFRAC = 13 when H2OPARAM is below every node; FRACREFAC2 has 13 columns so the extrapolation between nodes 12 and 13 is intended, but easy to mis-transcribe. | `ifrac_b02 = 2 + count(H2OPARAM < REFPARAM(k), k = 2..12)`. | n/a |
| N35 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F` TAUGB9 | IOFF is set when the layer loop *reaches* LAYLOW and LAYSWTCH and kept for all following layers (blocks 1/2/3 of ABSN2OC), i.e. it depends on loop order, not only on the layer. | `n2o_block_b09 = 3 for L ≥ LAYSWTCH, 2 for L ≥ LAYLOW, else 1`. | n/a |
| N36 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F` RRTMINIT loop 1200 / TAUGB2 | CORR1/CORR2 are tabulated at 0.005 steps of FP and looked up at INT(200 FP + 0.5); this quantisation limits band-2 agreement to 1.6e-5 relative. | Closed forms at the quantised FP. | n/a |
| N37 (note) | wrf-model/WRF v4.8.0 | `phys/module_pbl_driver.F` YSU call; `phys/physics_mmm/bl_ysu.F90:120` | YSU's `psim/psih` arguments are the full profile functions ln(z1/z0) − ψ (= sfclayrev's FM/FH), not the stability corrections sfclayrev returns as PSIM/PSIH (0.56 vs 5.68 at step 60). | Assembly couples `Sfc.fm → Pbl.psim`, `Sfc.fh → Pbl.psih`. | n/a |
| N38 (note) | wrf-model/WRF v4.8.0 | `phys/module_surface_driver.F` (LSM branch after sfclayrev) | `TH2/T2/Q2` returned by sfclayrev are overwritten by the land-surface branch (q2 0.00416 → 0.00339 at step 60); `HFX/QFX/LH/QSFC/TSK` are replaced by the slab's, and it is the slab's hfx (evaluated at the sub-stepped TSLB(1)) that pbl_driver receives, 0.1 % from sfclayrev's. | Assembly references sfclayrev's 2 m diagnostics to the kernel dump; prescribes the slab fluxes into YSU. | n/a |
| N39 (instrumentation) | ctessum-claude/WRF 922acdc | `phys/module_ra_rrtm.F` in-kernel hooks | Keyed on `esm_dump_active()`, so they append RRTM internals into ANY open dump file (the `subassembly_rad` files carry duplicate `taug/pfrac/…` keys, 2.9 MB). | Loaders keep the last key; change the gate to `esm_dump_inner` or a scheme check on the next instrumentation pass. | fork only |
| B6 | NCAR/fire_behavior @ eb77580 | `state/state_mod.F90:214` (Apply_wafs) with `share/fuel_mod.F90` Calc_wind_adjustment_factor | `waf` is allocated `1:n_fuel_cat` (13) but indexed with `nfuel_cat(i,j)`, which is 14 for no-fuel cells → out-of-bounds read when `wind_vinterp_opt = 1`. | `Anderson13FuelTable.waf` returns the formula value for code 14; consumers must not use it. | not yet |
| N40 (note) | NCAR/fire_behavior @ eb77580 | `physics/ros_wrffire_mod.F90:52,72,79` | `196.850` ft/min per m/s and `0.00508` m/s per ft/min are written independently and are not reciprocals (1/0.00508 = 196.8504); `SLOPE_FACTOR` is declared `integer, parameter :: SLOPE_FACTOR = 1.0`. | Both literals kept as separate parameters. | n/a |
| N41 (note) | NCAR/fire_behavior @ eb77580 | `physics/ros_wrffire_mod.F90:139-142` | fgi is treated as the MOIST load: the fits use `(1 − M/(1+M)) fgi`, unlike Rothermel/Andrews where w₀ is oven-dry; and `wn = fuelload/(1+st)` (Rothermel 1972) rather than `w0 (1 − st)`. | Transcribed as written (documented differences from `rothermel/fire_spread.esm`). | n/a |
| N42 (note) | NCAR/fire_behavior @ eb77580 | `physics/ros_wrffire_mod.F90:76` | `if (tanphi > 0.0)` is always true because `tiny()` was added to tanphi; `phis` is then `5.275 beta^-0.3 tiny^2` = 0 on flat ground anyway. | `phis` written with the same ifelse. | n/a |
| N43 (note) | NCAR/fire_behavior @ eb77580 | `state/state_mod.F90:664` | `fuel_time = weight/0.85`, whereas Mandel et al. (2011) and EarthSciModels `fuel_consumption.esm` use 0.8514. | `fuel_time_divisor = 0.85`. | n/a |
| N44 (note) | NCAR/fire_behavior @ eb77580 | `physics/level_set_mod.F90` Calc_tend_ls case 4 | With `fire_upwinding = 4` the normal uses `diff2 = max(D⁻,0) − min(D⁺,0)` (a magnitude-like combination, not a signed derivative) while ∣∇ψ∣ uses the Godunov Hamiltonian; `Update_ignition_times` nudges time_now by 2 ε(real32). | Normal not transcribed (ROS takes nvx, nvy as inputs); nudge reproduced. | n/a |
| N45 (note) | wrf-model/WRF v4.8.0 | `chem/KPP/mechanisms/radm2/radm2.eqn` reactions 22, 24, 37 | Default-real literals `.20946e0`, `.78084`, `300.0`, `-2.3` inside otherwise `_dp` rate expressions: the O2/N2 fractions and the O3P+O2+M exponent are single-precision values (0.20946000516, 0.78084000111, −2.29999995). | Bound as their binary64 values in `radm2.esm` (exact agreement). | n/a |
| N46 (note) | wrf-model/WRF v4.8.0 | `chem/KPP/inc/radm2/kpp_mechd_ibu_radm2.inc` | `rc_n2o5` is computed entirely in REAL(4) (`rh`, `es`, `qvs` single) before the `REAL(…, dp)` cast. | `k_R043` tolerance rel 1e-6 (residual ≤ 7.5e-8). | n/a |
| N47 (note) | wrf-model/WRF v4.8.0 | `radm2.eqn` reaction 60 | Negative product coefficient `−0.90 OH`: KPP's net OH stoichiometry is −1.9 (reactant −1 plus product −0.9). | Shadow reaction `R060x` with rate 0.9 k. | n/a |
| N48 (note) | wrf-model/WRF v4.8.0 | `radm2.eqn` labels | The RADM2 label numbers skip 25, 59, 61, 63; the KPP/`RCONST` index is the sequential position (label 44 = RCONST(43)). | esm ids `Rnnn` follow the sequential KPP index; the label is kept in `name`. | n/a |
| N49 (note) | wrf-model/WRF v4.8.0 | `chem/KPP/kpp/kpp-2.1/src/Makefile`, `configure_kpp` | KPP does not build on a current host: `yacc scan.y` without `-d` (no `y.tab.h`), and `-lfl` assumed. | Patched in the data-dir copy only. | n/a |
| N50 (instrumentation) | ctessum-claude/WRF 592f693 | `dyn_em/module_first_rk_step_part1.F` `subassembly_pbl` hook | Dumped `p`/`psfc` differ from the kernels' `p_phy`/`psfc` by up to 0.28 Pa (same class as the radiation `p8w` note); the kernels agree with each other. | Assemblies use kernel-level inputs; align the hook on the next instrumentation pass. | fork only |
| N51 (note) | wrf-model/WRF v4.8.0 | `dyn_em/start_em.F` CFBM init; `CMakeLists.txt` ENABLE_CFBM | The fire_behavior model (`ifire = 1`) exists only in the CMake build and refuses any domain that is not Lambert conformal; the ideal `em_fire` case (`module_initialize_fire.F` sets `map_proj = 0`) therefore cannot run it, and `test/em_fire/namelist.input` (`ifire = 2`) exercises the old WRF-SFIRE code. | Coupled references come from fire_behavior's WRF-side routines replayed on test7/wrf.nc columns. | not yet |
| N52 (note) | NCAR/fire_behavior @ eb77580 | `tests/test7/namelist.input_wrf` (`ifire = 2`) | test7's `wrf.nc` was produced by WRF-SFIRE, not CFBM: its `UF/VF` come from SFIRE's bilinear 3-D-wind interpolation and its `RTHFRTEN` from SFIRE's `fire_tendency`, which multiplies the exponential profile by `prop_heat(i,k,j)`; CFBM's `Fire_tendency` has no such factor (0.7–1.5 % difference), while the `grnhfx` aggregation is identical. | Tests reference CFBM replays; wrf.nc values documented only. | n/a |
| N53 (note) | NCAR/fire_behavior @ eb77580 | `io/wrf_mod.F90` Fire_tendency | Divides the heat flux by cp and stores it as a θ tendency (no Exner factor); the tendency loop stops at `kde − 2`, leaving the top layer 0 while the flux at that layer is defined; `Provide_atm_feedback` initialises `grnqfx/canhfx/canqfx` only on the interior points. | `rthfrten_wrf` makes the truncation explicit; θ ≈ T noted; tests use interior columns. | n/a |
| N54 (note) | NCAR/fire_behavior @ eb77580 | `share/interp_mod.F90` Interp_profile vs `io/wrfdata_mod.F90` | The standalone passes `kime = kde − 1` levels (kdmax = kde − 3) while WRF passes `kfde = kde` (kdmax = kde − 2); `Interp_horizontal_nearest` clamps `j_in` with `ime` instead of `jme` (harmless on square domains). | Component metaparameter `NZW` documents the bound. | not yet |
| B7 | wrf-model/WRF v4.8.0 | `cmake/modules/FindnetCDF-Fortran.cmake` | Fails on Ubuntu's `nf-config --flibs` (library dir not extracted) → "Could NOT find netCDF-Fortran"; needs `-DnetCDF-Fortran_LIBRARY=…`. | Build recipe documents the workaround. | not yet |
| B4 | wrf-model/WRF fork (instrumentation) | `phys/module_ra_rrtm.F` RTRN loop 3500 | Instrumentation-only: the first per-band surface upward-flux accumulator omitted `TOTUFLUX(0)`; fixed in fork commit 0e52608. Not a WRF bug. | Re-dumped `scm_ref2_*`. | n/a |
| B8 | wrf-model/WRF v4.8.0 (and `develop`); introduced in upstream 297203407 (#380, T1_MOZCART_KPP, 2018) | `chem/KPP/util/wkc/compare_kpp_to_species.c:379-383` (the KPP coupler generator; the symptom appears in the generated `chem/module_kpp_<mech>_interface.F`) | The coupler's fixed-species special case forces `found_match = 2` on any KPP species named `CO2`, overwriting the name match to WRF-Chem's transported `chem(P_co2)` made a few lines earlier; `gen_map_wrf_to_kpp`/`gen_map_kpp_to_wrf` then skip CO2 in both copy loops, and `wki_one_d_vars` emits no `FIX(indf_CO2)` either, so `VAR(ind_CO2)` is never set. CO2 is `#DEFVAR` in 16 KPP mechanisms (radm2, radm2sorg, racm, racm_mim, racmpm, racmsorg, racmsorg_aqchem, racm_esrlsorg, racm_esrlsorg_aqchem, racm_soa_vbs, racm_soa_vbs_het, racm_soa_vbs_aqchem, saprc99, saprc99_mosaic_4bin_vbs2, saprc99_mosaic_8bin_vbs2_aq, gocartracm) and `#DEFFIX` only in t1_mozcart. In RADM2 (`chem_opt = 101`) `VAR(ind_CO2)` is uninitialised on the first call (observed 4.67e-310) and the previous cell's value afterwards; with `vertmix_onoff = 1` every species becomes NaN from chemistry call 3 and the Rosenbrock logs "Step size too small" for the rest of the run (529,584 times in 59 h), although WRF still reports SUCCESS. **Why it has gone unnoticed, and how general the damage is (assessed 2026-09-15; not yet verified in 3-D).** (i) CO2 is product-only in RADM2 (no reaction consumes it), so a wrong value changes no other species' rates and reaches them only through the Rosenbrock step-size control, whose error norm includes every species. (ii) In a normal 3-D run the uncopied `VAR(ind_CO2)` keeps the PREVIOUS grid cell's value, which is usually a physically plausible CO2; genuine garbage occurs only in the first cell of the first chemistry call. (iii) WRF does not fail: the solver prints "Forced exit from Rosenbrock … Step size too small" and the run still reports SUCCESS. (iv) Users do report that symptom on the WRF & MPAS-A Support Forum with mixed, unresolved causes; in one thread (forum.mmm.ucar.edu/threads/forced-exit-from-rosenbrock-solver.14021) a `chem_opt = 301` user (GOCART-RACM, one of the 16 affected mechanisms) made it go away by enabling photolysis on all domains, and nobody traced anything to CO2. So the catastrophic failure is established only for this repo's idealized em_scm_xy column with vertical mixing on; whether a standard 3-D WRF-Chem case shows more than solver-step perturbations (var_out differences of up to 4.8e-4 relative were measured here) is unknown. | Fixed in the ctessum-claude fork (55d1c08, branch `earthsciml-instrumented-chem`): the override applies only when no WRF-Chem match was found, which regenerates exactly the two missing copy lines in all 16 mechanisms and leaves t1_mozcart byte-identical. Fixed 59 h run `data/eqwefic/chem_scm_b8`: 0 step-size aborts, no non-finite or negative species; call-1 RCONST/jv/T/M/H2O/FIX bit-identical to the buggy build, so `wrfchem_scm_midtrop_rate_constants` is unaffected. Rebuild note: `touch chem/KPP/util/run_wkc` or the coupler does not rerun, and set `FLEX_LIB_DIR`. | not reported upstream (no issue/PR found 2026-09-15); **revisit during the 3-D comparisons** (decided 2026-09-15): reproduce on a standard 3-D WRF-Chem case before filing, and describe it as a silent correctness bug with configuration-dependent failure unless the 3-D case also fails |
| N55 (note) | wrf-model/WRF v4.8.0 | `chem/KPP/kpp/kpp-2.1/src/Makefile:82` | The bundled KPP 2.1 runs `$(YACC) scan.y` without `-d`, so `y.tab.h` is never produced and `scan.l` fails to compile with a modern bison/byacc. | Patched to `$(YACC) -d scan.y` in the fork (2ce0388); required for any WRF_KPP=1 build. | not yet |
| N56 (note) | wrf-model/WRF v4.8.0 | `chem/Makefile` | Tracked in git but rewritten from `Makefile_org` by `gen_kpp.c` at configure time (its own header says "MANUAL CHANGES TO THIS FILE WILL BE LOST"), so every WRF_CHEM build leaves a 500+ line spurious diff. | Never commit it; instrumentation dependencies go in `chem/depend.chem` instead. | n/a |
| N57 (note) | wrf-model/WRF v4.8.0 | WRF_CHEM=1 WRF_KPP=1 build order | The KPP coupler reads `Registry/Registry` while it is still being assembled: a first `./compile em_scm_xy` fails because `Registry/io_boilerplate_temporary.inc` has not been generated yet. Re-running `./compile` unchanged succeeds. | Build WRF-Chem twice. | not yet |
| N58 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F:4254` (MM5ATM loop 7000) | `IF(TAUCLOUD(L).GT.0.01)CLDFRC(kte+1-L)=1.` replaces the diagnosed cloud fraction of every layer whose LW cloud optical depth exceeds 0.01 by 1 before it is copied into RRTM's CLDFRAC, and it keys on TAUCLOUD alone: at SCM call 396 layer 51 has CLDFRA = 0 exactly with TAUCLOUD = 0.01072 and is handed to RTRN as fully overcast, and at call 120 fractions of 0.0212-0.561 all become 1. The SW scheme keeps the unmodified CLDFRA, so LW and SW see different cloud fractions in the same layer. Consequence: RTRN can only ever see 0 < CLDFRAC < 1 in a layer whose cloud optical depth is at or below 0.01, i.e. with ABSCLD = 1 - exp(-1.66 TAUCLOUD) <= 0.0165; over an 8 h scan of the reference SCM run the largest fractional cloud fraction reaching RTRN anywhere is 0.1470. | `rrtm_column.esm` `cldfrac` reproduces the override (`par.tau_overcast` = 0.01) and is tested in four cloud regimes; the fractional-cloud tests of `rtrn_sweep.esm` are chosen at that ceiling (call 240, CLDFRAC = 0.1470 at TAUCLOUD = 0.009966). | not yet |
| N59 (note) | NCAR/MMM-physics @ 550b5b4 | `mp_wsm6.F90:1290-1474` (mass-conservation block) | Reachability of the budget rescaling in the reference runs, over all 10 driver-replay dumps (7 SCM calls, 3 supercell rows, every column and layer): the cloud-water factor is 1.0 exactly and the graupel factor 1 - 1e-16, so neither ever binds; the ice, rain and snow factors bind at 0.9289 (9983 col 34 layer 15), 0.5877 (9983 col 30 layer 16) and 0.9736 (7980 col 21 layer 8); and the ENTIRE warm branch (`t > t0c`, four budgets) never binds materially (g_qr = 1 - 1e-16, g_qs = 1.0 exactly, g_qg = 1 - 3.4e-8). No SCM column activates the block at all. | `mass_conservation.esm` transcribes both branches; the warm-branch factors have no regime that exercises their binding (only their update path, which every regime with layers above 0 C covers). Tests name the binding columns explicitly. | n/a |
| N60 (note) | NCAR/MMM-physics @ 550b5b4 | `mp_wsm6.F90:1069, 1384, 1391, 1399, 1403-1404, 1460-1462, 1583` | `paacw` is the mixing-ratio-weighted MEAN of the snow and graupel riming rates, `(qs psacw + qg pgacw)/(qs+qg)`, but it is then added in full to BOTH the snow and the graupel budget while cloud water is debited `paacw + paacw` and the fusion heating uses `xlf*(... + paacw + paacw + ...)`. Mass and energy are conserved (the scheme uses 2 x the weighted mean as the total riming sink, exact when qs = qg), but the rimed water is split EQUALLY between snow and graupel regardless of qs/qg, so where qg << qs the graupel budget still receives half of it; no dumped column has qs > qcrmin with qg <= qcrmin, so the qg -> 0 limit is not isolated by the reference data. Separately, the diagnostic `rainprod2d` at line 1583 sums `psacw + pgacw + paacw`, i.e. all three riming terms, although only `paacw` is ever applied to the state. | Transcribed as written: `mass_conservation.esm` counts `paacw` twice in `source_qc` and in the qc update and once in each frozen budget; `psacw`/`pgacw` are deliberately not inputs of the component. | not yet |
| N61 (note) | NCAR/fire_behavior @ eb77580 | `physics/level_set_mod.F90` `Extrapol_var_at_bdys` | The halo ghost is `(1 − BIAS)(2a − b) + BIAS·max(2a − b, a, b)` with `BIAS = 1`, i.e. `max(2a − b, a, b)` — a MAX-GUARDED, not a plain, linear extrapolation, and the `(1 − BIAS)` term is dead code. Two consequences: (i) when the field increases toward the wall (a ≥ b, a fire's level set) the guard is inactive, the ghost is exactly 2a − b and the wall-cell second difference `(diffrx − difflx)` is IDENTICALLY ZERO, so the artificial viscosity vanishes on the wall lines by construction — not a consistent approximation of ψ_xx there; (ii) when the field decreases toward the wall the ghost is clamped to max(a, b), the wall cell's ENO1 derivative vanishes, an O(1) error the code accepts because a fire's level set always grows toward the walls. Also: the four diagonal CORNER ghosts are computed but never read (ENO1 at a corner reads only the two edge ghosts). | Transcribed faithfully as `stencils/extrapolate_linear_ghost` + the `bc:extrapolate_linear` closure (ESD PR #37); `central_D2{x,y}_extrapolate_bc` ship no convergence case and say why. Corner ghost not represented. | no (documented behaviour) |
| B9 | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F:4409-4420` (SETCOEF label 5300) against `4385-4391`; declared `phys/module_ra_rrtm.F:2193-2201` | Above LAYTROP, SETCOEF's `5300 CONTINUE` branch sets `FORFAC(LAY)` and the column amounts but never `SELFFAC(LAY)`, `SELFFRAC(LAY)` or `INDSELF(LAY)`, and the three arrays are plain uninitialised locals of `SUBROUTINE RRTM`; layers LAYTROP+1..NLAYERS therefore hold whatever was on the stack. Every `TAUGBn` reads them only inside its `DO 2500 LAY = 1, LAYTROP` loop, so the optics are unaffected, but the values are undefined and build-dependent. Observed: re-running the archived em_scm_xy reference case with a rebuilt binary (`data/eqwefic/scm_crosscheck`) reproduced 119 of the 122 dumped RRTM variables -- TAUCLOUD, CLDFRAC, TAUG, PFRAC, ITR, WKL, all level and per-band fluxes, HTR/HTRC, GLW, OLR -- BIT-FOR-BIT against `dumps/rrtm/scm_ref2_120.json`, and differed in exactly `selffac`, `selffrac` and `indself` at all 23 layers above LAYTROP = 80 (`selffac` 8.48e22 in one build against a leftover level pressure 93.61 in the other, `indself` -1.1e9 against 0). | `rrtm_setcoef.esm` defines all three everywhere; its assertions mask above LAYTROP (`selffac_lower` = `np.where(arange(nlayers) < laytrop, selffac, 0)`), so no expected value is pinned to garbage. Any future test of these arrays must keep that mask. | not yet |
| N62 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_rrtm.F:3754, 3850` (MM5ATM) against `3991-3994`, `4252-4253` | `RRTMLWRAD` fills `QG1D` from `QG3D` (`2011-2019`) and passes it to `MM5ATM`, which declares `QG` as an `INTENT(IN)` dummy (3850) and then never references it: the longwave cloud path block computes CLWP, CIWP, PLWP, PIWP from QC, QI, QR, QS only, and `TAUCLOUD = ABCW CLWP + ABICE CIWP + ABRN PLWP + ABSN PIWP` has no graupel term and no `ABGR` coefficient. Graupel is radiatively invisible to RRTM. Measured in the em_quarter_ss supercell reference (`dumps/rrtm/qss_i20_268.json`): qg reaches 6.2e-4 kg/kg in the column used by the mixed-phase regime and 1.3e-2 kg/kg elsewhere in the storm at 1 h, i.e. up to 20x the local qs, which the same (rho q)^0.75 path with ABSN would turn into an optical depth of order 10. The SW scheme (`module_ra_sw.F`) does the same. | `rrtm_column.esm` transcribes the four-term form as written and takes no `qg` input; the mixed-phase test description records the omission. | not yet |
| B10 | wrf-model/WRF v4.8.0 | `Registry/Registry.EM_COMMON:279-284`, `:1966`, `:58` against `:3322` | Eight auxinput3 (`i3`) fields — `HFX_FORCE`, `LH_FORCE`, `TSK_FORCE`, their `_TEND` partners, plus `ZNT` and `LU_MASK` — are NOT inside `package scmopt scm_force==1`, so they are registered on stream 3 unconditionally. Because `share/mediation_integrate.F:350` reads auxinput3 with no `scm_force` guard, a `force_ideal.nc` that happened to contain a `ZNT` variable would silently overwrite the roughness length even at `scm_force = 0`. The shipped `force_ideal.nc` does not, so the reference run is unaffected. | None for `data/eqwefic/scm_ref` (verified the file contains only Z_FORCE/U_G/V_G/W_SUBS/*_UPSTREAM_*/TAU_* and tendencies); a hazard for any future forced SCM case. | not yet |
| B11 | wrf-model/WRF v4.8.0 | `dyn_em/module_force_scm.F:93-140` against `dyn_em/module_first_rk_step_part1.F:2019-2101` | `force_scm` declares its forcing dummies `REAL, DIMENSION(num_force_layers)` (8 with this namelist), but at `scm_force = 0` the actual arguments are the size-1 placeholder allocations `inc/allocs_0.f90:5492` produces for the deactivated `scmopt` package. This is non-conforming argument association on every call; it is harmless only because of the `IF (scm_force .EQ. 0) return` at `:183`, which executes before any dummy is touched. | None (the early return is reached first), but it makes `scm_force = 0` runs depend on a return-before-touch ordering rather than on valid argument association. | not yet |
| N63 (note) | wrf-model/WRF v4.8.0 | `phys/module_sf_slab.F:488-506` against `Registry/Registry.EM_COMMON` `GRDFLX` | SLAB1D computes the surface soil heat flux internally as `FLUX(I,1) = RNET(I) - QS(I)` but never assigns it to the `GRDFLX` state array, so `GRDFLX` is identically zero for the whole run in the history output. | The surface energy budget cannot be closed from `wrfout` for `sf_surface_physics = 1`; the Phase 3.1 soil prototype therefore prescribes the top boundary from `TSK` rather than from `GRDFLX`. Confirmed zero across all 59 frames of `data/eqwefic/scm_ref`. | not yet |
| N64 (note) | wrf-model/WRF v4.8.0 | `dyn_em/module_initialize_scm_xy.F:674` (t_base) against `:696` (theta_m conversion) and `module_big_step_utilities_em.F` `rk_rayleigh_damp` | With `use_theta_m = 1` the damped field `t` is the MOIST potential temperature, but `t_base` is filled from the DRY theta before the dry-to-moist conversion, so `damp_opt = 2` relaxes theta_m toward a dry reference. In em_scm_xy theta_m − theta_base = +0.050 K in the top layer at step 1 and the damper cools the lid by −1.48e-4 K/s from the first step. Only the scm_xy initialization was checked. | `rayleigh_damping.esm` reproduces it (theta_base = t_base + t0). | not yet |
| N65 (note) | wrf-model/WRF v4.8.0 | `module_big_step_utilities_em.F` `pg_buoy_w` (k = kde) with `calc_cq` | At the top interface the moisture coefficient is `cqw(kde−1)`, the interface below, because `calc_cq` fills cqw only on 2..kde−1. Negligible aloft (q_t ≈ 1e-4). | Transcribed as is. | n/a |
| N66 (note) | wrf-model/WRF v4.8.0 | `dyn_em/module_em.F:773` → `curvature` | `rk_tendency` applies the Earth-curvature momentum terms unconditionally, including idealized Cartesian f-plane cases. em_scm_xy gets (u²+v²)/a = 1.41e-5 m/s² in w (a few % of the w tendency) and −u·w/a, −v·w/a in u and v. | `earth_curvature.esm`. | n/a |
| N67 (note) | wrf-model/WRF v4.8.0, REAL*4 builds | `calc_p_rho_phi` → `pg_buoy_w` | The single-precision equation of state leaves p′ with 1.2–1.6e-2 Pa rounding. The 1/Δη pressure gradient turns that into a w tendency of up to 3.0e-4 m/s² against a physical maximum of 4.7–5.9e-4, so a real32 column's w is largely rounding-driven. | Couplings bound total dw/dt at 1e-3; the component matches pg_buoy_w to 6e-9 when fed WRF's p′. | n/a |
| N68 (note) | wrf-model/WRF v4.8.0 | `phys/module_radiation_driver.F:3498-3499` (radconst) | For JULIAN < 80 the sun longitude is formed as DPD*(JULIAN+285.) ≈ 360 degrees instead of the equivalent small negative angle; in REAL*4 the near-360 value keeps ~3e-5 degrees, so just before day 80 the declination is off by ~1e-7 rad (1.7e-4 relative at julian 79.9). Physically negligible; the branch is redundant since 365*DPD = 360. | `wrf_solar_geometry.esm` keeps both branches; the equinox-edge test bounds declin at abs 5e-7. | n/a |
| N69 (note) | wrf-model/WRF v4.8.0, REAL*4 builds | `phys/module_radiation_driver.F:3527-3537` (calc_coszen) | tloctm = gmt + xt24/60 + xlon/15 is ~30 h in the em_scm_xy case, so its REAL*4 granularity (3.8e-6 h, 1e-6 rad of hour angle) moves coszen by up to 7.7e-7 near sunrise and sunset; 1e-8–1.4e-7 elsewhere. | Crossing-step tests bound coszen at abs 2e-6. | n/a |
| B13 | wrf-model/WRF v4.8.0 | `phys/module_radiation_driver.F:3939-3956` (SUBROUTINE cal_cldfra1) | Dead store over uninitialised locals. The `ELSE` arm at 3939 — taken when `F_QI`, `F_QC` and `F_QS` are not all PRESENT — sets `CLDFRA(i,k,j)=0.`, but the block after `ENDIF ifouter` at 3942 unconditionally recomputes `CLDFRA` from `QCLD` and `weight`, so that assignment can never survive. In that same arm `QCLD` and `weight` are never assigned: they are plain scalar locals, so line 3945 (`QVS_WEIGHT`) and line 3954 (`IF (QCLD .LT. QCLDMIN)`) read an undefined value on the first iteration and the previous cell's value afterwards, i.e. the cloud fraction of one column leaks into the next. The same hole exists INSIDE the true arm: the five inner branches cover (QI,QC,QS), (QI,QC,no QS), (QC only), (QC,QS with F_ICE_PHY) and the Ferrier `mp_physics` test, so a microphysics option with `F_QC` false leaves both locals unset as well. Harmless for every option WRF actually ships through this path with `icloud = 1` (all of them set at least `F_QC`), which is presumably why it has survived. Noticed while transcribing cal_cldfra1 for `components/atmospheric_radiation/cloud_fraction/cal_cldfra.esm`. | The component transcribes only the `F_QC .and. F_QI .and. F_QS` branch — the one the em_scm_xy and em_quarter_ss reference runs take — and does not reproduce the ELSE arm; its description says so. | not yet |
| B12 | wrf-model/WRF v4.8.0 @ c01746f, NCAR/fire_behavior @ c9dda02 | `dyn_em/start_em.F:2251-2302`, `dyn_em/module_first_rk_step_part1.F:1633-1639`, `phys/fire_behavior/driver/initialize_mod.F90:146-190` against `Registry/registry.fire:29,39,72-79,81-82,97` | The CFBM (`ifire = 1`) ignores `config_flags%restart` entirely, so a restarted run silently resets the fire to unburned. `Registry/registry.fire` flags the full fire state for restart — `lfn`, `lfn_hist`, `lfn_0..2`, `lfn_s0..s3`, `tign_g`, `fuel_frac`, `fire_area`, `fmc_g` all carry `r` — and WRF does write and re-read them into `grid%*`, so a restart *appears* supported. But those arrays are only ever written *by* the fire: the sole copy (`module_first_rk_step_part1.F:1633-1639`) runs one way, `grid%fire_state%x -> grid%x`, and no `grid%x -> grid%fire_state%x` assignment exists anywhere in the tree (`grep -rn "fire_state%\(lfn\|tign_g\|fuel_frac\|fire_area\)[^)]*="` over `dyn_em/ share/ main/ frame/` returns nothing). On restart `start_em.F:2290` calls `Init_fire_state_within_wrf` unconditionally — its only guard is the saved-variable `is_cfbm_initialized`, false in every fresh process — and that resets `state%lfn`/`tign_g`/`fuel_frac` to their unignited defaults, discarding the restored arrays at the first step. The ignition is re-anchored too: `start_em.F:2277-2288` overwrites the `namelist.cfbm` `&time` start with the *segment's* `namelist.input` start, so a fixed `fire_ignition_start_time1` re-fires in every segment. The legacy WRF-Fire path (`ifire = 2`) does thread restart correctly (`phys/module_fr_fire_driver.F:130,703`), which is why the registry plumbing is complete and why the gap is easy to miss. Noticed in Phase 3.3 while about to submit the Last Chance reference case as a 19-segment restart chain on a 4 h-wall partition; the chain would have reset the fire 18 times and produced 19 disjoint 45-minute fires. | The Last Chance reference run must be a single continuous job (3 d wall) rather than a restart chain, or carry a fix. Any CFBM test tuples taken from a restarted run would be invalid. A correct fix must also restore `lfn_hist`, `fmc_g` and the `fire_*_old` previous-step atmospheric fields, which `state_fire_t` holds but the one-way copy never exports. | not yet |
| N70 (note) | wrf-model/WRF v4.8.0, REAL*4 builds | `dyn_em/module_big_step_utilities_em.F` `calc_p_rho_phi` → `pg_buoy_w` | The REAL*4 state at a mid-run step is not in float64 hydrostatic balance, and the imbalance is the same size as the entire physical signal in w. Substituting the exactly balanced φ′ — obtained from the step's OWN θm, q_t, μ′, μ_b by WRF's own relations (integrate `dp′/ds = −(1+q_m)μ′ − q_m μ_b` down from the lid with `q_m` the interface total water including `pg_buoy_w`'s top extrapolation of `cqw`; invert `p = p0 (R_d θm/(p0 α_d))^(cp/cv)` for α_d; integrate `dφ′/ds = μ_d α′ + α_b μ′` up from φ′(surface) = 0) — for WRF's dumped `ph` at step 1410 of the em_scm_xy column moves φ′ by at most 0.050 m²/s² (5.1 mm of interface height out of an 11979 m column) and p′ by at most 0.303 Pa, and takes the vertical pressure-gradient-plus-buoyancy tendency from 4.23e-4 m/s² to 3.3e-11. N67 bounds only the p′ ROUNDING (1.2–1.6e-2 Pa → up to 3.0e-4 m/s²); this note is the full departure, of which the rounding is one part. Balanced profile saved at `data/eqwefic/phase3_probes/phi_p_hydrostatic_balanced_step1410.{json,npy}`. | Two consequences for EqWeather-SCM. (i) w cannot be matched to a REAL*4 reference at this step at all: essentially the whole reference w tendency is the column's departure from balance. `couplings/eqweather_scm_dynamics.esm`'s `hydrostatic_balance` test asserts that from the balanced state the total w tendency reduces to exactly the non-pressure part (Coriolis + curvature + sponge) at Linf 3.28e-11 m/s², bounded at abs 1e-10. (ii) An esm column started from a REAL*4 state rings acoustically, but balancing is NOT a fix: over a 600 s re-run from the balanced state max\|w\| falls only 9.35e-4 → 6.81e-4 m/s (−27 %) and wall time 164.9 → 125.7 s (−24 %), while u, v and θm agree with the unbalanced run to eight significant figures; at t = 600 s max\|dw/dt\| is 2.77e-4 balanced against 2.82e-4 unbalanced. The column forgets its initial condition and settles at its own acoustic level, so the mode is permanent and equation-level, not a startup transient. WRF's counterpart is the off-centring parameter `epssm` of its split-explicit acoustic sub-step, a property of the time scheme with nothing to transcribe. | n/a (precision/time-scheme property, not a coding error) |
| B4 | wrf-model/WRF v4.8.0 | `chem/module_dep_simple.F:963-964` vs `:1031, :1034` | The SAME Wesely (1989) cuticle conductance term is spelled two different ways 70 lines apart. The O3 reference resistances are `rluo1 = 1./(1./3000. + 3./rlu(iland,iseason))` and `rluo2 = 1./(1./1000. + 3./rlu(...))`, i.e. `3/rlu`; the general-gas wet and rain branches write `rlux = 1./(1./(3.*rlu(iland,iseason)) + ...)`, i.e. `1/(3 rlu)`. The two differ by a factor of **9** in that term. Wesely Eqs. 11/13/14 use `1/(3 rlu)`, so `:963-964` look like the typo. Numbers: at `rlu = 2000`, `rluo1` is 545.45 s/m as written and 2000.0 s/m in the `:1031` spelling; at `rlu = 9000`, 1500.0 vs 2700.0 s/m. `rluo1`/`rluo2` are used for O3 under dew/rain (`:1051-1052`) and, through `f0(n)/rluo1`, for every gas with a non-zero reactivity. | Any `.esm` transcription must pick one and say which. **NOW VERIFIED LIVE (2026-09-21).** An `ESM_SWEEP` hook added to `wesely_driver` calls `rc()` over every land-use category, season and wetness state; `components/atmospheric_deposition/wrf_wesely/surface_resistance.esm` transcribes each site exactly as WRF spells it and reproduces the sweep in **1040 wet/rain/snow assertions, 0 fail**. So the inconsistency is present in the running model, not just in the source. Measured: at `rlu = 2000` (deciduous/cropland, season 1) `rluo1` is **545.45 s/m** as WRF writes it and **2000.0 s/m** under the Eq. 11 spelling used 70 lines later, a factor of **3.667** in the resistance; at `rlu = 9000` (season 2) it is 1500.0 vs 2700.0, a factor of 1.800. O3 under dew takes `rluo1` OUTRIGHT as its cuticle resistance, so the discrepancy passes straight into Rc: WRF gives Rc(O3, dew) = 80.07 s/m over deciduous broadleaf, 70.15 over cropland, 102.70 over grassland. | not yet |
| B5 (note) | wrf-model/WRF v4.8.0 | `chem/module_dep_simple.F:927, 945, 962` | `rdtheta` is declared, set to `0.` and never assigned again, so the terrain-slope factor of Wesely Eq. 5, `rdc = 100 (1 + 1000/(rad+10)) / (1 + 1000 rdtheta)`, is permanently 1. The in-canopy buoyant-convection resistance therefore has no slope dependence at all. | Transcribe `rdc` without the slope term, or carry `rdtheta` as a parameter defaulting to 0 and note that WRF cannot set it. | not yet |
| B6 (note) | wrf-model/WRF v4.8.0 | `chem/module_dep_simple.F:1585-1587` | The comment above `depvel` says "SCPR23 is calculated as (Sc/Pr)**(2/3) using Sc= 1.15 and Pr= 1.0 / SCPR23 = 1.10". The code at `:3487-3488` instead uses a per-species Schmidt number `sc = 0.15/dvj(l)` and **Pr = 0.72**: `scpr23(l) = (sc/0.72)**(2./3.)`. The comment is stale and contradicts the code by a factor of `(1/0.72)^(2/3) = 1.2445` on Rb. | Follow the code, not the comment; `scpr23` is per species and Pr = 0.72. | not yet |
| B7 (note) | wrf-model/WRF v4.8.0 | `chem/module_dep_simple.F:178-185, 237` | For every non-MOZART mechanism (all of RADM2, RACM, CBMZ) `iseason` is only ever 2 (`julday < 90 .or. julday > 270`) or 1, and the snow override is commented out (`! if(snowc(i,j).gt.0.)iseason=4`). Seasons 3, 4 and 5 of all seven 25x5 land-use tables, and every `iseason == 4` branch inside `rc`, are dead code in a RADM2 run. | A RADM2-targeted `.esm` only needs seasons 1 and 2, and must not expose a winter branch WRF can never take. | n/a |
| B8 (note) | wrf-model/WRF v4.8.0 | `chem/module_phot_mad.F:1541-1545` | In `madronich1_driver` the aerosol-extinction column is filled at index `k+1` on the clear path (`aerext(k+1)=aerwrf(i,k+1,j)`) but at index `k` on the aerosol path (`aerext(k)=pm2_5_dry(i,k,j)*bext340+...`), inside the same `do k=kts,kte` loop. WRF's own comment two lines above is `!rf check ki (or ki+1 or ki-1 ?)`. With aerosols on, the profile is shifted one level against the clear-sky convention. | Not exercised here: `chem_opt = 101` sets `haveaer = .false.` (`chem_driver.F:461-463`), so the reference column takes the `aerwrf` path only. Must be resolved before any aerosol-coupled photolysis transcription. | not yet |
| B9 (note) | wrf-model/WRF v4.8.0 | `chem/module_phot_mad.F:2676` + `chem/module_data_radm2.F:123` + `share/module_model_constants.F:72` | Three different values of pi are live on one code path. `calc_zenith` defines its own local `pi = 3.1415926535590` and converts every angle with it; `madronich1_driver` computes `zenita = cos(zenith*pi/180.)` with the `pi = 3.141592654` it inherits from `module_data_radm2`; WRF's own `piconst = 3.141592653589793` is used by neither. Relative errors against the true pi are 4e-11 and 2e-10. | Below the 1e-5 single-precision test tolerance, but transcribed faithfully as `par.pi_zenith` and `par.pi_driver` in `components/gaschem/madronich/madronich_parameters.esm` so that a future real64 kernel reference still matches. | n/a |
| N22 (note) | wrf-model/WRF v4.8.0 | `chem/module_phot_mad.F:2021-2101, 1722` | The **entire O2 Schumann-Runge parameterization is dead code** as shipped. `srband`'s wavelength loop opens with `IF (wl(kl)>205.) RETURN` and the loop starts at `kl0 = 30`, where `wl(30) = 254.8 nm`, so it returns before applying anything. Independently, `xs(kl,1)` (the O2 cross section) is **exactly 0.0 in every one of the 101 active bins** — it is non-zero only in bins 1-29, which `kl0 = 30` excludes. Measured on the reference column: `ao2` is 0 over all active bins and `d(1,lev)`, the O2 photolysis rate, is identically zero. `phot1(n) = d(n+1)` drops that row regardless. The `sra(11,9)`/`srb(11,5)` tables and the whole `cvo2` column exist to feed it. | No Schumann-Runge component was written; `layer_columns.esm` still computes `cvo2` (it is a faithful intermediate) and says in its description that nothing consumes it. Reactivating the branch means `kl0 = 1`, which WRF's own comment at `:1722` contemplates. | not yet |
| N23 (note) | wrf-model/WRF v4.8.0 | `chem/module_phot_mad.F` `xs(:,20)`, `xs(:,18)` | The comments `ch3coo2h 20 actually use 0.28*(h2o2 value)` and `hcoch=chcho 18 estimate, no reliable measurement` describe the PROVENANCE of two stored `xs` columns, not relations the code evaluates. Measured from the dumped table, `xs(:,20)/xs(:,10)` over the 60 bins where both are non-zero is **not constant**: it ranges 0.1414 to 0.2874, clustering near 0.276. | Both are transcribed as tables in `j_values.esm`. Noted because an implementation built from the literature cannot match: no measured peroxyacetic-acid spectrum is 0.276 times hydrogen peroxide's. | n/a |
| N24 (note) | wrf-model/WRF v4.8.0 | `chem/module_phot_mad.F:2213-2310` (`subgrid`) | The cloud-layer insertion makes the radiative-transfer grid SIZE state-dependent: `max(int(cloud*dz*0.02),1)` extra levels per cloudy model layer, giving `nlevel` = 70, 72, 77, 78 at the four dumped daytime calls of the reference run. | Not a Fortran defect, but a format boundary: an esm `index_sets` size is fixed at load, so the Madronich components here are written for and tested on the CLEAR column only. Representing the cloudy case needs a fixed maximum grid with masking or an adaptive-grid discretization — a design decision, not a transcription. | n/a |

| B14 | NCAR/fire_behavior @ eb77580 | `nuopc/fire_behavior_nuopc.F90:919-922` against `state/state_mod.F90:87, :176` and `physics/fmc_wrffire_mod.F90:62, :300` | The NUOPC coupler accumulates `fire_rain = fire_rain + ptr_rainrte * ts` with the comment "convert m s-1 to m and accumulate", i.e. in METRES, but `fire_rain` is declared "Accumulated total rain" in MILLIMETRES and the moisture model's thresholds are `rain_threshold = 0.05` and `saturation_rain = 8.0` mm/h. A missing factor of 1000 makes `rain_int` 1000x too small, so in a NUOPC-coupled run rain below 50 mm/h never crosses the threshold and the wetting branch is effectively unreachable. Separately, `fire_rain` is only `allocate`d (`state_mod.F90:176`) and never zeroed, so the `+=` starts from uninitialised memory; the `imp_rainacc` path at `:922` assigns directly with no conversion at all. Not exercised by the WRF path (`state_mod.F90:844` reads WRF's own `rain`), which is why it has survived. | None for this repo's tests (every reference run is rain-free), but any future NUOPC-derived moisture reference is invalid until it is fixed. | not yet |
| B15 | NCAR/fire_behavior @ eb77580 | `physics/fmc_wrffire_mod.F90:339, :348` with `:85-87` | Both `select case` blocks in `Advance_moisture_classes` have only `case (1)` and NO `case default`: if `wetting_model(k)` or `drying_model(k)` were ever not 1, `rlag`, `emc_d` and `emc_w` fall through undefined into the `if (rlag > 0.0)` test at `:367`. The arrays are `parameter`s set to all-1 today, so the fall-through is unreachable, but the structure is one namelist option away from reading uninitialised locals. In the same routine `fmc_gc`, `fmc_equi` and `fmc_lag` are allocated at `:85-87` and never initialised (contrast `this%fmep = 0.0` at `:89`), so memory cells outside `ifts:ifte, jfts:jfte` hold garbage -- visible in every dump as a `1.87e13` in the halo. | Neither is transcribed: the .esm has one drying and one wetting form, and `fmc_old` is a coupling input rather than component-owned state. | not yet |
| N71 (note) | NCAR/fire_behavior @ eb77580 | `physics/fmc_wrffire_mod.F90:342-343` | In the wetting branch both ends of the equilibrium bracket are set to the same value, `emc_w = emc_d = saturation_moisture(k) + deltas = 2.5 + deltas`, so `equi = max(min(fmc_old, emc_d), emc_w)` at `:384` collapses to 2.5 regardless of where the class started: during rain every class is driven towards 250 % moisture and the bracket logic that makes the drying branch hysteretic does nothing. Combined with `fmc_gc_initialization(k) = 2` ("from computed equilibrium", `:66`, `:376`), a first step that happens to be raining initialises the dead classes at 2.5 rather than at a drying equilibrium of order 0.01. | Transcribed as written; the four wetting tests of `fuel_moisture_wrffire.esm` assert `emc_d = emc_w = 2.5` explicitly so the collapse is pinned rather than assumed. The initialisation branches are not transcribed (`fmc_old` is an input). | n/a |
| N72 (note) | NCAR/fire_behavior @ eb77580, real32 builds | `physics/fmc_wrffire_mod.F90:344` with the `if (rlag > 0.0)` gate at `:367` | The wetting rate is `rlag = rec_wetting_lag_sec(k) (1 - exp(-r / 8))`. Just above the threshold `-r/8` is small enough that `exp` rounds to exactly 1.0 in single precision, `rlag` is exactly 0, and the `if (rlag > 0.0)` gate then SKIPS THE CLASS ENTIRELY -- it neither wets nor dries, `fmc_equi` and `fmc_lag` keep the previous step's values, and the ESM_DUMP diagnostic arrays (written only inside that gate) keep their initialised 0, so a dump of such a cell records "not written", NOT "zero". Measured in `data/eqwefic/fire_build/fmc_rain`: a cell whose accumulated rain gives `rain_int = 0.0500000045` mm/h has `r = 3.7e-9`, `rlag = 0` and `fmc_gc` unchanged at 0.05, while the identical cell one call earlier (`rain_int = 0.0500000007`, `r = 0` exactly) took the drying branch and moved. The model is therefore discontinuous at the threshold in a way the source does not show: as `r -> 0+` it stops drying before it starts wetting. The band is narrow (about `r < 6e-8` mm/h) so nothing physical rides on it, but it means a threshold-edge cell's dumped `emc_*`/`rlag`/`equi`/`change` must never be used as expected values. | The .esm has no `rlag > 0` gate, so it computes `change = dt rlag = 0` and returns `fmc_new = fmc_old` -- the same answer for the state, by a different route. Its `equi` is 2.5 where the dump shows 0; the wetting tests deliberately avoid the threshold-edge cell. | not yet |
| N73 (note) | NCAR/fire_behavior @ eb77580 | `physics/fmc_wrffire_mod.F90:229-230` | `Average_moisture_classes` does `n = nfuel_cat(i, j)` (real to default integer, truncating) and indexes `this%fmc_gw(n, k)`, which is dimensioned `(1:mfuelcats, :)`, with no bounds check. A no-data or halo cell with `nfuel_cat = 0` reads out of bounds. The dumps show `nfuel_cat` does contain 0.0 and uninitialised garbage outside the tile, so this is reachable the moment the loop bounds are widened. Same shape as B6 (`Apply_wafs` indexing `waf(1:13)` with category 14). | The .esm takes `class_weight` as a coupling input, so no lookup happens inside the component. | not yet |
| N74 (note) | NCAR/fire_behavior @ eb77580 | `physics/level_set_mod.F90:503-545, :584-626, :664-706, :740, :755-760` | `tbound` is NOT a CFL reduction, despite the name. `Prop_level_set` takes the minimum over the three Runge-Kutta stages and over MPI tasks, and the only thing it then does with it is `if (dt > tbound) call Stop_simulation('CFL violation: ...')`. It never shortens the step, never sub-cycles, and never enters `tend`: `Calc_tend_ls` computes the tendency first and derives `tbound` from the same quantities afterwards. It therefore has NO effect whatever on the instantaneous tendency d(psi)/dt, which is why `level_set_tendency_wrffire.esm` does not transcribe it. What would be lost by omitting it is the run-abort, i.e. a stability guard on the INTEGRATOR, which belongs to whatever time scheme a consumer wraps the tendency in. | Deliberately not transcribed; recorded here so the omission is a decision rather than a gap. | n/a |
| N75 (note) | NCAR/fire_behavior @ eb77580 | `physics/fmc_wrffire_mod.F90:283-289, :298-299, :324-325` | `Advance_moisture_classes` nests its loops `j -> k (moisture class) -> i`, so every quantity that does not depend on the class -- `rain_diff`, `rain_int`, the second-order time means of t/p/q, `pw`, `pws`, `rh`, and the write of `rh_fire(i, j)` -- is recomputed five times per cell and `rh_fire` is written five times with the same value. `rhmax`, `rhmin`, `rainmax` and `rainmin` are initialised and updated but never read or printed (and count each cell five times). `rh_fire` is `intent(out)` but only written inside the tile, so its halo is undefined on return. | Correctness unaffected; the .esm computes the class-independent quantities once. | n/a |
| N76 (note) | NCAR/fire_behavior @ eb77580 | `io/namelist_mod.F90:56` with `physics/fire_model_mod.F90:103-116` | `fire_lsm_reinit` DEFAULTS TO `.true.`, and neither `tests/test7/namelist.fire` nor the Last Chance Gulch `namelist.cfbm` sets it, so level-set reinitialisation (`Reinit_level_set`, and `Reinit_level_set_fast_dist` when `fast_dist_reinit_opt > 0`) runs in every reference run this repo uses. It is applied to `lfn` between the propagation and the ignition-time update, i.e. it is an operator applied to the STATE and not a term in d(psi)/dt. | `level_set_tendency_wrffire.esm` transcribes the tendency and is unaffected, and the level-set inputs of every test are the already-reinitialised `lfn` the Fortran hands to `Calc_tend_ls`. But a long .esm integration of the level set will drift from WRF's, because psi is never re-signed-distanced. Not transcribed; the reinitialisation PDE (`fire_upwinding_reinit`, default 4 = hybrid WENO5/ENO1, `reinit_pseudot_coef`) is its own component. | n/a |
| B16 | NCAR/MMM-physics @ 550b5b4 | `mp_wsm6.F90:729` `nislfv_rain_plm` (called for cloud ice) | **The semi-Lagrangian fallout DESTROYS the contents of the TOP TWO LAYERS OF THE COLUMN on every call, by a dt-INDEPENDENT amount, and the mass does not appear at the surface.** (Re-characterised 2026-09-22: it is the top two layers of the model column, not the top of the falling hydrometeor layer. WRF's `nislfv_rain_plm`, extracted verbatim and driven on five synthetic 12-layer columns at dt = 0.5-4 ms, left layers 11 and 12 at EXACTLY zero in all five, including a top-loaded column holding 7.0e-3 and 8.0e-3 there; every layer below converges normally.) Measured on the em_scm_xy cirrus column (call 120, real64 `kernels/wsm6_driver`): the column ice budget loses **4.7542e-10 kg m⁻² per sub-step at dtcld = 0.01, 0.02 and 0.04 s alike** (rates −4.754e-08 / −2.377e-08 / −1.189e-08 kg m⁻² s⁻¹), while `fallc(1)`, the surface flux, is exactly 0. The lost mass is precisely the burden of the two top ice layers: level 58 holds 4.7542e-10 kg m⁻² and level 59 holds 1.1236e-15, and both are emptied to zero every sub-step. The routine's remap drops whatever lies outside the arrival-point range (`qa(km+1) = 0`, and the `intp` loop exits at `zi(k) >= za(km+1)`), so the top edge of the profile is not conserved. Physically negligible here — 1.5e-08 of the column burden per sub-step, 4.7e-10 kg m⁻² per 60 s model step — but it is a genuine conservation failure and it is the reason the ice fallout tendency has no dt → 0 limit at the topmost cloudy level (level 58: −1.88e-10 / −9.42e-11 / −4.71e-11 K s⁻¹-equivalent at dtcld = 0.01/0.02/0.04, i.e. a fixed increment ∝ 1/dtcld, exactly like `pigen`). | Bounds how well any continuous sedimentation operator can match WRF at the cloud-top edge: the dt → 0 operator of N77 (EarthSciDiscretizations PR #42) reproduces WRF at every level to 5 significant figures EXCEPT the top two, where the irreducible residual is this deleted mass (1.88e-10, 0.54 % of the column peak). Since 2026-09-22 `wsm6/sedimentation.esm` mounts that rule, which keeps the top two layers conservative, so its four tendency tests take the RULE's value in the top two layers and WRF's dt → 0 limit everywhere below (every category carries < 1e-12 kg m⁻³ there in those columns), and state so. | not reproduced (deliberately) |
| N77 (note) | NCAR/MMM-physics @ 550b5b4 | `mp_wsm6.F90:1879-2122` `nislfv_rain_plm` | **What WSM6's fallout is the dt → 0 limit of, measured.** The scheme is a forward semi-Lagrangian remap, and its converged operator is a SECOND-ORDER flux divergence with two ingredients that a first-order donor-cell scheme does not have. (1) The interface fall speed is a THIRD-ORDER interpolation of the layer-centred speed, `wi(k) = 9/16(ww(k)+ww(k-1)) − 1/16(ww(k+1)+ww(k-2))` for k = 3..km−1, with `wi(1) = ww(1)`, `wi(2) = ½(ww(2)+ww(1))`, `wi(km) = ½(ww(km)+ww(km−1))`, `wi(km+1) = ww(km)`, then the override `if ww(k) == 0 then wi(k) = ww(k−1)` ("terminate of top of raingroup") and, at finite dt only, the `decfl > 0.05` clamp. (2) The donor face value is a monotone piecewise-linear reconstruction whose slope is the AVERAGE of the two one-sided differences, not minmod and not MC: `dip = (qa(k+1)−qa(k))/(dza(k+1)+dza(k))`, `dim = (qa(k)−qa(k−1))/(dza(k−1)+dza(k))`; if `dip·dim ≤ 0` the slope is zeroed, else `qpi = qa + ½(dip+dim)dza` and `qmi = 2qa − qpi`, with both reset to `qa` if either goes negative. Verified: evaluating `F(k) = wi(k)·qmi(k)` and `dm/dt = (F(k+1) − F(k))/dz` in float64 on the dumped call-120 state reproduces the kernel's converged ice tendency at EVERY level to 5 significant figures (Linf difference 1.88e-10 against a peak of 3.49e-08, and that residual is entirely B16's deleted level-58 mass). A textbook minmod PLM does NOT reproduce it (31.6 % Linf difference), and neither does the interface velocity alone (37.2 %). (3) **Found 2026-09-22 while mounting the rule: the flux takes its face value from the UPWIND side, which is not always the layer above.** The third-order `wi` overshoots BELOW ZERO at the edge of a falling layer (a face whose lower neighbour is empty or much slower: 3-12 faces per column in the WSM6 sedimentation tests, all at the edges of ice layers). There `za(k) = zi(k) − wi(k)dt` lies ABOVE `zi(k)` and the remap draws the mass crossing face k from the TOP face value `qpi(k−1)` of the layer BELOW, so the dt → 0 flux is `wi(k)·qmi(k)` for `wi(k) ≥ 0` and `wi(k)·qpi(k−1)` for `wi(k) < 0`. Evaluating the formula without that switch leaves 5.6e-7 to 6.4e-6 of the column peak in the ice tendency of the four tests; with it, 1.0e-8 to 3.9e-8. Rain, snow and graupel have no negative `wi` in those columns and agree to 2e-10 to 4e-9 either way. | This is the specification of EarthSciDiscretizations PR #42 (`sedimentation_jh2010_flux_D_lev`), mounted in `wsm6/sedimentation.esm` on 2026-09-22. The rule as opened transcribed (1) and (2) but not (3); (3) was added to #42 on 2026-09-22 (ESD `14b5db5`, stencil `sedimentation_jh2010_face_flux`), which took the component's ice tendency residuals from 1.1e-6..5.9e-6 to 4.4e-8..2.7e-7 of the column maximum, so ice now carries the same 1e-6 tolerance as rain, snow and graupel. The donor-cell `sedimentation_upwind1_flux_D_lev` it replaced differs by 16-112 % of the column maximum in the same tests. | reproduced (all three) |
| B19 | NCAR/fire_behavior @ eb77580 | `physics/level_set_mod.F90:1019, :1039, :1061` against `:1278, :1298` | The band test that chooses WENO5 over ENO1 in the level-set REINITIALISATION is missing its `abs()`. `Advance_ls_reinit` writes `if (lfn_curr(i, j) < threshold_hlu)` in `fire_upwinding_reinit` cases 3, 4 and default, whereas `Calc_tend_ls` writes `if (abs (lfn(i, j)) < threshold_hlu)` in the corresponding `fire_upwinding` cases 8 and 9. `threshold_hlu = fire_lsm_band_ngp * dx` is meant to be a band of a few cells AROUND the front, so the signed test puts every cell with a level set below `+threshold_hlu` on the fifth-order branch -- including the entire BURNED INTERIOR, where the level set is large and negative and where the WENO5 stencil's premise (a smooth field near a zero crossing) does not hold. How much it is exercised: on test7 at step 20 the level set runs from -112 m to +2042 m, so only 2 of the 6400 interior cells have psi < -100 m and the bug changes almost nothing there; it bites on a mature fire with a large burned area, e.g. the Last Chance Gulch d05 run at 12 h, where `fuel_frac` has reached 0 over thousands of cells. | Transcribed AS WRITTEN, not corrected: `EarthSciDiscretizations-fire` `grids/cartesian_uniform_2d/stencils/weno5_eno1_signed_D1{x,y}_interior.esm` use the signed test and say in their descriptions why, and `components/wildland_fire/fire_behavior/level_set_reinit_wrffire.esm` inherits it. A transcription that silently inserted the `abs` would not reproduce the model. | not yet |
| N77 (note) | NCAR/fire_behavior @ eb77580 | `physics/level_set_mod.F90:838` (`dt_s = reinit_pseudot_coef * dx`) with `:1085-1087` | The reinitialisation's pseudo-time bookkeeping is dimensionally misnamed. `dt_s` is called a time step but is `reinit_pseudot_coef` (dimensionless) times `dx` (metres), i.e. a LENGTH; and `tend_r = lfn_s0 (1 - grad)` is DIMENSIONLESS, since `lfn_s0` is a smoothed sign and `grad` is metres of `lfn` per metre of distance. The update `lfn_fin = lfn_ini + (dt_s * rk_coeff) * tend_r` is therefore metres + metres and perfectly consistent -- the pseudo-time variable simply has units of length -- but nothing in the code says so. | `level_set_reinit_wrffire.esm` declares `tau_step` in `m` and `dpsi_dtau` in `1` rather than inventing a second, and its description says why. | n/a |
| N78 (note) | NCAR/fire_behavior @ eb77580 | `physics/level_set_mod.F90` Runge-Kutta stage 3 of `Reinit_level_set` | At stage 3 the caller passes `lfn_s3` as BOTH the `lfn_ini` and the `lfn_fin` dummy of `Advance_ls_reinit`, so the two arguments alias. Any instrumentation that dumps them at the end of the routine reads `lfn_ini` after `lfn_fin` has been written, and the stage-3 pair therefore differs by exactly zero. Stages 1 and 2 do not alias. | The `ls_reinit` dump hook (fork `66a800e`) records this in its own commit message; `level_set_reinit_wrffire.esm` tests stages 1 and 2 only, and its stage-3 data is used only for the net-change measurement, where `lfn_fin` is still the correct final field. | n/a |
| N79 (note) | NCAR/fire_behavior @ eb77580 | `io/namelist_mod.F90:61` against `tests/test7/namelist.fire` and the Last Chance Gulch `namelist.cfbm` | `reinit_pseudot_coef` defaults to **0.01** in `namelist_mod`, but every shipped namelist this repo has sets **0.0001**, a hundred times smaller, with no comment explaining the discrepancy. Measured consequence on test7 step 20 (`data/eqwefic/dumps/fire/run_reinit` against `run_reinit2`): at 1e-4 one reinitialisation of one WRF step moves the level set by at most **5.7 mm** and on average 0.14 mm, 2.3e-4 of the 25 m cell, while `rms(|grad psi| - 1)` over the domain is 0.177 -- i.e. the field is a long way from a signed distance function and the reinitialisation is very nearly inert. At the module default 0.01 the same step moves it **0.19 m per Runge-Kutta stage and 0.57 m per step**, 2.3 % of a cell. So whether reinitialisation matters at all depends entirely on which of the two values a run happens to get. | Both regimes are tested: `level_set_reinit_wrffire.esm` has the shipped 1e-4 case and a 1e-2 case, and it is only in the 1e-2 case that the frozen sign is observable at all (the shipped case cannot distinguish a frozen from a recomputed `lfn_s0` at any tolerance the reference's precision allows). | not yet |
| N80 (note) | NCAR/fire_behavior @ eb77580 | `physics/level_set_mod.F90:1162, :1179` and `:2136`, `:2192` | Three dead variables in the level-set upwinding, all harmless but all misleading to a transcriber. (1) `threshold_hll = -fire_lsm_band_ngp * dx` is computed at `:1179` and never read anywhere in the file — the band is one-sided in the source and two-sided only because every test that uses it is written on `abs(lfn)`; a reader who takes the `hll`/`hlu` pair at face value will look for a signed band that does not exist. (2) `Select_weno3` declares a local `lfn_ip2` at `:2136` that is never assigned in either branch and never read; in the `uf >= 0` branch the DUMMY `lfn_ip2t` is unused too, so the positive-selector reconstruction is a four-point stencil `{i-2, i-1, i, i+1}` even though the call site passes five values. (3) `Select_weno5` has the same dead local `lfn_ip3` at `:2192` (the `uf >= 0` branch never assigns it and nothing reads it), with `lfn_ip3t` likewise unused on that side. | Transcribed as the math actually is: `weno3_js_upwind_D1` takes five parameters and reads four in each branch, and its description says which; `weno5_js_upwind_D1` (PR #37) already did the same with seven. `threshold_hll` is not transcribed. | not yet |
| N81 (note) | NCAR/fire_behavior @ eb77580 | `physics/level_set_mod.F90:1264-1294` (`case (6)`, `case (7)`, `case (8)`) against `:1296-1314` (`case (9)`) | The four WENO options of `Calc_tend_ls` are two schemes and a band test, spelled out four times. `case (7)` is `case (9)`'s in-band branch with `if (abs (lfn(i,j)) < threshold_hlu)` removed, and `case (6)` is `case (8)`'s in-band branch with the same test removed — both pairs byte-for-byte, down to the `a_valor`/`signo_x`/`signo_y` selector. So (6, 8) and (7, 9) are each one discretisation at two band widths, and pure WENO is the banded scheme with the band opened past the domain diagonal. | Load-bearing for this repo rather than a defect: option 7 needed no new EarthSciDiscretizations rule (it reuses the option-9 family with `weno_band_cells` past the diagonal), and for the same reason option 6 needs no new rule now that the WENO3 family exists (EarthSciDiscretizations PR #41) — only a rerun. `INVENTORY.md`'s statement that options 0-3, 5, 6 "each need their own rule family" is therefore no longer true of option 6, and that row has been corrected. | n/a |
| B10 | wrf-model/WRF v4.8.0 | `chem/dry_dep_driver.F` (the `do nv=2,num_chem` mixing loop) | `vertmx` is called over `kts..kte` and returns a mixed profile for all `kte` layers, but the write-back loop is `do k=kts,kte-1` — the **TOP model layer's mixed value is computed and discarded**, so vertical mixing never changes the top layer. The same `kte-1` bound is used for the column-mass check that diagnoses deposited mass, so the diagnostic is consistent with the write-back but not with what `vertmx` actually solved. | `components/gaschem/chem_transport/chem_vertical_mixing.esm` computes the tendency at every layer INCLUDING the top and says so in its description; a consumer reproducing WRF exactly must drop the top one. | not yet |
| N25 (note) | wrf-model/WRF v4.8.0 | `chem/module_vertmx_wrf.F` | The chem vertical-mixing solve is real32 and theta-weighted (`alfa_explicit = .25`, `beta_implicit = .75`). At a `dt` small enough for `(phi_out − phi_in)/dt` to approximate a rate, the change in a mixing ratio is **one or two real32 ulps**: measured on the reference column at `dt = 0.01 s`, most species give 1–4 ulps and CO gives exactly zero. An in-model dump therefore cannot supply an instantaneous tendency for this scheme at any usable precision. | Ground truth comes from a real64 kernel driver instead (`data/eqwefic/vertmx_kernel`; the module has 201 lines and NO dependencies, so it builds standalone in seconds). At `dt = 1e-3 s` in real64 the component matches to 1e-7 or better for every species whose tendency exceeds its own float64 rounding. A dumped `dt` ladder of 0.5/2/8/32 s is kept as an independent cross-check and agrees to about 1 percent, which is the theta-scheme's own O(dt) error. | n/a |
| B11 | wrf-model/WRF v4.8.0 | `chem/module_phot_mad.F:2245, 2274, 2304` | **The log-linear air-density interpolation onto cloud-inserted levels divides by a SUM of heights where a difference belongs.** All three sites read `hlocal = 1./alog(air(i-1)/air(i))` followed by `x0 = (z(lev)-zz(i-1))/(zz(i)+zz(i-1))` and `zair(lev) = air(i-1)*exp(-x0/hlocal)`. Because `hlocal` is a *normalized* scale height (in units of the layer thickness), `x0` must be the normalized offset `(z-zz(i-1))/(zz(i)-zz(i-1))`; with the sum it is neither a fraction nor scale-free — the same layer gives a different answer at 1 km and at 10 km. Two consequences, both measured on the reference column (call 1, a 6-layer cloud): the interpolation **does not return `air(i)` at `z = zz(i)`**, so on cloud-inserted grids the air density at levels that coincide EXACTLY with model levels is wrong by **4.9 % at model level 55 rising to 8.9 % at level 60** (it stays stuck near `air(i-1)`); and the error grows with altitude, which is the signature of the sum. `zair` feeds every pressure-quenched quantum yield, so it biases `j_ch2om`, `j_ch3cho`, `j_ch3coch3`, `j_ch3coc2h5`, `j_hcocho`, `j_ch3cocho` and `j_hcochob` inside cloud by up to ~8 %; `j_hcocho` was the worst channel at every cloudy level. Clear columns are untouched — the clear branch assigns `zair(lev) = air(i)` directly (`:2218`, `:2229`). | Must be REPRODUCED, not corrected, for the cloudy grid to match WRF; the Option-A component transcribes it as written and points here. It is also the reason the cloudy residual chase initially looked like a 7 % boundary error: evaluating the quantum yields with the MODEL air density instead of the RT-grid `zair` is correct for a clear column and wrong by up to 8 % for a cloudy one. | not yet |
| B17 | wrf-model/WRF v4.8.0 (NCAR/MMM-physics @ 52cfbd7), WPS 4.3 | `phys/physics_mmm/bl_gwdo.F90:416`; `Registry/Registry.EM_COMMON:1187` (`ELVMAX`, input stream); `share/module_check_a_mundo.F` | **GWDO is silently switched off everywhere when the input has no `ELVMAX`.** The 2024-11 revision of the scheme reads `omax` = `ELVMAX` (maximum sub-grid orographic height) and sets `ldrag = omax <= hmt_min` (50 m), meaning no drag. WPS releases whose `GEOGRID.TBL` has no `ELVMAX` entry (WPS 4.3 here; WPS computes it only in the post-geogrid `util/compute_gwdo.py`) produce `met_em` files without it, `real.exe` leaves it at 0, and `gwd_opt = 1` then runs with zero stress at every point. No check in `module_check_a_mundo.F` or `real.exe` warns about it. Found building the GWDO real-terrain reference run. | The reference run uses `compute_gwdo.py` fields copied into the `met_em` files (`data/eqwefic/notes/gwdo_real_reference_run.md`). A GWDO component must take `omax` as an input and document the `omax <= 50 m` off switch. | not yet |
| B18 | wrf-model/WRF v4.8.0 | `chem/module_mosaic_initmixrats.F:17, :80` (line numbers in pristine WRF; :85 in the fork after the fix) (`#define CASENAME 4`, and the `#if (CASENAME == 0) \|\| (1) \|\| (2) \|\| (3)` around the whole body of `mosaic_init_wrf_mixrats`) | **With the shipped `CASENAME 4`, MOSAIC's initial aerosol routine is compiled out entirely, so a `chem_in_opt = 0` run starts every aerosol bin, of every species, at exactly zero — silently.** `chemics_init.F:2000` still calls it, `aer_ic_opt` is still read, and nothing warns. Aerosol then exists only where MOSAIC nucleates it, which in a clean column is bin 1 alone. Noticed 2026-09-22 setting up the `chem_opt = 170` SCM reference run: every `so4_a01..a08` was 0 at ktau 1. | The fork branch `earthsciml-instrumented-chem170` (972ca76) admits `CASENAME 4` to the `#if`, so `aer_ic_opt = 1` gives `mosaic_init_wrf_mixrats_opt1`'s four constant lognormal modes (accumulation so4/nh4/oc/bc, Aitken, coarse dust, coarse sea salt) and all eight bins are populated. Any EqWeFiC MOSAIC reference data must say which initial-condition path produced it. | not reported |
| N83 (note) | wrf-model/WRF v4.8.0 | `chem/module_mosaic_initmixrats.F:299` (`aaprof_nsm(lldum_hysw,nsm) = aaprof_nsm( 9,nsm) * 1.0e-3`, coarse dust mode) | The other three modes set their hysteresis water from a NAMED component index (`lldum_so4`, `lldum_cl`); the dust mode hard-codes 9, meaning `oin`. The component order is per-mechanism: for `cbmz_mosaic` it is so4, no3, cl, co3, nh4, na, ca, oin, oc, bc (read off `massptr_aer` in the `mosaic_drydep` dump), so index 9 is `oc`, which is zero in that mode. The coarse dust mode therefore starts with no hysteresis water instead of 5e-4 of its `oin`. | Tiny (hysteresis water only, and only in the initial condition), but it makes the dust mode's initial state mechanism-dependent in a way the code does not intend. Transcribe the INTENDED form (`lldum_oin`) and note the difference if a test pins WRF's value. | not reported |
| N84 (note) | NCAR/MMM-physics (WRF 4.8 `phys/physics_mmm`) | `phys/module_cu_ntiedtke.F:552-562` (`cu_ntiedtke_post_run`: `rthcuten = (tf - t)/exner * rdelt`) | **New Tiedtke's in-model tendencies are cancellation-limited, not transcription-limited.** The kernel returns the updated state and the driver differences it in real32 over `delt` = 60 s. One ulp of a 300 K temperature is 3e-5 K, so `rthcuten` carries ~2.4e-7 K/s of pure round-off — measured as \|r64 − r32\| ≤ 8.9e-7 K/s over 16 dumped steps whose amplitudes run from 1e-6 to 2.6e-2 K/s, i.e. the whole signal at the quiet steps. | Reference values for a Tiedtke component must come from the real64 replay (`wrf-chem170/kernels/ntiedtke_driver`), with the in-model dump kept only as a cross-check. Same shape as N12/N69 elsewhere in this file. | not reported |
| N85 (note) | NCAR/MMM-physics @ 52cfbd7 | `bl_gwdo.F90:536` (`hd = velco*(2.*sqrt(temc)-temc)/brvf`, `temc = 2 + 1/sqrt(usqj)`) | **The saturated wave stress is conditioned by a difference that vanishes exactly at the critical Richardson number.** `2*sqrt(temc) - temc` is zero at `temc = 4`, i.e. at `usqj = Ri = 0.25 = ric`, which is the same value the branch test `rim <= ric` compares against, so the columns that take the saturation branch marginally are the ones where the factor is smallest. `taup(k+1)` goes as its square, so a real32 round-off of 1.7e-4 in `Ri` becomes 6.3e-3 in the factor and 1.2e-2 in the stress; the tendency is then a vertical difference of `taup`, which cancels again. Measured on row s2_j81_i76 column 31 level 11 of the GWDO reference run: the real64 and in-model real32 `dtaux3d` differ by 2.0e-2 of the cell value there, 1.5e-3 of the row maximum, with identical branch decisions. | GWDO stage-2 assertions on `dtaux3d`/`dtauy3d` cannot be pinned tighter than ~2e-3 of the row maximum against real32 in-model references; use the real64 replay as the reference and say so (`data/eqwefic/notes/gwdo_real_reference_run.md`). | not yet |
| N86 (note) | wrf-model/WRF v4.8.0 | `phys/module_ra_sw.F:173-184` (the `PRESENT(pm2_5_dry)` branch) and `:465-466` (`XSCA` gains `beta*aer_dry1*bext340*DZ + beta*aer_water1*bexth2o*DZ`), fed by `chem/chem_driver.F:1710` (`sum_pm_driver`, called every step) | **`aer_ra_feedback = 0` does NOT switch off aerosol-radiation coupling for the Dudhia shortwave scheme.** Dudhia adds the chem-derived PM2.5 dry and water mass to its layer scattering whenever `pm2_5_dry`/`pm2_5_water` are PRESENT, i.e. in every WRF-Chem run with an aerosol package, and `sum_pm_driver` refills them every time step. Only the RRTMG/Goddard optical-depth path is gated on `aer_ra_feedback`. Noticed 2026-09-22 because the `chem_opt = 170` SCM reference run's meteorology diverged from the identical `chem_opt = 0` run: bitwise identical at step 1, 0.04 K by step 60, ~2 K by step 601, enough to change which New Tiedtke convection type fires. Bisected with one binary: `chem_opt = 1` (RADM2, no aerosol package) is **bitwise identical** to `chem_opt = 0` on every dumped field at steps 1-5, while `chem_opt = 170` with gas chemistry, aerosol chemistry, photolysis, deposition and vertical mixing ALL switched off (only the aerosol initial condition present) already differs: layer scattering 1.2x (surface) to 3.1x (model top) of the clear-sky value and net surface shortwave 13.13 -> 8.49 W m-2 at the first differing call. | Legitimate physics, not a bug, but it decides what an EqWeather-only model can be tested against: the `chem170_scm` run's meteorology is aerosol-coupled, so a weather-only SCM cannot reproduce its trajectory. Stage-1 and stage-2 component references are unaffected (each dump is a self-contained kernel call). A stage-3 EqWeather comparison must either use the `chem_opt = 0`/`1` runs or mount this scattering term. See `data/eqwefic/notes/chem170_scm_reference_run.md`. | not reported |
| N87 (note) | wrf-model/WRF v4.8.0 | `chem/module_mosaic_driver.F` (the MOSAIC column state `rsub`, real32) around `aerchemistry`, `mosaic_newnuc_1clm` and `mosaic_coag_1clm` | **MOSAIC's per-stage increments read from the in-model dumps are cancellation-limited, coagulation severely.** Each stage updates `rsub` in place in real32, so a stage increment is the difference of two nearly equal single-precision numbers. Measured over 6 dumped steps as (|rsub_before| + |rsub_after|) * 2^-24 / |increment|: **coagulation median 0.04-0.09** (one to two significant digits left) with worst entries at 2 (the increment is below the representable resolution); gas-particle transfer median 5e-5 to 3e-4 with worst entries also at 2; nucleation median 6e-8 to 1e-4. | **Resolved 2026-09-22 by instrumenting the rates instead of the states** (WRF fork `a40edfe`): nucleation, coagulation and gas-particle transfer now write their INSTANTANEOUS rate where they form it, so a stage reference no longer requires differencing. The measurement above still describes the stage *dumps*, and is why the rates were added: at every dumped step 90-113 of the 472 coagulation number entries have a genuine rate but an increment at or below the real32 resolution. Where the increment IS resolved (>10x resolution) the rate reproduces it to 0.1 % median, 4.4 % at the 90th percentile, 16 % worst — the residual is the solver's own nonlinearity over 60 s, not noise. The earlier plan of a real64 replay is no longer on the critical path for these stages, and the unresolved real64 build of `kernels/mosaic_subproc_driver` no longer blocks anything (its real32 build still reproduces WRF bit for bit and remains a cross-check). Same shape as N84 for New Tiedtke. | not reported |
| N88 (note) | wrf-model/WRF v4.8.0 | `phys/module_sf_noahlsm.F:7` (`USE module_model_constants, only : CP, R_D, ...`), `:24` (`RD = 287.04`, `SIGMA = 5.67E-8`), `:377` (`PARAMETER (R = 287.04)`), `:2221` (PENMAN's `PARAMETER ... CP = 1004.6`) | **One Noah call evaluates three different values of two thermodynamic constants.** The module imports `CP` (= 7 R_d/2 = 1004.5) and `R_D` (= 287.0) from `module_model_constants`, then shadows the gas constant with its own `RD = 287.04` and `SIGMA = 5.67E-8` (against the dynamics' `STBOLT = 5.67051E-8`), and SUBROUTINE PENMAN shadows the specific heat again with a local `CP = 1004.6`. So `rch` in PENMAN uses 1004.6 while `rr` in CANRES and `SHEAT` at the end of SFLX use 1004.5, and the soil column sees a Stefan-Boltzmann constant 9e-5 smaller than the radiation scheme's. The differences are 1.4e-4 (rd), 9e-5 (sigma) and 1e-4 (cp) relative -- far below the scheme's own uncertainty, but they make it impossible to write the component against one constants library, so `components/land_surface/noah/noah_parameters.esm` carries Noah's values separately from `lib/wrf_constants.esm` and the distinction is load-bearing for the 1e-9 tests. Not a bug in the physics; a portability and reproducibility hazard for anyone refactoring the constants. | `noah_parameters.esm` carries Noah's own rd/sigma and PENMAN's cp separately from `lib/wrf_constants.esm`; the three physics components read whichever the routine they transcribe reads. Without this the 1e-9 tests fail at 1e-4. | no |
| N89 (note) | wrf-model/WRF v4.8.0 | `phys/module_sf_noahlsm.F:2239` (`RR = EMISSI*T24*6.48E-8/(SFCPRS*CH) + 1.0`) against `:1136` (CANRES: `RR = (4.*EMISSI*SIGMA*RD/CP)*(SFCTMP**4)/(SFCPRS*CH) + 1.0`) | **The same physical group is written once as a literal and once symbolically, and the two disagree.** PENMAN hard-codes `6.48E-8` for 4 sigma R_d/cp; with Noah's own `SIGMA = 5.67E-8`, `RD = 287.04` and PENMAN's `CP = 1004.6` the exact value is 6.48048e-8, so the literal is 7.4e-5 low, while CANRES forms the same group symbolically (and with cp = 1004.5) and gets 6.48113e-8. A component that reproduces WRF must keep both spellings; `potential_evaporation.esm` keeps the literal and `canopy_resistance.esm` the symbolic form. Harmless numerically, but the literal will not track any future change to the constants. | `potential_evaporation.esm` keeps the 6.48e-8 literal, `canopy_resistance.esm` the symbolic 4 emissi sigma rd/cp. Both are needed to reproduce WRF. | no |
| N90 (note) | wrf-model/WRF v4.8.0 | `phys/module_sf_noahlsm.F:1756-1766` and `:1836` (HRT: `CALL TDFCND(DF1N, SMC(K), ...)` then `RHSTS(K) = (DF1N*DTSDZ2 - DF1K*DTSDZ)/DENOM`, `DF1K = DF1N` at the end of the loop) | **The soil heat equation uses an UPWIND conductivity at each interface, not a face average.** TDFCND is evaluated at the layer centre, and that single value is then used for the conductive flux through the layer's LOWER interface, so the interface between layers k and k+1 carries the conductivity of layer k alone; the textbook choice for a conservative finite-volume scheme is the harmonic (or at least arithmetic) mean of the two adjacent layers. With Noah's default 10/30/60/100 cm layers and the strong moisture gradients that develop in the top layer, the two differ by several per cent in the surface flux. It is first-order accurate rather than second, and it is asymmetric: warming from above and warming from below do not see the same resistance. Reproduced deliberately in `components/land_surface/noah/soil_heat.esm` (K_soil[kn] = df_layer[kn-1]); a face average would silently disagree with WRF. | `soil_heat.esm` sets K_soil[kn] = df_layer[kn-1] (upwind), NOT a face average, and says so. The ESD rule varcoeff_face_flux_laplacian_lev takes the interface conductivity from the consumer, so no rule change was needed. | no |
| N91 (note) | wrf-model/WRF v4.8.0 | `phys/module_sf_noahlsm.F:1424-1444` (`SUBROUTINE FAC2MIT`: `IF ( SMCMAX == 0.395 )`, `.OR. ( SMCMAX == 0.476 )`, ...) | **A branch selected by exact floating-point equality against a decimal literal, which changes answer with the build's precision.** FAC2MIT picks the saturation threshold that decides whether SMFLX runs its predictor-corrector, by comparing the soil porosity against seven hard-coded decimals. In WRF's real32 build SMCMAX is the real32 value of the SOILPARM entry and the literal rounds to the same real32, so silt loam matches 0.476 and gets flimit = 0.74. In a real64 kernel build the SAME table value widens to 0.47600001096725464 while the literal becomes 0.47600000000000003, the test FAILS, and the routine silently returns its default flimit = 0.90. Every soil type in the table is affected. It is inert in the present reference set -- the wettest dumped column has fac2 = 0.66, below both thresholds, so the same branch is taken either way -- but a wetter soil would take a different path in the two builds, and the .esm components cannot reproduce both. Not transcribed: `soil_moisture.esm` writes the continuous right-hand side, which the two SMFLX branches share in the dt -> 0 limit. | `soil_moisture.esm` does not transcribe the predictor-corrector at all; both branches converge to the same instantaneous tendency, so the discrepancy cannot reach the component. Recorded because it will bite anyone comparing a real64 replay against WRF at high soil moisture. | no |
| N92 (note) | wrf-model/WRF v4.8.0 | `phys/module_sf_noahlsm.F:3774` (`VAL = (1. - EXP ( - KDT * DT1))`, `DT1 = DT/86400`) and `:3783` (`INFMAX = (PX * (DDT / (PX + DDT)))/ DT`) | **The infiltration limiter cannot be referenced at an arbitrarily small time step, so the usual 'shrink dt until the increment is the derivative' recipe has a floor here.** Both px and ddt are proportional to dt, so infmax has a clean dt -> 0 limit, pcpdrp r/(pcpdrp + r) with r = dd kdt/86400 -- but the Fortran reaches it through 1 - exp(-x) with x = kdt dt/86400, which loses log10(x) digits to cancellation as dt falls. Measured on the step-601 column of the chem_opt = 170 reference run, the real64 replay differs from the exact limit by 5.8e-7 at dt = 1e-6 s, 5.5e-8 at 1e-5, 1.1e-8 at 1e-4, 2.2e-8 at 1e-3, 2.2e-7 at 1e-2 and 2.2e-5 at 1 s: a V with its minimum near 1e-4 s, cancellation on the left branch and the linearisation error of val on the right. It does not affect WRF itself, where dt = 60 s puts x at 3e-3 and the cancellation is negligible; it affects anything that tries to extract a derivative by differencing. | `soil_moisture.esm` writes the limit form directly. Its runoff1 assertions are referenced at ESM_DT = 1e-4 s and bounded at rel 5e-8, which is the REFERENCE's floor and not the component's accuracy (measured residual 1.3e-8). | no |
| N93 (note) | wrf-model/WRF v4.8.0 | `dyn_em/module_initialize_scm_xy.F:246` (`grid%snow(i,j) = 0.`); `phys/module_physics_init.F:318` (`LOGICAL, PARAMETER :: FNDSNOWH=.true.`); `phys/module_sf_noahdrv.F:2140-2148`; `phys/module_sf_noahlsm.F:509` | **Noah's snow and snow-covered frozen-soil paths are unreachable from any idealized column.** `em_scm_xy` hard-wires the initial snow water equivalent to zero with no namelist override, so `SNOPAC`, `SNOWPACK`, `SNFRAC`'s saturation branch and `ALCALC` never execute. Setting SNOW alone is not enough either: `FNDSNOWH` is a compile-time `.true.`, so LSMINIT's documented "SNOW HEIGHT NOT FOUND - VALUE DEFINED IN LSMINIT" fallback (`SNOWH = SNOW*0.005`) is **dead code**, and `SFLX` then divides by `SNOWH = 0` at `:509` and aborts in `'Physical snow depth is less than snow water equiv.'`. | Blocks stage-1 reference data for the entire snow pack. Worked around on fork branch `earthsciml-instrumented-noahcold` with an `&scm` namelist variable `scm_snow` (default 0.0, so an unset namelist reproduces stock WRF) that sets both `grid%snow` and `grid%snowh`. See `data/eqwefic/notes/noahcold_scm_reference_run.md`. | not yet |
| N94 (note) | wrf-model/WRF v4.8.0 build system | `share/module_llxy.F` -> `module_llxy.f90` (any Registry-generated source) | **A parallel `./compile -j 8` can leave a generated `.f90` zero bytes**, and the failure surfaces far away: the module compiles "successfully" to an object with no `.mod`, and the link then fails with `Cannot open module file 'module_llxy.mod'` in unrelated files. Re-running `./compile` does NOT fix it, because the stale `.o` is newer than the `.F` so make never regenerates the empty `.f90`. | Cost a build cycle on the cold-season run. Fix: delete every `*.f90` of size 0 **and** the `.o` of any module whose `.mod` is missing, then `./compile` serially. | not yet |
| B20 | wrf-model/WRF v4.8.0 | `chem/module_mosaic_newnuc.F:1007` (`qh2so4_avail = (qh2so4_cur - qh2so4_crit)*dtnuc`), with consequences at `:1011` (the guard) and `:1027-1044` (the `dum_n1`/`dum_n2`/`dum_n3` composition partition) | **`wexler_nuc_mosaic_1box` multiplies an excess mixing ratio by the timestep and then uses the product as a mixing ratio.** The subroutine's own declarations and comments say what was intended: `qh2so4_avail` is declared `! amount of h2so4 available for new particles (ppm)` (`:984`), `dtnuc` is `! nucleation time step (s)` (`:907`), the guard's comment reads "if 'available' h2so4 vapor < 4.0e-18 mole/mole-air ~= 1.0e2 molecules/cm3" (a concentration), and the partition's comment says the ratio is `min(2, (nh3/h2so4 **gas molar ratio**))` (a ratio of two gas mixing ratios). None of those readings survives the `*dtnuc`. **The intended line is `qh2so4_avail = qh2so4_cur - qh2so4_crit`** -- Wexler's assumption that nucleation is fast on the step, so the vapour relaxes to the critical concentration and the excess forms new particles. Three consequences at `dtnuc = 60 s`: (1) `qh2so4_del = min(0.9999*qh2so4_cur, qh2so4_avail)` clamps to the first argument unless `qh2so4_cur` is within ~1.7 % of `qh2so4_crit`, so the scheme strips ~all H2SO4 vapour every step instead of relaxing it to critical; (2) the inflated denominator makes NH3 look scarce, so the new particles come out nearly pure sulfuric acid where they should be substantially neutralised; (3) the guard is 60x easier to pass, so nucleation also fires more often than intended. Measured at the dumped steps against the dt -> 0 limit of the same expressions (WRF fork `a40edfe`): sulfate increment/dt is 1x-56x from the rate, number 1.4x-79x, **ammonium 363x-6.4e4x**. The 1x-56x range is the clamp's own signature, `60*(1 - qh2so4_crit/qh2so4_cur)`, measured before the cause was found. | **Fixed in the fork and the reference data recaptured (2026-09-22, user decision; WRF fork `earthsciml-instrumented-chem170`).** The clamp analysis was verified against the stock dumps before anything was changed: at step 1 all 46 firing levels have `|qh2so4_del| = 0.9999*qh2so4_cur` exactly and the measured rate/increment ratio matches `60*(1 - qh2so4_crit/qh2so4_cur)` to 1e-4 over the range 5.1x-59.6x; at the three later single-level events the vapour sits within 0.5-0.9 % of critical, inside the ~1.7 % band where the clamp does not bind, and there the increment is `(qcur - qcrit)*dtnuc` as predicted. Further evidence found while fixing: **WRF's own newer copy of this routine, `chem/module_mosaic_newnucb.F:1668`, already has the line without the `*dtnuc`**, and the ternary route at `:612-626` is correct (`ratenuclt*dtnuc*mass_part` is a genuine rate x dt, and its composition partition divides by `qh2so4_cur`, a mixing ratio). **Measured on identical input columns** (the same column through both schemes, `kernels/mosaic_subproc_driver`): vapour left after the call rises from 1.0e-4 of what entered to 0.063 at step 1 (the scheme now relaxes to critical instead of stripping the vapour), and from 0.44-0.68 to 0.991-0.995 at the near-critical events; the new particles' (mole NH4)/(mole SO4) rises from 0.0009-0.0055 to 0.054-0.33, i.e. 30x-60x more neutralised; particle number falls by 4.5 % at step 1 and by 20x-60x at the near-critical events. The guard fires at the SAME levels on identical inputs (46/46, 1/1, 0/0), so consequence (3) above is real in principle but changes nothing in this case. **The corrected scheme is exactly dt-independent**: replayed at dtnuc = 60, 6 and 0.6 s it returns bit-identical increments, confirming it is an instantaneous projection. In the full model the fix moves the meteorology, as B20 sits upstream of aerosol mass which feeds Dudhia's scattering (N86): identical at step 1, 0.0013 K by step 60, up to 1.9 K and 24 W/m2 in `hfx` by step 601. The component transcribes the CORRECTED scheme; the stock dumps are kept at `data/eqwefic/dumps/chem170_wrf_stockB20` as a cross-check. Even corrected the scheme is a projection, not a rate, so it joins an ODE document under the operator-split convention (as `pigen`, N3), and the instrumentation records the projection increment rather than inventing a rate for it. | not reported (user decision 2026-09-22: hold) |
| N95 (note) | wrf-model/WRF v4.8.0 | `phys/module_sf_noahlsm.F:1447-1585` (`SUBROUTINE FRH2O`, the `1001`/`1002` Newton loop, `ERROR = 0.005`, `KCOUNT`) | **FRH2O is not a root-find; it is a ONE- OR TWO-STEP Newton relaxation seeded from the previous time step, so the unfrozen water it returns is not a function of (T, smc) alone.** The loop exits as soon as a single increment falls below `ERROR = 0.005`, starting from `SWL = SMC - SH2O` with SH2O the PREVIOUS step's value. Measured over the 332 layer-calls of the cold-season reference set: it exits after **1** iteration in 257 calls and **2** in 72, never more. Evaluating the scheme's own freezing-point relation at the answer it returns leaves a residual of up to **2.0e-3** in log space over the 218 calls whose solution is interior (the other 111 sit on the `SWL = 0` clamp, where the constrained solution need not satisfy it at all). Dividing by the Newton denominator, a CONVERGED solver would differ from WRF by roughly 5e-5 m^3/m^3 of liquid water, about 3e-4 relative -- three orders above the replay floor. **Consequence for stage 2:** a `frozen_soil.esm` cannot write the free-water content as a closed algebraic relation, and must not simply iterate to convergence either. It has to reproduce the early exit AND carry the previous sh2o as its initial guess, which makes the free water a lagged state rather than a diagnostic. esm-spec 4.3.1.1 (causal self-reference) is the construct for the iteration; the seed has to come in as a consumer-supplied field. | Not yet written. The soil-moisture component's tendency assertions are therefore restricted to unfrozen columns, and its cold-season tests assert only the quantities SMFLX computes BEFORE the phase-change sink runs. | no |
| N96 (note) | wrf-model/WRF v4.8.0 | `phys/module_sf_noahlsm.F:441-484` (REDPRM's greenness selection of `ALB`, `EMBRD`, `XLAI`, `Z0BRD`) against the `ALBBRD`/`EMBRD` the driver passes in at `:330` | **The background albedo and emissivity a caller hands to SFLX are overwritten by REDPRM before anything uses them, so a dump of the ARGUMENTS records values the scheme never applies.** `module_sf_noahdrv.F` passes the Registry's `ALBBCK`/`EMBCK` as `ALB`/`EMBRD`; REDPRM then replaces both from the vegetation type's `ALBEDOMIN`/`ALBEDOMAX` and `EMISSMIN`/`EMISSMAX`, selected by the greenness fraction. In the cold-season reference run the incoming `embrd` is 0.920 while the value ALCALC actually uses is 0.985, so referencing the snow albedo and emissivity against the dumped argument puts a 7 % error into the emissivity and silently shifts the albedo. Any component transcribing ALCALC, SNFRAC or CANRES must take the REDPRM-selected values (which `soil_veg_parameters.esm` computes) and not the dumped arguments of the same name. | `snow_cover.esm`'s tests supply `tbl_albedomintbl[vegtyp-1]` and `tbl_emissmaxtbl[vegtyp-1]`, the greenness branch these columns take, and say so. Found by a 7 % emissivity failure. | no |
| N97 (note, RESOLVED 2026-09-23) | wrf-model/WRF v4.8.0 | `phys/module_sf_noahlsm.F:2941-2957` (SNKSRC: `XH2O = SH2O + QTOT*DT/(DH2O*HLICE*DZ)`, the two interval clamps, `TSNSR = -DH2O*HLICE*DZ*(XH2O-SH2O)/DT`) | **The frozen-soil latent sink has a clean dt -> 0 limit but, as WRF stores it, no reference step at which to MEASURE it -- the window is squeezed from both ends.** Unclamped, `tsnsr = -qtot` exactly and at any dt: the phase change absorbs the whole energy imbalance and the layer's temperature does not move. But WRF returns it through the difference `xh2o - sh2o`, of size `qtot dt/(rho_w hlice dz)`. Below about 1e-3 s that difference is ~1e-13 against an sh2o of order 0.1 and cancellation destroys it -- at the 1e-6 s replay NOT ONE of the 332 layer-calls returned `tsnsr` bitwise equal to `-qtot`, and the worst was off by 5.9e-3 relative. Above about 1e-2 s the increment instead exceeds the distance to the equilibrium `free`, the interval clamp binds, and the answer becomes a finite-dt artefact (80 of 332 at 0.01 s). **RESOLVED by instrumentation, 2026-09-23** (fork `earthsciml-instrumented-noahcold`): four new grep-guarded splices publish the RAW energy-driven `xh2o` before any clamp, the `sh2o` and `dz` it is formed from, WHICH clamp fired (none / held at sh2o / stopped at free / floored at 0 / capped at smc), and -- for the same reason one level up -- HRT's own per-layer `sice` and `sh2o` at the point it decides whether to evaluate the phase change at all. Over the reference set the decision is: no clamp at 193 layer-calls, held at sh2o at 139, and the other three clamps never fire. The same pass also publishes SNOPAC's melt rate BEFORE its pack-exhaustion limiter, which is dt-dependent in the same way. | `frozen_soil.esm`'s sink is now PINNED: `phase_active` is asserted against the published clamp decision at ZERO tolerance and matches at all 332 layer-calls, and `tsnsr` and `dsh2o_dt_phase` are asserted against exact expressions in the published `qtot` and `dz` instead of the differenced value. Every assertion in the component is now exact at zero tolerance (it was 4.2e-12 on `free` before). | no |
| N98 (note) | wrf-model/WRF v4.8.0 | `phys/module_sf_noahlsm.F:3383-3390` (SNOPAC: `IF (ESD- SNOMLT >= ESDMIN) THEN ... ELSE EX = ESD / DT ; FLX3 = EX*1000.0*LSUBF ; SNOMLT = ESD`) | **SNOPAC manufactures a RATE from an AMOUNT when a step's melt would overshoot the pack, and it rewrites the melt ENERGY to match.** The melt rate is properly `ex = flx3/(rho_w lsubf)`, a rate with no dt in it. But if `esd - ex dt` would fall below `esdmin`, WRF replaces it with `ex = esd/dt` and then back-computes `flx3 = ex rho_w lsubf` -- so the reported melt energy is no longer the energy balance's residual but whatever is needed to consume the remaining pack in one step. A second, earlier test does the same for sublimation: `esd - esnow1 dt <= esdmin` zeroes the whole melt block. Both are PROJECTIONS of the state after a step, both are dt-dependent by construction, and neither belongs in a tendency -- the same category as SSTEP's supersaturation redistribution and the slab scheme's snow cap. Differencing the pack to recover the melt would therefore measure the projection rather than the rate, on top of the cancellation-versus-clamping bind N97 describes. | `snow_energy.esm` writes the rate and reports the two limiters as the indicators `pack_exhausted` and `melt_limited`. Its `ex` is referenced against the fork's `sno_ex_raw` publisher -- the rate BEFORE the limiter -- and reproduces it to 2.4e-11 relative, against the 5.0e-4 replay floor the cold-season notes give for the differenced `snomlt`: seven orders better, and the whole reason the publisher was added. The limiter branches themselves are UNTESTED, not merely unused: the reference set never reaches either at the replay step size. | no |
