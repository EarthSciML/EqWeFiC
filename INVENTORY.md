# Component inventory

Status values: `not started` · `instrumented` · `stub` (tests written, on this repo) · `physics` (tests pass locally) · `PR` · `merged`.
`Existing` names an EarthSciModels file that already implements (part of) the component.

## EqWeather — reference suite (WRF v4.8.0)

| Component | Sub-components (planned) | Fortran source | Existing in EarthSciModels | Status |
|---|---|---|---|---|
| `lib/wrf_constants.esm` | — | `share/module_model_constants.F` | — | PR EarthSciModels #15; physics (tests pass, 2026-09-04) |
| `lib/wrf_thermo.esm` | T↔θ, θv, θli, moist and dry density, Exner function, Bolton e_s and Bolton qsat (YSU form), fpvs/qsat over water and ice, L(T), cpm; `lib/wrf_air_properties.esm`: ν, Dv, ka | `module_model_constants.F`, per-scheme | `atmospheric_dynamics/sp_ch1/*` | PR EarthSciModels #15; physics (shared by ysu, wsm6, slab, sfclayrev, dudhia_sw, rrtm_lw; duplicates consolidated 2026-09-05) |
| Column grid + rules | `column_nonuniform_1d` grid, face-flux divergence, interface-K diffusion with prescribed fluxes, 5 `integral` forms (downward sedimentation flux deferred) | EarthSciDiscretizations PR #34 | `grids/column_nonuniform_1d/` | physics PR (julia/python/rust gate green, 2026-09-05) |
| 2-D fire grid rules | `cartesian_uniform_2d`: hybrid WENO5/ENO1 level-set ∣∇ψ∣ and per-axis derivative, Godunov-upwind per-axis derivative, centred D2, all under a guarded linear-extrapolation halo (`bc:extrapolate_linear`) | `phys/fire_behavior/physics/level_set_mod.F90` | `grids/cartesian_uniform_2d/` | EarthSciDiscretizations PR #37 (2026-09-07): 7 rules + 9 stencils + 2 MMS problems + 11 conformance cases; lint 0 findings; CI green on all five bindings — julia (reference), python, go, typescript and the cross-binding `compare` gate at 2131/1/426, the single failure being the pre-existing `simulation/duo_swe_dp [rust]` `[reserved_index_symbol]` load error (upstream ESS drift, unpinned checkout) that also reddens `rust`; the `build` docs job is red on `main` since 2026-08-29. All 11 new cases pass under every binding; convergence errors agree across julia/rust/python to ~1e-13. Observed orders 1.00 (Godunov, L2 and Linf) and 1.47 (hybrid L2, expected 1.5). **PR #40** (2026-09-21) adds the reinitialisation and cosine-blend variants (3 + 4 files); **PR #41** (2026-09-21, stacked on #40) adds the hybrid WENO3/ENO1 family of `fire_upwinding = 8` -- `weno3_js_face_recon`, `weno3_js_upwind_D1`, `weno3_eno1_D1x/D1y_interior` and three rules -- the first WENO3 on this grid, classical Jiang-Shu and deliberately NOT the 1-D WENO-Z `weno3_recon`. Lint on #41's branch: 592 findings, all the repo-wide L008 version skew, no golden changed |
| Surface layer (sfclayrev) | φm/φh, bulk Richardson regimes, z0 over water, u*, exchange coefficients, 2 m/10 m diagnostics | `phys/physics_mmm/sf_sfclayrev.F90` | `local_scale/surface_layer_profile.esm` (not reused: different functions) | PR EarthSciModels #16; physics (Phase 1, 2026-09-05): `components/atmospheric_dynamics/sfclayrev/` (`SfclayRev` + parameters, `similarity_functions`, `bulk_richardson_zol`, `surface_roughness`, `sfclayrev_thermo`), 610/610 `./esm test` in 18 regimes (6 SCM land + 12 Fortran-generated water/option regimes); zolri secant transcribed as a recurrence; KIM tofd not transcribed (B3) |
| PBL (YSU) | PBL height (bulk Ri), K-profile, countergradient term, entrainment flux, top-down mixing, diffusion PDE | `phys/physics_mmm/bl_ysu.F90` | `holtslag_boville/*` (related) | PR EarthSciModels #18; physics (Spike A + cloud-top branch, 2026-09-05): `components/atmospheric_dynamics/ysu/ysu.esm`, 157/157 `./esm test` (5 regimes incl. a synthetic cloud-topped boundary layer); `ysu_topdown_pblmix` cloud-top radiative entrainment transcribed (`radsum` as a column integral); EarthSciModels gate blocked until PR #2 merges; geometry coupling-target parameters added 2026-09-15 (standalone counts unchanged); EarthSciModels PR refresh needed. 2026-09-20: gained the STATE coupling-target parameters the EqWeather-SCM state document needs (theta_in, u_in, v_in, qv_in, qc_in, qi_in), lowered by `couplings/tests/scmstate_couple_ysu_inputs.esm`. The six `ic(x) = input_x` / `D(x) = dxdt` pairs became plain algebraic reads `x = input_x`: the scheme is a tendency diagnostic (dthdt, dudt, dvdt, dqvdt, dqcdt, dqidt are its outputs) and the consumer owns the state, so a component-owned copy would have drifted under the PBL tendency alone once the dynamics also acted on it. Every assertion is at t = 0 on an observed, so 157/157 is unchanged |
| Land surface (slab) | 5-layer soil heat diffusion, surface energy balance | `phys/module_sf_slab.F` | — | PR EarthSciModels #17; physics (Phase 1, 2026-09-05): `components/land_surface/slab/slab.esm` (+ `slab_parameters`, `slab_templates`), 84/84 `./esm test`; surface energy balance + soil heat PDE on `column_nonuniform_1d` (ESD PR #35 rule); 4 SCM regimes (day, night, morning flux reversal, isothermal); snow cap and water points as indicators only (no SCM regime); one-layer force-restore form not transcribed. 2026-09-20: gained the state coupling-target parameter `T_s_in`, lowered by `couplings/tests/scmstate_couple_slab_inputs.esm`, so the state document can integrate D(T_s) = dTs_dt and read the soil column back in. The soil geometry `ze` is deliberately NOT one: the layer depths come from SOILPARM and are time-invariant. 84/84 unchanged |
| Land surface (Noah) | soil heat/moisture, canopy resistance, Penman, snow | `phys/module_sf_noahlsm.F` | `urban_canopy/hydro/*` (related) | **physics, PART (2026-09-22)**: `components/land_surface/noah/` -- `noah_parameters.esm` (Noah's own constants, deliberately NOT lib/wrf_constants: FORTRAN_BUGS N88), `canopy_resistance.esm` (CANRES, rcs/rct/rcq/rcsoil/rc/pc, the root-zone stress written as a thickness-weighted integral), `potential_evaporation.esm` (PENMAN, rch/rr/t24/epsca/etp), `evapotranspiration.esm` (EVAPO/DEVAP/TRANSP, edir/ec/et/ett/eta/beta including the dew branch) and `soil_heat.esm` (TDFCND + HRT as the flux-form PDE D(K D(T) - F) lowered by the ESD rule varcoeff_face_flux_laplacian_lev, plus the yy/zz1 surface closure, the diagnostic skin temperature, ssoil and sheat). **448/448** over 16 idealized-column regimes at rel 1e-9 against the real64 replay; worst residuals 5e-15 relative for the algebraic parts and 1.1e-10 K/s for the soil-temperature tendency. Second tranche (2026-09-22): `soil_veg_parameters.esm` (REDPRM's SOILPARM/VEGPARM/GENPARM lookups and the kdt/frzx/greenness derivations, 48/48 BIT-EXACT including seven soil types checked against the independent real-terrain run) and `soil_moisture.esm` (SMFLX/SRT/WDFCND: Richards' equation as D(K_w D(sh2o) - F_w) lowered by the SAME ESD rule as the heat equation -- no new ESD rule was needed, because WRF upwinds the gravity drainage so it drops into the rule's prescribed-flux slot -- plus the canopy reservoir, dew routing, the infiltration limiter in closed dt -> 0 form, and surface and subsurface runoff; 176/176). **672/672 over the five physics components.** Third tranche (2026-09-22, cold-season data): `snow_cover.esm` (SNFRAC + the 0.98 cap, ALCALC with the snow-age albedo decay, CSNOW; 268/268 over 67 pack steps of all four cold columns, both the partial-cover and the SNFRAC-saturation branch) and 17 cold NOPAC tests added to `soil_moisture.esm` (227/227), which pin for the first time every path that reads soil ice -- sicemax, the vkwgt ice weighting of the diffusivity, the sice term in the storage deficit -- plus BOTH branches of the frozen-ground infiltration reduction fcr, whose active branch is reached at exactly one step (case A step 1, dice = 0.19 m). The water supply on those steps is DEW, not precipitation, so the dew routing is pinned on a second independent set of columns. **1087/1087 over the whole land_surface tree plus lib.** Fourth tranche (2026-09-23): `frozen_soil.esm` (TBND, TMPAVG's freezing-clipped layer average, FRH2O and SNKSRC), **1084/1084**. N95 is resolved rather than worked around: FRH2O's seed is the CURRENT sh2o, and the loop never runs past two iterations, so the two Newton steps are written out in closed form with WRF's own exit test between them -- exact to 4e-12 over all 332 layer-calls, with no recurrence and no convergence iteration. HRT's evaluation guard is reproduced and itself asserted against the replay. The SINK is now PINNED too (2026-09-23): four new grep-guarded splices publish the raw pre-clamp xh2o, the sh2o and dz behind it, WHICH clamp fired, and HRT's own per-layer ice, so tsnsr no longer has to be recovered from a cancelling difference (FORTRAN_BUGS N97, resolved). **1529/1529, every assertion EXACT at zero tolerance.** The activity switch matches the published clamp decision at all 332 layer-calls. STILL TO DO: SNOPAC's energy balance (T12 and the melt block), the soil-moisture tendency on frozen columns (which needs the sink pinned first), the snow pack (SNOPAC/SNOWPACK/CSNOW/ALCALC/SNOWZ0), SSTEP's supersaturation redistribution (a post-step projection, reported as an indicator), the SMFLX predictor-corrector (a finite-dt device; both branches share the instantaneous right-hand side), and the urban and opt_thcnd = 2 branches; the reference set is above freezing and snow-free, so those branches are untested rather than merely unused. Stage-1 data: real-terrain `SFLX` in/out dumps at 210 points (7 steps × 5 rows × 6 points, mountains and plains) in `gwdo_real/dumps`, see `data/eqwefic/notes/gwdo_real_reference_run.md`; plus 16 idealized-column `SFLX` dumps (day/night, dry and precipitating) in `dumps/chem170_wrf` that also carry the VEGPARM/SOILPARM/GENPARM tables `REDPRM` reads, and a real64 kernel driver `wrf-chem170/kernels/noah_driver` that replays them without a table file (agrees with WRF to real32 round-off: `t1` 3.3e-5 K, sensible heat 4.1e-4 W m⁻² of 130). See `data/eqwefic/notes/chem170_scm_reference_run.md` **Stage-1 reference data for the snow pack and the frozen-soil sink now EXISTS (2026-09-22): `data/eqwefic/dumps/noahcold_wrf/case{A,B,C,D}/`, 84 dumps from four idealized cold-season columns; SNOPAC sub-freezing 38 steps, SNOPAC melt 29, NOPAC 17, FRH2O paths 1 and 2 covered (3 and 329 layer-calls). The real32 replay reproduces the in-model dumps EXACTLY and precision flips no branch at any of the 84 steps. FRH2O's Flerchinger fallback (path 3) is still uncovered -- Newton converged in <= 2 of its 10 iterations everywhere. Needs WRF fork branch `earthsciml-instrumented-noahcold` (`scm_snow`, FORTRAN_BUGS N93). See `data/eqwefic/notes/noahcold_scm_reference_run.md`.** Replay agreement per quantity is tabulated in the notes: the real32 driver is IDENTICALLY ZERO on all 19 snow and soil outputs across all 84 dumps, and the real64 precision floor is 5e-4 relative for `snomlt`/`flx3` (both differences across the melting threshold), ~4e-4 `sheat`, ~1e-5 for the snow state itself, and 1e-6 or better for `albedo_out`/`flx1`/`flx2`/`beta` -- a component cannot honestly be pinned tighter than that. |
| Microphysics (WSM6) | saturation adjustment; warm rain (praut, pracw, prevp); ice processes; melting/freezing; sedimentation | `phys/physics_mmm/mp_wsm6.F90` | — | PR EarthSciModels #19 (refreshed 2026-09-07 to 790 assertions, head 7faecd0); physics (Spike C + ice phase, 2026-09-05): `components/atmospheric_dynamics/wsm6/{saturation,warm_rain,hydrometeor_slopes,cold_accretion,ice_deposition,melting_freezing,saturation_adjustment,sedimentation}.esm` (+ `wsm6_parameters`, `microphysics_templates`), 621/621 `./esm test`; all 26 process rates incl. melting/freezing as an in-place increment block with closed budgets; saturation adjustment done (`saturation_adjustment.esm`, 52 assertions, target state + increment + τ-relaxation form); sedimentation of rain, snow+graupel, ice as −(1/ρ) D(ρ v_t q, lev) via ESD `sedimentation_jh2010_flux_D_lev` (**PR #42**, mounted 2026-09-22 in place of the donor-cell `sedimentation_upwind1_flux_D_lev` of PR #36; needs `ESD_ROOT=…/EarthSciDiscretizations-integ`), 65/65 (4 regimes; fluxes vs dt→0-extrapolated real64 replays; the 13 non-trivial tendency columns now vs WRF's verbatim `nislfv_rain_plm` extrapolated to dt→0 on each test column, ≤ 1.14e-7 of the column max for rain/snow/graupel and ≤ 2.7e-7 for ice since #42 gained the negative-interface-speed branch, FORTRAN_BUGS N77(3), on 2026-09-22 (ESD `14b5db5`; ice was ≤ 6.4e-6 without it), all four at 1e-6 of the column max); mass-conservation rescaling done 2026-09-07 (`mass_conservation.esm`, 169 assertions in 4 regimes): the five/four sequential species-budget checks written as one cumulative factor per budget (`mass_conservation_factor` = v/max(S,v)) times the raw rate, plus the state update they feed; reproduces the real64 replay EXACTLY (0.0) in all 22 `<rate>_final` columns and the `*_upd` state, residual only 2e-19 kg/kg/s in dqv_dt and 2e-15 K/s in dT_dt; 790/790 `./esm test` for the scheme **Rate form added 2026-09-20 so the scheme can join an ODE document** (`couplings/eqweather_scm.esm`): WSM6 is an operator-split adjustment, not a rate, so each stage that applies an in-place increment now also exposes that increment divided by the sub-step it is applied over — `melting_freezing.esm` dq{c,i,r,s,g}_dt and dT_dt, `mass_conservation.esm` dq{c,i,r,s,g}_dt beside its existing dqv_dt/dT_dt, `saturation_adjustment.esm` dqv_dt/dqc_dt/dT_dt. All pure additions; 790/790 unchanged. Also: the five stages with a nested `sd`/`sat` subsystem (sedimentation, melting_freezing, cold_accretion, ice_deposition, saturation_adjustment) declared no metaparameter of their own and so could only be mounted at the leaf's default 40 layers — each now has a bindable `NLEV` passed down to the subsystem, and `sedimentation.esm`'s two `${ESD_ROOT}` grid imports, which were hard-bound to `NLEV: 40`, follow it. That is what lets the scheme sit in the 59-layer SCM column. The sub-step is a parameter of the tendency, not the integrator's step: ndt = max(nint(dt/dtcldcr),1) with dtcldcr = 120 s, so the em_scm_xy run at dt = 60 s has ndt = 1 and dtcld = 60 s. **Known gap surfaced by the coupled cloudy test, RE-MEASURED 2026-09-21**: the donor-cell sedimentation flux divergence is **1.46x** WRF's PLM ice fallout at the reference cloudy state (5.304e-8 against WRF's own stage increment 3.637e-8 kg/kg/s at dtcld = 60 s, 1.52x against its dtcld→0 limit 3.4935e-8). The earlier "2.4x" compared this ONE process against the sum of ALL microphysical processes (2.17e-8), which partly cancel, and is withdrawn. Note which stages converge: WRF's ice fallout is dt-dependent by only 4 % between dtcld = 0.04 and 60 s, while the process-rate block does NOT converge, because `pigen = max(0, (roqi0/den − q_i)/dtcld)` (`mp_wsm6.F90:1200`) projects q_i onto the diagnosed equilibrium ice content in ONE sub-step — its increment is 7.78e-8 kg/kg at dtcld = 0.01, 0.02 and 0.04 s alike (N3). The saturation adjustment is identically zero in this column, so `pigen`, not `pcond`, is what forbids a dt→0 extrapolation of the whole scheme. **LIKE-FOR-LIKE, 2026-09-22:** the coupled document's own `mp_sed_dqi_dt` against WRF's dtcld→0 ice fallout on the same column, levels and quantity differs by **2.04e-8 Linf against a 3.49e-8 peak (58 %)**, per-level ratios 0.26–2.07. The component's 65/65 does not contradict this: its ice assertions are, by its own description, "a wiring check, not an independent reference" (WRF dumps no ice flux above k = 1). Transcribing WRF's own operator — a third-order interface fall speed plus a monotone average-slope piecewise-linear face value (FORTRAN_BUGS N77) — reproduces WRF at every level 50–57 to five significant figures, so a PLM flux rule in ESD IS warranted; AUTHORED as **EarthSciDiscretizations PR #42** (`sedimentation_jh2010_flux_D_lev`, stacked on #36): against WRF's converged ice fallout on this column it leaves 2.07e-13 (5.9e-6 of the peak) where donor-cell leaves 58.6 %. Mounted 2026-09-22 (`-integ` worktree). See the EqWeather-SCM STATE document row and TENDENCY_ERROR_BUDGET.md. |
| SW radiation (Dudhia) | clear-sky absorption/scattering, cloud albedo/absorption, column integrals | `phys/module_ra_sw.F` | — | PR EarthSciModels #20; physics (Spike B + guards, 2026-09-05): `components/atmospheric_radiation/dudhia_sw/`, 222/222 `./esm test` (7 regimes incl. synthetic stratus and a renormalised surface fog); transmissivity renormalisation and beam floor transcribed; exact only when no depleting layer lies below a renormalised one (coupled-sweep format gap); geometry coupling-target parameters added 2026-09-15 (standalone counts unchanged); EarthSciModels PR refresh needed. 2026-09-20: gained the state coupling-target parameters T_in, qv_in, qc_in, qr_in, qi_in, qs_in and qg_in, lowered by `couplings/tests/scmstate_couple_sw_inputs.esm`; the `ic(T)` / `D(T) = dTdt` pair became the algebraic read `T = input_T`, for the same reason as YSU — the scheme reports a heating rate and the consumer owns the temperature. 222/222 unchanged |
| Solar geometry (radiation driver) | declination, eccentricity-corrected solar constant, equation of time, hour angle, coszen | `phys/module_radiation_driver.F:3469-3541` (radconst, calc_coszen) | `lib/solar.esm` (not reused: Spencer/NOAA formulation, declination differs by 0.022 rad) | physics (2026-09-15): `components/atmospheric_radiation/wrf_solar/wrf_solar_geometry.esm` (+ `wrf_solar_parameters.esm`), 63/63 `./esm test`: 13 em_scm_xy regimes from the `sw` hook (sunrise/sunset crossings, night, post-1440-min wrap) + 6 synthetic regimes from `kernels/solar_driver.F90` (julian < 80 branch, radt > 0, southern/eastern hemisphere, polar day; fork 1b4b556); real32, rel 1e-5 (coszen abs 2e-6 at crossings, N69; equinox-edge declin abs 5e-7, N68); supplies DudhiaSW csza/solcon (RRTM LW takes no solar input), not yet wired. 2026-09-19: EarthSciAST #413 made the transcendentals apply their argument's unit SCALE, which exposed `degrad` being declared `units: "1"` -- `sin(sun_longitude*degrad)` typed as deg and double-converted, collapsing declin to ~2e-4 rad and reddening 17 assertions here and downstream. Fixed by declaring `degrad` as `rad/deg` in `lib/wrf_constants.esm`; back to 63/63 and Eq 2 now type-checks `consistent [angle]` |
| Grid-scale cloud fraction (radiation driver) | condensate threshold, ice-weighted saturation (Murray over water and over ice), Randall (1994) fractional fit with its exponential floor and 0.01 snap | `phys/module_radiation_driver.F:3761-3986` (cal_cldfra1, icloud = 1) | — | physics (2026-09-20): `components/atmospheric_radiation/cloud_fraction/{cal_cldfra,cal_cldfra_parameters}.esm`, **63/63** `./esm test`. Pointwise in `lev`; observes `cldfra` and `branch` (the Fortran's cldfra1_flag: 1 = no condensate, 2 = saturated, 3 = Randall fit). Reference is the real64 `kernels/cldfra_driver` of the WRF fork, which compiles cal_cldfra1 EXTRACTED VERBATIM from the radiation driver at build time; the in-model real32 CLDFRA is NOT usable as a reference because the Randall denominator cancels (up to 5.1e-5 absolute, 1.2e-4 relative, measured) — evaluating this component in binary32 reproduces it to 6.0e-8, so the gap is WRF's arithmetic, not the transcription. Residual against the real64 kernel <= 4.3e-16; tolerances abs 1e-12 (columns) and rel 1e-9 (spot checks). Six regimes from two reference runs: em_scm_xy calls 1 (fully clear, with RHUM up to 27 in the stratosphere — the condensate guard is the only thing keeping those layers at 0), 120 and 360 (pure-ice high cloud); em_quarter_ss columns 20/40 at calls 61 (pure liquid), 268 (the full ice-weight sweep 0 -> 1, plus the -6.9 exponential floor in 8 layers and the 0.01 snap in 10) and 396 (deep ice anvil, 30 fractional layers). Mutation-checked: replacing the ice fit with the water fit fails 44/63, dropping the phase blend 44/63, dropping the -6.9 floor 4, the 0.01 snap 2, the condensate guard 1. Only the `F_QC .and. F_QI .and. F_QS` branch is transcribed (the reference runs' WSM6); the dead ELSE arm is FORTRAN_BUGS.md B13. Supplies `cldfra_in` to the RRTM column stage, closing the last prescribed input of the EqWeather-SCM state document. Not yet an EarthSciModels PR |
| LW radiation (RRTM) | per-band optical depth, Planck, up/down sweeps, cloud optics and overlap, heating rate | `phys/module_ra_rrtm.F` | — | PR EarthSciModels #21 (refreshed 2026-09-07 to the current 1451 assertions); physics (Phase 1 stages 1-3, 2026-09-06; cloud-overlap regimes and warm/mixed-phase cloud optics 2026-09-07): `components/atmospheric_radiation/rrtm_lw/{rrtm_lw_heating,rrtm_column,rrtm_setcoef,rrtm_gas_optics,rtrn_sweep}.esm` (+ `rrtm_parameters`, `rrtm_spectral_tables`, `rrtm_reference_atmosphere`, `rrtm_ozone_climatology`, `rrtm_kdist_band01-16`), 1451/1451 `./esm test` (real32 dumps, rel 1e-5): column mapping incl. the O3DATA climatology as a layer integral (with WRF's one-layer shift, B5), SETCOEF, all 16 TAUGB band routines and the GASABS quantisation, the RTRN sweep with the full cloudy branch, heating rate; assembled end-to-end in `couplings/rrtm_lw_column*.esm` (stages gained `<x>_in` coupling-target parameters, gas optics a `pfrac_flat` output). Cloud-overlap coverage added 2026-09-07 (dumps `scm_ref2_120.json`, `scm_cloud_{240,396,648}.json`): `rtrn_sweep.esm` 196 assertions in 7 regimes — clear, optically thick overcast (call 120, TAUCLOUD <= 0.760, ABSCLD <= 0.72, 31.9 W/m2 surface cloud forcing), fractional cloud (call 240, CLDFRAC = 0.1470 at TAUCLOUD = 0.009966, the largest the scheme admits), three-layer fractional overlap (call 648) — and `rrtm_column.esm` 480 assertions in 8 regimes incl. the `TAUCLOUD > 0.01 -> CLDFRAC = 1` override and the zero-fraction override (call 396, N58). Mutation-checked: dropping the CLDFRAC weighting from EFCLFRAC fails 16/28 assertions in each fractional regime while clear and pure-overcast stay green. Warm and mixed-phase cloud optics added 2026-09-07 from a new reference case, the idealized supercell em_quarter_ss with `ra_lw_physics = 1` on a 59-layer / ztop = 12800 m grid chosen so p_top = 177.07 hPa keeps NLEV = 59, NBUF = 44 (esm tests cannot override metaparameters), dumped through the new `ESM_DUMP_I` column selector (fork 5cb62da; dumps `qss_i20_{61,268}.json`, `qss_i40_396.json`): `rrtm_column.esm` 691 assertions in 11 regimes — a pure warm liquid cloud (TAUCLOUD 1.26-6.43 = ABCW CLWP alone), a mixed-phase storm column (TAUCLOUD <= 18.74 with pure-rain, liquid+rain, all-four, pure-ice and snow-dominated layers) and an ice-and-snow anvil (layers 17-30 pure snow, TAUCLOUD 0.047-0.465 = ABSN PIWP alone) — and `rtrn_sweep.esm` 224 assertions in 8 regimes, the new one the opaque limit (ABSCLD = 1 to 3e-14 in 25 layers, 45.4 W/m2 surface and 98.9 W/m2 TOA cloud forcing, HTR - HTRC = 41.0 K/day). Mutation-checked at +10 %: ABCW fails 10 assertions, ABRN 6, ABSN 12, all confined to the three new regimes; the SCM regimes stay green. Nothing in MM5ATM's cloud block is now untested except graupel, which WRF itself discards (N62); geometry coupling-target parameters added 2026-09-15 (standalone counts unchanged); EarthSciModels PR refresh needed. 2026-09-20: `rrtm_column.esm` gained the state coupling-target parameters qv_in, qc_in, qr_in, qi_in, qs_in and cldfra_in, lowered by `couplings/tests/scmstate_couple_rrtmcol_inputs.esm`; cldfra_in is fed by the new CalCldfra component, which is what retires the dumped cloud-fraction profile. 1451/1451 unchanged |
| Cumulus (New Tiedtke) | trigger, updraft/downdraft, closure | `phys/physics_mmm/cu_ntiedtke.F90` | — | **instrumented (2026-09-22); required since 2026-09-22**: real-terrain row dumps (driver inputs, flipped kernel-boundary arrays, kernel outputs, tendencies) at 7 steps × 5 rows in `gwdo_real/dumps`, see `data/eqwefic/notes/gwdo_real_reference_run.md`; plus 16 idealized-column dumps in `dumps/chem170_wrf` covering ALL THREE convection types (deep `ktype` 1, shallow 2, mid-level 3) and the inactive branch, from a Weisman-Klemp column at dx = 20 km. Real64 kernel driver `wrf-chem170/kernels/ntiedtke_driver`, built from a copy of the kernel with an inner dump spliced in after `cumastrn`, so `ktype`, cloud base/top and cumastrn's own tendencies and mass fluxes come out. **Use the real64 replay as the reference:** WRF forms `rthcuten` as a real32 difference over dt = 60 s, which is cancellation-limited at ~3e-7 K/s (N84). See `data/eqwefic/notes/chem170_scm_reference_run.md` |
| Gravity-wave drag (GWDO) | orographic GW stress, flow blocking, wind-reversal limiter (Hong et al. 2025 revision, needs `ELVMAX`) | `phys/physics_mmm/bl_gwdo.F90` | — | **instrumented (2026-09-22)**: real-terrain reference run (Last Chance d01, 8.1 km, 18 h, `gwd_opt = 1` + New Tiedtke + Noah), 140 row dumps (`gwdo_real/dumps`, WRF fork branch `earthsciml-instrumented-gwdo` @ 790bb4f); real64 kernel driver `kernels/gwdo_driver`, real32 replay bit-identical on 140/140 rows; GWDO off in the daytime mixed layer, active at night (FORTRAN_BUGS B17: silently off without `ELVMAX`); see `data/eqwefic/notes/gwdo_real_reference_run.md` |
| Subassembly: surface+land+PBL column | — | SCM dumps | — | not started |
| Subassembly: radiation column | — | SCM dumps | — | not started |
| Subassembly: full SCM physics suite | RRTM LW chain + DudhiaSW → rthraten, SfclayRev → u*/fm/fh/Ri_b/wspd/10 m wind/z0 → YSU, slab hfx/qfx → YSU, exposing WRF's whole driver-level tendency vector (rthblten, rublten, rvblten, rqvblten, **rqcblten**, **rqiblten**, rthraten, rth_phys_sum, hfx, qfx, gsw, glw, olr, pblh) | merged dumps `esm_dump_scm_suite_<step>.json` (`tools/merge_dumps.py` over `subassembly_all/_pbl/_rad` + the sfclayrev/sw/ysu/rrtm kernel dumps of the same step) | `couplings/` | stub with tests (2026-09-07; slab co-mounted 2026-09-12): `couplings/scm_physics_column.esm` (day, step 1410 / call 2820) and `scm_physics_column_night.esm` (deep stable night, step 900 / call 1800), 2 x 50/50 with `./esm test <file>`. 9 mounts, 167 coupling edges. **Five of the six namelist schemes are now co-mounted.** The slab entered 2026-09-12: EarthSciAST #213 (mount-edge `index_set_rename`) plus #298 (a top-level `models.<k>` `{ref}` mount runs the full §4.7 edge pipeline) let its 4-cell soil column sit beside the 59-layer atmosphere as `soil`/`soil_nodes`, and the same #298 made the documents' own 44-entry `index_sets` block obsolete AND actively wrong (`rlay` now arrives folded to 103) — deleting it was the repair. The chain is WRF's: SfclayRev `flhc`/`flqc` and this step's `Sw.gsw`/`Heat.glw` drive the slab's energy balance, and the slab's `hfx`/`qfx` drive YSU instead of being prescribed. 15 slab outputs exposed, 8 asserted; tolerances are the measured N12 instantaneous-vs-end-of-step gap (hfx 7.5e-4, qfx/lh 3.1e-4, qsfc 1.8e-4 by day), and YSU seeing the instantaneous flux moved `K_h`/`K_m` by Linf 0.042/0.025 m^2/s so those two loosened to `abs` 0.06/0.04. **WSM6 is still out**, but no longer for an EarthSciAST reason: the SCM column is cloud-free (`qc = qr = qs = qg = 0`, `qi` <= 5.2e-8), the merged dump has no `mp_` variables, and microphysics is a separate end-of-step driver call; it is covered by `couplings/microphysics_column{,_mixed}.esm` on the supercell column. |
| Deferred: RRTMG LW/SW, Noah-MP, MYNN, Thompson, KF | | | | — |
| Subassembly: surface layer + PBL | shared column state, sfclayrev → YSU couplings (u*, fm, fh, Ri_b, wspd, 10 m wind, z0) | driver-level dump `subassembly_pbl` (fork 592f693) | `couplings/` (EarthSciModels convention) | stub with tests (2026-09-06): `couplings/surface_pbl_column.esm`, 31/31 with `./esm test <file>` (step 60); slab not co-mountable (gap (n)) |
| Subassembly: surface layer + slab | shared state, sfclayrev FLHC/FLQC → slab | driver-level dump `subassembly_pbl` + slab real64 replay | — | stub with tests (2026-09-06): `couplings/surface_soil_column.esm`, 20/20 with `./esm test <file>` |
| Subassembly: RRTM LW end-to-end | RRTMColumn -> RRTMSetcoef -> RRTMGasOptics -> RTRNSweep -> RRTMLWHeating coupled by `variable_map` into `<x>_in` parameters | kernel dumps `scm_ref2_<call>.json`, `scm_cloud_240.json` | `couplings/` | stub with tests (2026-09-06, fractional-cloud regime 2026-09-07): `couplings/rrtm_lw_column{,_call1,_call240,_call1440,_call2400}.esm`, 5 x 25/25 with `./esm test <file>`; end-to-end residual <= 3.3e-3 W/m2 (itr quantisation flips at call 1440), <= 1.5e-4 W/m2 at call 240 (no flip). Regimes: clear (1), fractional cloud (240), overcast + weak fractional (600, 1440), overcast (2400); the thick overcast deck (call 120) is covered end-to-end by `couplings/radiation_column.esm` and `physics_column.esm` |
| Subassembly: radiation (RRTM LW + Dudhia SW) | LW chain + DudhiaSW on one 59-layer column, rthraten = dthdt_lw + dthdt_sw | driver-level dumps `subassembly_rad_60/_300` | `couplings/` | stub with tests (2026-09-06): `couplings/radiation_column{,_night}.esm`, 2 × 30/30 with `./esm test <file>` |
| Subassembly: full physics column (radiation + surface layer + PBL) | RRTM LW chain + DudhiaSW → rthraten → YSU; SfclayRev → u*, fm, fh, Ri_b, wspd, 10 m wind, z0 → YSU; `rth_phys_sum = rthraten + ∂θ/∂t|PBL` | driver-level dumps `subassembly_all/_rad/_pbl` (steps 60, 300) + kernel dumps of the same steps | `couplings/` | stub with tests (2026-09-06): `couplings/physics_column.esm` (day) and `physics_column_night.esm` (stable), 2 × 40/40 with `./esm test <file>`; slab excluded (gap (n), fluxes prescribed), WSM6 excluded (in-place step); `ysu.esm` gained `rthraten_in` |
| Subassembly: microphysics step | Sat -> Melt -> {Warm, Cold, IceDep} -> Rescale -> SatAdj coupled by `variable_map param_to_var` into `<x>_in` parameters, including the `sd`/`sat` subsystem parameters of the mounted stages | kernel dumps `qss_ref_9983` (cols 24, 30) | `couplings/` | stub with tests (2026-09-07): `couplings/microphysics_column.esm` (liquid-dominated core) and `microphysics_column_mixed.esm` (glaciated flank, rain-budget rescaling binding at f_qr = 0.59), 2 x 40/40 with `./esm test <file>`; 7 mounts, 106 coupling edges; sedimentation excluded (in-place PLM, not a rate), so the chain starts at melting/freezing with a dumped post-sedimentation state; end-to-end residual <= 6e-6 of the column maximum in the rates and <= 1.2e-5 in pcond; the document no longer declares `lev` at all — the leaves' index sets merge in at the mount edge (#298), which retired both the old literal-40 sizing workaround and EarthSciAST #236. Briefly red 2026-09-12 (#311: the mount-edge template injection did not reach the stages' nested `sd`/`sat` subsystem, `unlowered_operator: input_T`); fixed by EarthSciAST PR #313, merged 2026-09-14, and **2 x 40/40 on the CLI built from main @ 58d21a1a1 (2026-09-15)** |

## EqAtmChem — WRF-Chem `chem_opt=1` then **`170`** (CBMZ_MOSAIC_KPP; revised 2026-09-22 from `300`, see PLAN.md 1.4b)

| Component | Fortran source | Existing | Status |
|---|---|---|---|
| Anthropogenic emissions | `chem/emissions_driver.F` | `earthsci_data/nei2016_monthly.esm` (pattern) | not started |
| Biogenic emissions | `chem/module_bioemi_*.F` | — | not started |
| Dry deposition (Wesely) | `chem/dry_dep_driver.F`, `chem/module_dep_simple.F` (`wesely_driver` -> `rc` -> `landusevg` -> `depvel` -> `cellvg`), applied by `chem/module_vertmx_wrf.F`; dump hooks `esm_dump_want('wesely')` and the `ESM_SWEEP` regime sweep in the fork | **NEW: `atmospheric_deposition/wrf_wesely/{wesely_data,wesely_parameters,surface_resistance,deposition_velocity}.esm`**. The pre-existing `wesley_dry_gas.esm` + `wesley1989/` is NOT used and NOT modified | physics, **903/903** (2026-09-21). The pre-existing component is a migration of `AtmosphericDeposition.jl` -- the Wesely 1989 PAPER plus Walmsley & Wesely 1996, 11 Wesely categories, GEOS-Chem species -- and WRF's code is a different scheme wearing the same name; all 19 overlapping species differ on at least one constant (H2O2 H* by 671x, ACTA 235x, CSL 952x; PAN/GLYX/MGLY/ACTA f0 flipped 0<->1), there is no `dhr` at all, no Erisman branches, an Rc ceiling of 9999 s/m where WRF has none, and 19 of the 38 gases are missing. Adding WRF tests to it would only have made them red, so a fresh component was written. **`surface_resistance.esm` 602/602** (7 meteorological states x 43 gases): all four Rc paths -- general, the O3 branch with its `min(100., rac)` cap, and the wholesale Erisman SO2 and NH3 replacements -- over the 25 USGS categories with dep_init's per-category season overrides, and the `dhr` temperature correction. **`deposition_velocity.esm` 301/301**: Ra as the Businger HEAT profile with the 0.74 Prandtl factor at `zr = 2 m`, Rb with `Pr = 0.72`, and **`cellvg`**, whose omission is the single largest structural error available here -- measured, skipping it overstates vd by 19% for HNO3, 18% for N2O5, 25% for OH. Tendency exposed as `vd` + `surface_flux_coeff` + `k_dep = vd/dz1`, with ONE `dz1` serving both cellvg's cell height and the layer thickness. **Ground truth**: `dumps/wesely/` from `chem_scm_dep/` -- the reference `chem_scm` run has `gas_drydep_opt = 0` and produces NO dry-deposition truth at all. **BOTH COVERAGE GAPS NOW CLOSED (2026-09-21).** The in-model dumps only ever see `iseason = 2`, `iland = 2`, a dry surface and one stability, so an `ESM_SWEEP` hook was added to `wesely_driver` that calls `rc()` over all 25 land-use categories x 5 seasons x 4 wetness/high-NH3 states, a 5-point temperature ladder, and `landusevg()` over a 5 x 3 stability x friction-velocity grid (including `rmol = 0` exactly). Total now **2048/2048**: `surface_resistance.esm` **1642** and `deposition_velocity.esm` **406**. That verifies the wet, rain and snow cuticle branches, the Erisman SO2/NH3 land-class cascades, the `dhr` correction and the 0/40 C and -1/-5 C thresholds, and all three branches of the Businger stability function and of cellvg's integral. **FORTRAN_BUGS B4 is thereby verified LIVE**: `rluo1` is 545.45 s/m as WRF writes it vs 2000.0 under the Eq. 11 spelling used 70 lines later (factor 3.667 at rlu = 2000; 1500.0 vs 2700.0 at rlu = 9000), and O3 under dew takes `rluo1` outright. Fortran findings B4-B7. | physics done, all regimes verified |
| Photolysis (Madronich, `phot_opt = 1`) | `chem/photolysis_driver.F` -> `chem/module_phot_mad.F` (`madronich1_driver` + `photolysis_mad`); dump hook `esm_dump_want('photmad')` in the fork, scheme `photmad` | `gaschem/madronich/{madronich_parameters,solar_geometry}.esm`. **`gaschem/fastjx/*` is NOT a starting point and is not used** | instrumented + solar geometry done (2026-09-21). **The reference SCM runs `phot_opt = 1` = PHOTMAD, not Fast-J and certainly not Fast-JX** (Fast-JX is not a WRF-Chem option at all; `phot_opt = 2` is Fast-J). Full diagnosis of why `gaschem/fastjx/*` cannot be repaired into it, 13 numbered defects, in `data/eqwefic/notes/photolysis_diagnosis.md`: different scheme and lineage, 0-D where the physics is a column, 10 of RADM2's 21 j-values present, actinic flux TABULATED over 18 bands where Madronich SOLVES delta-Eddington over 101 bins with Rayleigh/O3/O2-SR/aerosol/cloud optics and a wavelength-dependent albedo, no Chapman function, no 325 DU rescaling, no coupling to the model's own O3, and zero tests. **Ground truth established**: `dumps/photmad/` (calls 1, 60, 1200, 1410; calls 300/720/900 are night, the driver branches to label 199 and writes nothing) and `dumps/photmad_sunset/` (calls 230-266, zenith 80.8-87.6 deg, the Chapman branch), from `chem_scm_phot/`. Each dump carries the inputs, the FULL static tables (`wl`, `fext`, `xs`, `xqy`, `txs`, `jpl295/218`, `sra`, `srb`, the standard profiles), the regridded column, `albedoph`/`arayl`/`aaer` and the scalar optical constants, `s`/`qy`/`ao3`/`ao2` at 3 sample levels, the actinic fluxes `fldir/fldn/flup` + `endir/endn/enup` on the RT grid, `d_rt`, and both `phot1_s` (s^-1) and `phot1_min` (min^-1). **Verified identity**: `phot1_s(n,k) == jv(n,k)` of `dumps/radm2_wrf/*.json` for n = 1..22 to 1.1e-7 relative (the real32 x60/60 round trip) at all four daytime calls, so the RADM2 dumps are themselves an independent check on any reimplementation. `jv(1:21)` is what RADM2 reads; `jv(22)` = macr is computed and unused; `jv(29,50,51,52)` are the `!liqy` chlorine additions `phot1(25,23,24,26)`, also unused by RADM2. **Done**: `solar_geometry.esm` = `calc_zenith` + `chap` + `zenita`, **52/52** (13 regimes x 4 assertions, zenith 48.9-87.6 deg, nine of them in the Chapman branch), plus `madronich_parameters.esm`. **CHAIN NOW CLOSED (2026-09-21): 409 assertions in `components/gaschem/madronich/` + 84 in `couplings/madronich_column.esm`, 0 fail, 0 err.** Seven components -- `solar_geometry` 52, `column_setup` 148, `layer_columns` 37, `spectra` 36, `optical_depth` 11 (optical depths, delta scaling, `sphers`, the direct beam), `two_stream` 20 (`ps2str`, the 138-unknown tridiagonal delta-Eddington solve), `j_values` 105 -- plus a coupled document that mounts the last two over 9 coupling edges and computes **all 21 RADM2 j-values at 4 levels from the column state**, nothing about the radiation field supplied. Verified in float64 against the dump before authoring: the full chain reproduces WRF to **8.9e-6** relative on the 21 j-values at 7 levels. The expected values are `phot1_s`, which equals `jv(1:21)` to 1.1e-7, so `dumps/radm2_wrf/*.json` is an INDEPENDENT second check. The only recurrence in the whole chain is the Thomas pivot in `two_stream` (esm-spec 4.3.1.1, justified in the component description); `tauc` and `tausla`, which the Fortran accumulates by running addition, telescope exactly to contractions. **`srband` was NOT written: it is dead code** (FORTRAN_BUGS N22). **CLOUDY COLUMNS NOW COVERED (2026-09-22), Option A as the user chose it**: `cloud_grid.esm` (100) builds subgrid's cloud-refined grid on a FIXED NRT = 131 levels padded with zero-thickness duplicates of the top, and `couplings/madronich_column_cloudy.esm` (**315**) closes the chain on all four dumped columns (nlevel 70, 72, 77, 78). The padding equals WRF's unpadded computation to float64 roundoff (<= 1.8e-13 on O(1) fluxes), NOT bit-identically: padded layers carry WRF's 1e-36 optical-depth floor, and sphers's thickness division needs a guard with no Fortran counterpart -- see `data/eqwefic/notes/cloudy_column_design.md` s7. **Outside its regime the run STOPS** (2026-09-22): the component implements idt = 1 only, and without a check that was silently WRONG inside the bound (a very thick cloud layer: WRF 77 levels, unchecked component 72). Two shaped-parameter sentinel gathers now raise `E_TREEWALK_CONSTARRAY_OOB` naming the cause -- `'idt_supported' index 4 out of range 1..1` and `'rt_level_capacity' index 139 out of range 1..131` -- verified by `tools/check_cloud_grid_bounds.py` through the real file. Bound with idt = 1 and model-level cloud: 70 <= nlevel <= 130. Not built on a closed-function error, which the Rust binding turns into NaN (EarthSciAST #443). Relies on a non-const `interp.linear` axis that Julia/TypeScript reject at load (#443). FORTRAN_BUGS B11 (air interpolation divides by a sum of heights) is reproduced, not corrected. |
| RADM2 gas mechanism | `chem/KPP/mechanisms/radm2`; dump hooks `chem/KPP/inc/radm2/kpp_mechd_{u,l,b,ib,ia,a}_radm2.inc` (fork 2ce0388) | — | physics, rate coefficients + trajectories + tendencies (Phase 0/1, 2026-09-06; reaction-system tests first EXECUTED 2026-09-12, EarthSciAST #251): `components/gaschem/radm2/radm2.esm` (`RADM2RateConstants` model + `RADM2` reaction system, 157 entries for KPP's 156 reactions — `R060x` is a shadow reaction of rate `0.9 k_R060` standing in for radm2.eqn's negative product `-0.9 OH` — and 61 species) + `radm2_ratelaws.esm` (11 WRF rate-law templates) + `RADM2Tendencies`; **825/825 `./esm test`**: 540 thermal rate-coefficient assertions × 4 states at rel 1e-9 (rc_n2o5 1e-6; measured residual 6.2e-16 on 134 of 135), WRF-Chem and box-driver `RCONST(1:156)` bit-for-bit at 5 of 6 dumped levels; **108 trajectory assertions** (3 box regimes × 12 species × 60/300/1800 s) against the real64 KPP Rosenbrock box driver, green at rel 1e-5 with measured agreement better than rel 1e-7 everywhere; **177 tendency assertions** (3 regimes × all 59 variable species) against `Vdot` from `radm2_Fun`, each an observed `d<SPC>_dt = D(RADM2.<SPC>, t)`, 102 bit-identical and max residual 9.5e-7 (cancellation in the ALD budget, verified not stoichiometric). Gaps (p) and (q) closed. `H2O`/`M` are `constant: true` reservoir species → `parameter_overrides`, not `initial_conditions` (esm-spec §7.4). Document declares `"esm": "1.1.0"` for the `solver` block (reltol 1e-9, abstol 1e-3 = the box driver's own): the runner's default abstol 1e-6 collapses the step size in both daylight regimes. The 1.1.0 declaration does NOT block EarthSciModels PR #24: that repo's CI installs `earthsci_ast` from `git+https://github.com/EarthSciML/EarthSciAST.git@main` (`.github/workflows/test-esm.yml`), not from a release, and main has carried 1.1.0 since PR #299 — nothing here waits on a version being minted. Fortran bug B8 fixed in the fork (55d1c08): WRF-Chem box states now usable — `dumps/radm2_wrf_b8`, 13 calls × 59 levels of 60 s chemistry windows (WRF RTOL 1e-3; replay via the box driver for tight references); no multi-step WRF species trajectory (operator splitting). EarthSciModels PR #24 (not refreshed). |
| Vertical mixing of chem species | `chem/dry_dep_driver.F` (the `do nv` mixing loop) -> `chem/module_vertmx_wrf.F`; dump hook `esm_dump_want('chem_vertmx')` plus a real64 kernel driver in `data/eqwefic/vertmx_kernel` | `gaschem/chem_transport/{chem_vertical_mixing,vertmix_parameters}.esm` | physics, **160/160** (2026-09-21). More coupling than new physics: WRF-Chem mixes every gas with the PBL **heat** diffusivity `exch_h`, which is exactly `ysu.esm`'s `K_h` (already verified here to ~1e-7), and the surface flux is `WRFWeselyDepositionVelocity.vd` -- so the two halves already built meet here. Operator `dC/dt = (1/rho) d/dz(rho K dC/dz)` with the ground flux replaced by the deposition loss and zero at the model top, written as that derivative and lowered by ESD's `varcoeff_face_laplacian_lev_flux_bc`, which is WRF's stencil exactly (interface coefficient, no face averaging, layer-CENTRE gradient spacing, prescribed fluxes at both faces). Checked rather than assumed: the rule derives layer centres as `(ze[k]+ze[k+1])/2` while WRF passes its own `zz`; they agree to 5.8e-8. **Ground truth needed a real64 kernel driver** -- the in-model solve is real32 and theta-weighted, so at a `dt` small enough to be a rate the change is 1-2 ulps (N25). 4 calls x 10 species. Fortran findings B10 (WRF discards the top layer's mixed value), N25. | physics done |
| Wet scavenging | `chem/module_wetscav_driver.F` | `atmospheric_deposition/wet_deposition.esm` | not started |
| CBM-Z gas mechanism (`chem_opt = 170`) | `chem/KPP/mechanisms/cbmz_mosaic` (66 species, 142 reactions, 52 j values); dump hooks `chem/KPP/inc/cbmz_mosaic/kpp_mechd_{u,l,b,ib,ia,a}_cbmz_mosaic.inc` (fork branch `earthsciml-instrumented-chem170` @ eae782d) | — | **instrumented (2026-09-22)**: 16 column dumps of the full KPP interface (`var` in/out, `fix`, `RCONST` after `Update_Rconst`, `jv`, TEMP, C_M, C_H2O, p, rho, qv) in `data/eqwefic/dumps/chem170_wrf`, day and night. The RADM2 copy loops are symmetric here, so B8 does not apply (CBM-Z has no CO2 and only H2O/M are fixed). See `data/eqwefic/notes/chem170_scm_reference_run.md` |
| MOSAIC aerosol (8 bin, `chem_opt = 170`) | `chem/module_mosaic_driver.F` (`mapaer_tofrom_host` → `aerchemistry` → `mosaic_newnuc_1clm` → `mosaic_coag_1clm`), `chem/module_mosaic_drydep.F`, `chem/module_mosaic_initmixrats.F`; dump hooks `esm_dump_want('mosaic')` and `('mosaic_drydep')` in the fork | — | **instrumented (2026-09-22)**: 16 column dumps carrying the MOSAIC column state `rsub` (235 rows, named) BEFORE and AFTER each sub-process separately — gas–particle transfer and water uptake, nucleation, coagulation — plus the WRF chem/moist columns in and out; and 16 dry-deposition dumps (per-bin deposition velocities, section geometry, composition pointers, u*, aerodynamic resistance). Each stage is an operator-split increment over `dtchem` = 60 s; since 2026-09-22 (fork `a40edfe`) every sub-process ALSO writes its **instantaneous rate where it forms it** (`rate_newnuc`, `rate_coag_num`/`rate_coag_vol`, `rate_cond_nonvol`, `rate_cond_semivol`), which is what a component must be referenced against: the coagulation increment is unresolvable in real32 for ~20 % of entries (N87), NH3's condensation flux reverses sign inside the step, and nucleation is an instantaneous projection whose stock-WRF form was dt-dependent through bug **B20**, fixed in the fork on 2026-09-22 and the reference recaptured (the vapour now relaxes to critical rather than being stripped, 1.0e-4 -> 0.063 of the incoming vapour at step 1; new particles 30x-60x more neutralised; exactly dt-independent at dtnuc = 60/6/0.6 s; stock dumps kept at `dumps/chem170_wrf_stockB20`). Two Fortran findings: N82 (with the shipped `CASENAME 4`, `chem_in_opt = 0` starts every bin at exactly zero — the initializer is compiled out) and N83 (hard-coded component index 9 in the coarse-dust mode). Emissions, wet scavenging and cloud chemistry remain off |
| MOSAIC gas-particle mass transfer | `chem/module_mosaic_therm.F` `aerosolmtc` + `gas_diffusivity`, `mean_molecular_speed`, `fuchs_sutugin`; the sectional path | — | **physics (2026-09-23)**: `components/aerosol/mosaic/{gas_particle_mass_transfer,mosaic_mass_transfer_parameters}.esm`, **40/40**. Per gas and per bin (both independent, so a consumer maps over them). 25 cases: 3 steps x levels 1/10/25/45, bins 1/4/7, and H2SO4, HNO3 and NH3 so all three molar volumes are exercised. Residual 3.9e-8 median, 1.7e-7 worst on `kg`; `cond_rate` = gas*kg, the EXACT dt -> 0 limit of ASTEM's analytical non-volatile solution, 3.7e-8 / 1.5e-7; bound `rel 5e-7`. Needed a new hook (the condensation hooks record gas*kg, so kg is not recoverable from them); the recapture is bit-identical on 23,616 field comparisons. **Not the whole of gas-particle transfer**: the semi-volatile branch is kg*(gas - surface value) with the particle thermodynamics and adaptive sub-stepping, and is not transcribed here. `dp_wet` and `num_a` are inputs, from MOSAIC's size framework. The Gauss-Hermite modal path is not transcribed (WRF-Chem runs the sectional one) |
| MOSAIC aerosol dry deposition | `chem/module_mosaic_drydep.F` (`mosaic_drydep_1clm` wet-size preamble + `aerosol_depvel_2`, the Binkowski-Shankar kernel), `aer_drydep_opt = 1` | — | **physics (2026-09-22)**: `components/aerosol/mosaic/{dry_deposition_velocity,mosaic_drydep_parameters}.esm`, **12/12**. Per size bin (the bins are independent, so a consumer maps the component over them); 4 steps x bins 1, 4 and 8. Residual 3.0e-7 median, 7.2e-7 worst against the dumped per-bin velocities, bound `rel 2e-6`. The two composition sums (drymass, dryvol) are inputs: they map MOSAIC's species pointers rather than being physics. **Only `vdep` can be pinned** -- WRF stores none of the intermediates -- and two branches are transcribed but unexercised by any WRF-Chem run: `alnsg` is always log(1) = 0, which collapses the moment-0 and moment-3 forms, and `depresist_unstabpblfact` is always 1 |
| MOSAIC nucleation (binary H2SO4-H2O) | `chem/module_mosaic_newnuc.F` `wexler_nuc_mosaic_1box` (the `newnuc_method = 2` branch); rate hooks in the fork (`module_esm_mosaic_rates.F`) | — | **physics (2026-09-22)**: `components/aerosol/mosaic/{nucleation_wexler,mosaic_nucleation_parameters}.esm`, **72/72** `./esm test components/aerosol/mosaic`. Nine regimes from the B20-corrected chem170 run (NH4/SO4 of the new particles 0.0007 to 1.37, i.e. both composition branches, plus the NH3 cap). Exposed as an **increment per call**, not a rate: the scheme is an instantaneous projection and returns the same increment at dtnuc = 60, 6 and 0.6 s. Tolerances measured, not assumed: dens_part 2.7e-8, dq_nh3/dq_nh4a <= 4.5e-8, q_crit <= 1.3e-6, and nh4_per_so4/dq_h2so4/dq_so4a/dq_num <= 5.5e-5 because the excess q_h2so4 - q_crit amplifies real32 round-off in the critical concentration by 1.2x to 187x. The ternary (Napari) route is unreachable at `newnuc_method = 2` and is not transcribed |
| GOCART aerosols | `chem/module_gocart_*.F` | — | not started (superseded as the aerosol target by MOSAIC, 2026-09-22) |

## EqAtmFire — NCAR fire_behavior (Community Fire Behavior Model, WRF 4.8 `phys/fire_behavior`)

| Component | Fortran source | Existing | Status |
|---|---|---|---|
| Fuel categories table (Anderson 13) | `physics/fuel_anderson_mod.F90` | `wildland_fire/fuel_model_lookup.esm` (not reused: different loads/depths/T_f divisor) | PR EarthSciModels #23; physics (Phase 1, 2026-09-06): `components/wildland_fire/fire_behavior/anderson13_fuel_table.esm` (incl. waf), 107/107 |
| Fuel moisture model | `physics/fmc_wrffire_mod.F90` | `wildland_fire/nfdrs/*` (different formulation) | PR EarthSciModels #23; physics: `fuel_moisture_wrffire.esm` (five-class time-lag ODE + Fortran step), **214/214 (2026-09-21)**. The drying branch is 144 assertions from the test7 moisture run as before. The WETTING branch and the EXPONENTIAL update branch are now closed too: no reference RUN has rain (`fire_rain` is identically 0 through the whole 12 h Last Chance Gulch fire as well), so the reference was generated by a new kernel driver, `data/eqwefic/fire_build/fmc_rain/fmc_rain_driver.F90`, which calls `Advance_moisture_classes` directly on an 8-cell strip at 0, 0.04, 0.05, 0.5, 2, 8, 40 and 200 mm/h and at two step lengths (dumps `dumps/fire/run_fmc_rain/`). Four rain tests plus a rain-free 600 s control (70 assertions) cover the collapsed bracket `emc_d = emc_w = 2.5`, the saturating `rlag = rec_wetting_lag (1 - exp(-r/8))`, and `change > TOL` on both the wetting and the drying side -- every earlier test had `change <= 1.4e-4`. All five passed against the Fortran on the first run. New observeds (pure additions): `dfmc_g_dt` = d(fmc_g)/dt, the class-weighted tendency, and `fmc_g_state` = the same mean of `fmc_old`, which is the STATE the rest of the chain is evaluated at. New Fortran findings: B14 (NUOPC feeds `fire_rain` in metres into a mm-scaled threshold, from uninitialised memory), B15, N71-N73, N75 |
| Rate of spread (Rothermel + limits) | `physics/ros_wrffire_mod.F90` | `wildland_fire/rothermel/*` (not reused: Rothermel-1972 A, w0/(1+st), moist fgi, projection form, 6 m/s cap, chaparral) | PR EarthSciModels #23; physics: `rothermel_wrffire.esm` 104/104 + `ros_wrffire.esm` 44/44 |
| Level-set front propagation | `physics/level_set_mod.F90` | `wildland_fire/level_set_fire_spread.esm` (PDE structure matches, wind/slope factors differ) | PR EarthSciModels #23; physics (2026-09-07, extended 2026-09-21): `level_set_tendency_wrffire.esm` -- the PDE on `cartesian_uniform_2d`, with the DISCRETISATION SELECTED BY THE ESD RULES EACH TEST IMPORTS -- **263/263**: 91 vs a `fire_upwinding = 4` run, 112 vs the DEFAULT `fire_upwinding = 9` hybrid WENO5/ENO1 run, **14 vs a `fire_upwinding = 7` (pure WENO5) rerun**, **15 vs a `fire_upwinding = 10` (cosine blending zone) rerun**, and **31 vs a `fire_upwinding = 8` (hybrid WENO3/ENO1) rerun** (2026-09-21, dumps `dumps/fire/run_blend/`), which needed a new ESD rule `weno5_eno1_blend_norm_D2_extrapolate_bc` plus three stencils -- the library's first smooth partition-of-unity operator, the mask written as one clamped closed form rather than the reference's three branches. Discriminating: 185 of 6400 cells differ from the option-9 run and 173 of them are inside the blending zone (233 cells), largest 0.468 m/s against 2.0e-3 outside; swapping the hard-edged rule back in fails exactly the in-zone assertions. The component now mounts `fire_behavior_parameters.esm` as `par` for the free name `pi_blend` (fire_behavior's truncated `PI = 3.1415926`) (2026-09-21, dumps `dumps/fire/run_weno5/`). Option 7 needed no new rule: `case (7)` is byte-for-byte `case (9)`'s in-band branch with the `abs(lfn) < threshold_hlu` test removed, so the same weno5_eno1 rules reproduce it with `weno_band_cells` past the domain diagonal. It is genuinely discriminating -- 3480 of the 6400 interior cells differ from the option-9 run, by up to 1.30 m/s. The ENO1 wall strip is outside the `select case` and still applies, so wall cells are asserted too. `ignition_time_wrffire.esm` 6/6. Rules: EarthSciDiscretizations PR #37. **Coupling hook added 2026-09-21** (pure addition, 203 -> 203 on the old tests): the PDE uses `ros`, which is the supplied `ros_supplied` everywhere except cell `(i_coupled, j_coupled)`, where it is the coupling target `ros_in`; `i_coupled = 0` disables it, and `couplings/wildlandfire_behavior.esm` uses it to drive one cell's spread rate from the mounted fuel/moisture/Rothermel/ROS chain. **Option 8 closed 2026-09-21** (dumps `dumps/fire/run_weno3/`, 31 assertions): `case (8)` is `case (9)` with `Select_weno5` replaced by `Select_weno3`, and it needed a whole new ESD rule family -- `weno3_eno1_norm_D2_extrapolate_bc` + `weno3_eno1_D1x/D1y_extrapolate_bc` over four new stencils -- because there was no WENO3 on `cartesian_uniform_2d` at all and the library's only WENO3 anywhere is the 1-D `weno3_recon`, which is WENO-**Z** (`tau3`, eps 1e-6) rather than the classical Jiang-Shu `gamma_k/(beta_k + eps)^2` this code computes, so it could not be adapted (EarthSciDiscretizations PR #41, stacked on #40 and #37). Discriminating at the maximum available: on the run's own psi, options 8 and 9 differ in 175 of 6400 cells -- EVERY cell the WENO branch touches -- and agree to exactly zero in the other 6225; largest difference 0.550 m/s in `tend`, 0.377 in `|grad psi|`. Swapping the WENO5 rules back in fails 27 of the 31 assertions and leaves green exactly the four control cells outside the band or in the wall strip. Still not transcribed: `fire_upwinding` 0-3 and 5 (each needs a new ESD rule family AND a rerun) and 6 (which now needs only a rerun, see N81 and the notes below the table); none is selected by any shipped configuration, REINITIALISATION (N76: `fire_lsm_reinit` defaults to `.true.`, so it runs in every reference run; it is an operator on the STATE, not a term in d(psi)/dt), and `tbound`, which N74 shows is not a CFL REDUCTION at all -- it is computed after the tendency and only used to ABORT the run, so it cannot affect an instantaneous derivative and is deliberately omitted |
| Level-set REINITIALISATION | `physics/level_set_mod.F90` `Reinit_level_set` / `Advance_ls_reinit` | — (nothing in EarthSciDiscretizations or EarthSciModels implements a reinitialisation, signed-distance or eikonal SOLVER; the ESD eikonal files are MMS drivers that measure a gradient-norm operator) | **physics (2026-09-21): `components/wildland_fire/fire_behavior/level_set_reinit_wrffire.esm`, 45/45.** The PDE d psi/d tau = S(psi_0) (1 - |grad psi|) (Sussman/Smereka/Osher 1994 as adapted by Munoz-Esparza et al. 2018), lowered on `cartesian_uniform_2d`. **It is NOT part of d psi/dt** -- it runs between the propagation and the ignition-time update and re-signs the state -- so the acceptance criterion is its own pseudo-time tendency against the Fortran, which is what the 45 assertions check; it makes no claim about the instantaneous tendency. Why it matters anyway: the propagation scheme's WENO5/ENO1 band test is written on `|psi|` itself, so a psi that drifts from a signed distance function silently changes which discretisation each cell gets. THREE NEW ESD LIBRARY FILES on `integ/fire-column-rules` (local, unpushed; turn into an ESD PR): `stencils/weno5_eno1_signed_D1{x,y}_interior.esm` and `rules/weno5_eno1_signed_norm_D2_extrapolate_bc.esm` -- the reinitialisation picks its upwind side from the frozen smoothed sign `lfn_s0`, not from the wind-relative propagation sign, so the existing rule could not be reused; the new ones take a free name `upwind_sign` and reference no `wind_u`/`wind_v`. They also carry the SIGNED band test (B19). Lint: the three files produce no finding beyond the repo-wide L008 version skew that all 601 library entries currently have. REFERENCE: newly generated -- `Advance_ls_reinit` had no ESM_DUMP hook, so one was added (fork `ctessum-claude/fire_behavior@66a800e`, verified inert: the rebuilt binary reproduces `esm_dump_ls_tend_58` bit-for-bit) and test7 rerun at both pseudo-time coefficients (`dumps/fire/run_reinit`, `run_reinit2`). THE FROZEN SIGN IS PROVED LOAD-BEARING, but only in the second regime: at the shipped `reinit_pseudot_coef = 1e-4` the field moves 1.9 mm between Runge-Kutta stages, so recomputing S from the current field changes it by at most 5.6e-5 relative and NO assertion can tell -- substituting `psi_ref := psi` leaves that test green, and the description says so instead of claiming otherwise. At the namelist default 1e-2 the same substitution fails 4 assertions. Not transcribed: the outer iteration loop, the final `min(lfn_s3, lfn_in)` clamp, and `Reinit_level_set_fast_dist`. New Fortran findings: B16, N77-N79 |
| Fire → atmosphere heat/moisture flux | `physics/fire_model_mod.F90`, `fire_driver_mod.F90` | `wildland_fire/level_set/fire_heat_flux.esm` (MATCH) | PR EarthSciModels #22 (fire_heat_flux tests) + #23 (flame length, fuel fraction); tests added to a copy of `fire_heat_flux.esm` (PR-ready per PLAN 1.3, 22/22, identical algebra); `flame_length_wrffire.esm` 8/8; `fuel_fraction_cell.esm` 12/12; `fire_flux_to_atmosphere_wrffire.esm` (Provide_atm_feedback aggregation + Fire_tendency as −(1/ρ) D(F, lev) on `column_nonuniform_1d`, mass-weighted) 54/54 |
| Atmosphere → fire wind | `physics/fire_driver_mod.F90` | `wildland_fire/midflame_wind.esm` (form matches for opt 1; reduction factor differs) | physics (2026-09-06): `components/wildland_fire/fire_behavior/fire_wind_wrffire.esm` (`Interp_profile` log-profile interpolation incl. z-coupling), 21/21 in 7 regimes (briefly 0/21 on EarthSciAST `main` between commits `556675570` and `3cf6f6f92`: a scalar observed read as a rank-1 field inside the `kstar` `min` reduction, so it answered `inf` and `uf`/`vf` went `NaN` — EarthSciAST #432/#434 plus the older #431/#433, all fixed 2026-09-20) (standalone dumps + Fortran-generated driver regimes); horizontal atm→fire mapping not transcribed; `midflame_wind.esm` shares no equation (only `wind_vinterp_opt = 1` has its form) | **2026-09-21: 21/21 -> 24/24**, and the reason it still cannot be MOUNTED in `couplings/wildlandfire_behavior.esm` is now pinned down and is not the one INVENTORY assumed. The `fire_wind` dump was clamped to the 10 x 10 block at the tile origin, which carries no fire in test7, so every regime was a quiet corner; it is now widened to the whole tile (fork `ctessum-claude/fire_behavior@0c9d87b`, verified inert -- it reproduces `standalone_call10_cell_1_1` exactly), and a new regime `standalone_call10_cell_9_14` tests the atmosphere column that feeds the fire front. THE REAL BLOCKER IS `Interp_horizontal`, NOT the Lambert `Calc_ij`: in the standalone path `Calc_fire_wind` runs per ATMOSPHERE column (19 x 19) and the fire-mesh winds are produced afterwards by a genuine bilinear interpolation -- 5593 distinct `uf` values over the 80 x 80 fire mesh against 361 columns -- so fire cell (18, 64)'s wind is not any single column's; the nearest, (9, 14), differs by 2.2e-3 m/s. Mounting the component therefore needed `Interp_horizontal` transcribed first -- **done 2026-09-21** (row below), and FireWindWRFFire is now MOUNTED in `couplings/wildlandfire_behavior.esm` with a load-bearing edge. The Last Chance coupled run does NOT offer a way round it: its `fire_output` carries `uf`/`vf` over the whole 1200 x 1800 fire mesh, but replaying the interpolation from `wrfout` reproduces them only to a median 0.013 m/s and a maximum 0.058 m/s over 300 burning cells -- the same half-step state offset that bounds the RTHFRTEN check, and about a thousand times looser than this component's own rel 1e-5 tests.
| Atmosphere → fire horizontal mapping | `share/interp_mod.F90` `Interp_horizontal_bilinear` (via `io/coupling_mod.F90` `Interp_horizontal`, `io/wrfdata_mod.F90` `Interp_var2grid`, `hinterp_opt = 2` default) | — | **physics (2026-09-21, revised 2026-09-22): `components/wildland_fire/fire_behavior/fire_wind_hinterp_wrffire.esm`, 16/16.** The last untranscribed link between the atmosphere and the fire mesh, and what unblocked mounting the wind. **Written on the EarthSciAST closed function `interp.bilinear`, NOT on a regridding rule.** A hand-rolled EarthSciDiscretizations entry, `regridding/cartesian_bilinear_regrid.esm` (`method:bilinear`, `_base`/`_offset`/`_apply` stages), was written first and then WITHDRAWN (ESD `966ff14`) once the engine primitive was tested against it: the two agree to within **1e-15 at every one of the 6400 fire cells** for both wind components, give identical full-field residuals against the Fortran, and return the edge value exactly at the clamped corners. `interp.bilinear` clamps the coordinate to the axis range, which for a bilinear interpolant is exactly the Fortran's clamp of base index and offset; it takes a non-constant table and axis and vectorises inside an aggregate. So no ESD PR. The count went 30 -> 16 because the 14 assertions on the rule's internal stages (`i0`, `j0`, `di`, `dj`) had nothing left to test; every remaining assertion is on the output. MEASURED against the Fortran over all 6400 cells: **`uf` 1.084e-7, `vf` 4.353e-7** (1.3e-7 relative against ~3.3 m/s) -- real32 rounding in the reference. (An earlier version of this row said 1.08e-7 for BOTH components; only `uf` had been measured.) The two clamped corners match EXACTLY. **THE FRACTIONAL COORDINATES ARE GEOMETRY, NOT A REFINEMENT RATIO**, and this survives the change of implementation: one expects a fire mesh built by subdividing its parent grid to map affinely, `i_real = (i - 1/2)/sr + 1/2`, but the mapping runs through the inverse Lambert conformal projection and does not. Measured: dumped coordinates 1.08e-7, best least-squares affine fit 1.07e-3, textbook `sr` map 1.19e-3 -- the latter two ~120x the rel 1e-5 tolerance. So `interp.bilinear` is called in SOURCE-INDEX space, with the atmosphere grid's own uniform index axes built from its index sets, and every bit of projection curvature lives in the consumer-supplied `i_real`/`j_real`; `sr_x`/`sr_y` appear nowhere. Reference: the `hinterp` dump hook, fork `ctessum-claude/fire_behavior@22d426e` |
| Fire–atmosphere coupling file | `driver/fire_behavior.F90`, `physics/fire_model_mod.F90` | `couplings/wildlandfire_behavior.esm`, `../wildlandfire.esm` | **stub with tests (2026-09-21): `couplings/wildlandfire_behavior.esm`, 26/26.** **Eleven mounts, 94 coupling edges**, 28/28 (2026-09-21: the mid-flame wind joined, +2 mounts, +10 edges, +2 assertions). The wind is no longer prescribed: FireWindWRFFire interpolates the atmosphere profile of column (5,16) logarithmically to 1 m, FireWindHinterpWRFFire puts that column into the 19 x 19 atmosphere field and interpolates bilinearly onto fire cell (18,64), and the result drives ROSWRFFire. Column (5,16) is the heaviest of the four contributing to that fire cell, weight 0.5584 (then (5,17) 0.3240, (4,16) 0.0744, (4,17) 0.0432). All four wind edges are load-bearing by deletion: `Wind.uf -> Hinterp.u_src_in` and `Wind.vf -> Hinterp.v_src_in` cost **4 assertions each**, `Hinterp.uf_cell -> Ros.uf` and `Hinterp.vf_cell -> Ros.vf` **3 each**: Anderson-13 table -> five-class fuel moisture -> Rothermel -> rate of spread -> the level-set PDE, and in parallel level set -> ignition time -> subcell fuel fraction -> heat and moisture flux. The three tendencies it exposes are d(psi)/dt over the 80x80 mesh, `dfuel_frac_dt` (the fuel burn rate) and `dfmc_g_dt`. Only the model's EXTERNAL inputs are prescribed (fuel category, 2 m forcing, mid-flame wind, terrain slope, front normal, level-set stencil, previous fuel fraction); every quantity one component computes for another travels an edge, verified load-bearing by deletion of three of the nineteen (dropping `Ros.ros -> LevelSet.ros_in` costs 1 assertion, `Moisture.fmc_g_state -> Roth.fmc_g` 6, `FuelFrac.dfuel_frac_dt -> HeatFlux.fuel_burn_rate` 2). Regime: test7 step 20, cell (18,64), Anderson category 2, a heading front at 0.40 m/s, with the fuel-moisture model on (`dumps/fire/run_fmc`). TWO STAGING FACTS, measured: `Calc_ros_params` runs on the moisture BEFORE `Advance_moisture_classes` and `Calc_fire_fluxes` AFTER, so a continuous document cannot match both -- feeding both the STATE matches Rothermel to 2e-8 and the flux to 7.9e-7, inside real32, so nothing is loosened; and `Calc_flame_length` is called before `Calc_fuel_left` with the previous step's `fire_area` and the last RK stage's `ros`, so its two inputs are prescribed rather than coupled and the document says so. FireWindWRFFire is not mounted: the standalone wind instrumentation covers only the 10 x 10 block at the domain origin, which carries no fire. Pure additions this needed: `dt_fire`/`dfuel_frac_dt` on `fuel_fraction_cell.esm` (12/12 unchanged), `dfmc_g_dt`/`fmc_g_state` on `fuel_moisture_wrffire.esm`, `ros_in`/`i_coupled`/`j_coupled` on `level_set_tendency_wrffire.esm` |
| Fire–atmosphere coupling document | heat flux → flux/tendency chain | fire_behavior WRF-side routines replayed on test7/wrf.nc AND on the coupled Last Chance Gulch d05 run | `couplings/` | stub with tests: `couplings/fire_atmosphere_column.esm` 9/9 (2026-09-06, test7/wrf.nc) and **`couplings/fire_atmosphere_column_lastchance.esm` 16/16 (2026-09-21)**. **The "WRF-side coupled run not possible (CFBM needs a Lambert real-data domain, N51)" blocker is RETIRED**: the Last Chance Gulch case ran to completion as a single continuous job (`data/eqwefic/lastchance/runs/full`, WRF 4.8 + CFBM, `ifire = 1` on d05, 300 x 450 x 50 at 100 m, `sr = 4`, 84 fire_output frames, fire ignited 18:05 and burning for 12 h), and the new document replays it offline at 18:10, column (167,69), where GRNHFX peaks at 255 kW/m2. That matters because N52 records test7/wrf.nc was produced by WRF-SFIRE (`ifire = 2`), NOT by the CFBM this repo transcribes. MEASURED: (1) `Calc_fire_fluxes` reproduces the run's own fire-mesh flux from `fuel_frac_burnt_dt`, the Anderson `fgi` and `fmc_g` to **1.1e-7 relative over all 1117 burning fire cells** of the frame, across five live fuel categories; (2) the `Provide_atm_feedback` block mean reproduces the run's own GRNHFX/GRNQFX to **2.1e-7** over the 138 atmosphere cells above 1 kW/m2, which pins the fire-to-atmosphere index mapping; (3) `Fire_tendency`, replayed in Fortran by the new `data/eqwefic/fire_build/lastchance_feedback/lastchance_feedback_driver` on an extracted 7 x 7 x 50 tile, reproduces WRF's own in-run RTHFRTEN column to **4.5e-4 relative** (1.5e2 of 3.3e5 Pa K/s). That residual is the half-step state offset, not a transcription error: RTHFRTEN is formed in RK step 1 from the rho/z_at_w/mut the step STARTS with while wrfout stores the state it ENDS with; the heat and moisture residuals are the same relative number to seven digits (a pure geometry factor, not a flux error), and two independent reconstructions of rho -- the WRF dry EOS and the mass-coordinate identity -(c1h mut + c2h) dnw = rho_d g dz8w -- agree to 1e-5 in the lower twenty layers. The test asserts the column twice, against the Fortran replay at 1e-5 and against WRF's own output at the measured 4.5e-4. `fire_rain` is identically 0 over the whole fire, so this case says nothing about the moisture wetting branch |

### The untested branches of the fire model, and what was decided about each (2026-09-21)

These are the paths that can produce a wrong tendency with nothing going red, ranked by how
likely each is to matter in a realistic run.

| Branch | Status | Reasoning |
|---|---|---|
| Fuel-moisture WETTING (rain) | **CLOSED** | The highest-risk gap: during rain the equilibrium bracket collapses to 2.5 and the time lag changes by an order of magnitude with an exponential rain-rate dependence, so a wrong transcription moves `fmc_g` by O(1) and, through Rothermel, the rate of spread. No reference run has rain, so the reference was generated: `data/eqwefic/fire_build/fmc_rain/fmc_rain_driver.F90`. Four rain tests, all passing at rel 1e-5 on the first run. |
| The EXPONENTIAL update branch (`change >= TOL`) | **CLOSED** | Rode along with the rain work: every dumped test7 step has `change <= 1.4e-4`, so a 600 s moisture step was needed to cross `TOL = 1e-2`. Now covered on both the wetting and the drying side. |
| `fire_upwinding = 7` (pure WENO5) | **CLOSED** | Cheap and worth doing because `case (7)` is `case (9)`'s in-band branch with the band test removed, so no new EarthSciDiscretizations rule was needed -- only `weno_band_cells` past the domain diagonal. Reference: a test7 rerun (`dumps/fire/run_weno5/`). 3480 of the 6400 interior cells differ from the option-9 run, so it is a real check and not a re-run of the same arithmetic. |
| `fire_upwinding` 10 (cosine blending zone) | **CLOSED (2026-09-21)** | Done next because it was the cheapest of the remaining eight: it is option 9 with the hard band edge replaced by a cosine taper over B < \|psi\| <= 2B, so it reuses every stencil the library already had and only needed the blend itself. Four new ESD files, 15 assertions, discriminating at 173 in-zone cells. |
| `fire_upwinding` 8 (WENO3/ENO1) | **CLOSED (2026-09-21)** | The last one worth doing, and the only one of the remaining eight that was not cheap. There was no WENO3 on `cartesian_uniform_2d` at all, and the only WENO3 anywhere in EarthSciDiscretizations is a 1-D WENO-**Z** reconstruction with different algebra (`tau3`, eps = 1e-6) that must not be mixed with the 2-D classical Jiang-Shu idiom, so the pointwise pair was transcribed fresh from `Select_weno3`. Seven new ESD files (4 stencils + 3 rules, PR #41), 31 assertions, 175 of 6400 cells differing from option 9 -- every cell the WENO branch touches, with exactly zero difference elsewhere. Reference `dumps/fire/run_weno3/`. |
| `fire_upwinding` 6 (pure WENO3) | **OPEN, but no longer expensive** | N81: `case (6)` is `case (8)`'s in-band branch with the band test removed, exactly as `case (7)` is to `case (9)`. Now that the WENO3 family exists it needs NO new ESD rule -- only `weno_band_cells` past the domain diagonal and a rerun, the same recipe option 7 used. The earlier claim that it needs its own rule family is superseded. |
| `fire_upwinding` 0, 1, 2, 3, 5 | **LEFT OPEN, deliberately** | First- and second-order component-wise schemes that no shipped configuration selects and that Munoz-Esparza et al. (2018) show are inadequate; each needs its own rule family and its own rerun. The namelist default is 9, both reference runs use it, and the covered set (4, 7, 8, 9, 10) now spans the Godunov Hamiltonian, pure WENO5, the WENO3 hybrid, the hard-edged WENO5 hybrid and the blended one. |
| Level-set REINITIALISATION | **CLOSED (2026-09-21)** | Built as its own component, `level_set_reinit_wrffire.esm`, 45/45, with three new EarthSciDiscretizations library files and a newly added Fortran dump hook. The analysis that kept it open -- it is an operator on the STATE between steps, not a term in d psi/dt -- is unchanged and is now recorded in the component's own description rather than only here; what changed is that its pseudo-time tendency is verified. Measured while doing it: at the coefficient both shipped namelists use, one reinitialisation moves psi by 5.7 mm against a 25 m cell (2.3e-4), so on test7 it is very nearly inert -- but at the namelist_mod default, 100x larger, it is 0.57 m per step. Whether omitting it costs anything in a long integration depends on which of those two a run gets (N79). |
| `tbound` CFL bound | **SKIPPED, and it is right to skip it** | N74: despite the name it is not a CFL REDUCTION. `Prop_level_set` takes the minimum over the three Runge-Kutta stages and then only does `if (dt > tbound) call Stop_simulation('CFL violation')`. It never shortens the step, never sub-cycles, and is derived from the tendency rather than entering it, so it cannot affect an instantaneous derivative by construction. What it encodes is a stability guard on the INTEGRATOR, which belongs to whatever time scheme a consumer wraps the tendency in -- not to the component. |


## Dynamics (stage 3)

| Component | Content | Fortran source | Where | Status |
|---|---|---|---|---|
| `atmospheric_dynamics/wrf_arw/*` | flux-form Euler equations, hybrid sigma-pressure coordinate, base-state split, diagnostic pressure | `dyn_em/module_big_step_utilities_em.F`, `module_small_step_em.F`, Skamarock et al. 2021 Ch. 2 | EarthSciModels | physics, SCM subset (2026-09-15): `components/atmospheric_dynamics/wrf_arw/` — `rayleigh_damping` (20), `perturbation_coriolis` (15), `earth_curvature` (18), `vertical_momentum` (12), `column_geometry` (120) + `column_staggering` template library; 185/185 `./esm test`, real32 dumps from fork commit 83347b2 (`dyn_column`), residuals ≤1e-10 m/s² except where the reference is quantized (curvature u, v: 5e-11) or rounding-limited (p′: 0.016 Pa). The layer-to-interface p′ gradient is written with neighbour templates, pending an EarthSciDiscretizations rule. Flux-form 3-D equations not started. `column_geometry` re-factored onto `lib/wrf_thermo.esm` 0D templates and gained `z_w_agl` (2026-09-15). |
| Subassembly: SCM column dynamics | Geo → {Ray, Cor, Cur, Vm} (z_w, fnm/fnp, p′), total non-physics tendency of u, v, w, θm, φ | `dyn_column` + rad dumps, steps 1410/900 | `couplings/` | stub with tests (2026-09-15): `couplings/scm_dynamics_column{,_night}.esm`, 2 × 8/8; 5 mounts, 31 edges; total dw/dt bounded at 1e-3 (N67); physics suite not co-mounted (`[toplevel_model_ref_unresolved]`: a multi-model document cannot be mounted as a unit) |
| Subassembly: geometry-driven physics | WRFColumnGeometry → YSU, Dudhia SW, RRTM column and heating (via `<x>_in`), and → sfclayrev and slab (lowest-layer indexing); WRFSolarGeometry → Dudhia | geometry dumps (fork 83347b2) + suite dumps, steps 1410 and 900 | `couplings/` | stub with tests (2026-09-15): `geometry_radiation_column{,_night}.esm` 2 × 14/14, `geometry_surface_pbl_column{,_night}.esm` 2 × 41/41 |
| Subassembly: SCM column dynamics as a STATE document | D(u), D(v), D(w), D(φ′), D(θm), D(qv) over the five WRF-ARW dynamics components, with the ODE state fed back into geometry, vertical momentum, Coriolis, curvature and the sponge so heights, eta weights, the equation of state and the sponge reference profiles are recomputed every RHS evaluation | `dyn_column` step 1410 (fork 83347b2) | `couplings/` | stub with tests (2026-09-17): `couplings/eqweather_scm_dynamics.esm`, 15/15; 45 edges, 31 equations; uses the bare `wrf.g` mount name again since EarthSciAST #408 -> #410 (merged 2026-09-19) taught assertion `reference` scope to resolve it, so the model-qualified deviation from CLAUDE.md is retired; 14 additive coupling-target parameters on the five components lowered by `couplings/tests/scmstate_couple_*_inputs.esm`; base-state fields stay on the dumped library (time-invariant in this run). Closing the loop changes no bit of the RHS (same references and tolerances as the frozen-column document, plus dφ/dt = g w to 1e-12). Cost ≈0.27 s wall per simulated second. **Physics half co-mounted 2026-09-20** — see the row below |
| Subassembly: EqWeather-SCM STATE document, BOTH halves | D(u), D(v), D(w), D(φ′), D(θm), D(qv), D(qc), D(qr), D(qi), D(qs), D(qg), D(T_s), D(T_sk) — **thirteen** states over **twenty-four** co-mounted components (the five WRF-ARW dynamics components, SfclayRev, slab, YSU, cal_cldfra, the four RRTM LW stages, Dudhia SW, the θm conversion, and the eight WSM6 stages), with every tendency a SUM: D(u)/D(v) = Coriolis + curvature + sponge + RUBLTEN/RVBLTEN, D(θm) = sponge + the moist-theta conversion of the radiative, PBL **and microphysical** tendencies, D(qv)/D(qc)/D(qi) = YSU + WSM6, D(qr)/D(qs)/D(qg) = WSM6 alone (YSU does not diffuse the precipitating species), D(T_s)/D(T_sk) = the slab's | merged step-1410 / call-2820 and step-60 / call-120 dumps (`dyn_column` + `subassembly_{all,rad,pbl,mp}` + the sfclayrev/slab/ysu kernel dumps) | `couplings/` | stub with tests (2026-09-20, extended 2026-09-21): `couplings/eqweather_scm.esm`, **FOUR regimes, 65 clear-sky (step 1410) + 69 cloudy (step 60) + 72 deep night (step 900) + 72 dusk (step 300) = 278 assertions**; 339 coupling edges, 313 variables, 182 equations, 24 mounts, 656 ODE states. The two night regimes were added 2026-09-21 because the two originals were both sunlit and both convective, so they could not separate a state-dependent tendency error from a constant one and never entered YSU's stable branch or the longwave-only radiation path; step 900 is cos zenith −0.552, Ri_b +0.037, surface-layer regime 1, h = 262 m over 6 layers with K_h max 2.20 m²/s against 176.8 at step 1410, and step 300 is just after sunset (cos zenith −0.088) with the most exercised cloud fraction of the four (cal_cldfra returns 0.0272/0.0373/0.0670/0.1356 and 1.0, so the Randall fit and not only its snap is evaluated in a coupled document). `gsw` and `dthdt_sw` are asserted at **abs 0** in both and are exactly zero on both sides. The step-300 dumps were merged for the first time (`esm_dump_{dyn,scm}_suite_300.json`); the numbers are transcribed by `tools/add_scm_regime.py`, whose expression table re-derives BOTH pre-existing tests as a check. WSM6 entered 2026-09-20 and with it `q_t` stopped being frozen: with all six moisture species as states the document computes q_t = qv+qc+qr+qi+qs+qg and feeds it to the column geometry (the full column mass of p_hyd) and the vertical-momentum buoyancy term, through a new `q_t_in` coupling target on both and the mount-edge library `tests/scmstate_couple_qt_inputs.esm`; verified bit-identical to the frozen step-1410 profile, which WAS exactly qv+qc+qi. Mounting WSM6 needed three component changes, all pure additions (790/790 unchanged): each stage that is an in-place adjustment now exposes its tendency as increment/dtcld — six observeds in `melting_freezing.esm`, five in `mass_conservation.esm`, three in `saturation_adjustment.esm`; the five stages with a nested `sd`/`sat` subsystem gained a bindable `NLEV` so they can sit in a 59-layer column; and `sedimentation.esm`'s two ESD grid imports were hard-bound to `NLEV: 40` and now follow the metaparameter. dtcld is NOT the integrator's step: WSM6 takes ndt = max(nint(dt/dtcldcr),1) = 1 sub-step at dt = 60 s, so mp_dtcld0 = 60 s is a parameter of the tendency itself. **THE CLOUDY TEST (step 60 / call 120) is where microphysics is actually exercised**: cloud ice to 4.64e-5 kg/kg in layers 50-56, cal_cldfra = 1.0 in seven of them, OLR 129.7 W/m² against 256.0 at step 1410. It is ICE-ONLY (q_c = q_r = q_s = q_g ≡ 0; the em_scm_xy sounding never produces them), so it covers the deposition/sublimation/nucleation/aggregation/fallout branch and pins the rest at zero; liquid and mixed phase stay with `microphysics_column{,_mixed}.esm` on the supercell column. **The three microphysical tendencies DO NOT agree with WRF and their tolerances record that rather than certify it**: mp_dqv_dt residual 1.15e-8 against a reference maximum of 3.30e-8, mp_dqi_dt 2.81e-8 against 2.17e-8, mp_dtheta_dt 4.32e-5 K/s against 1.24e-4. **2026-09-21: the cause is now MEASURED, per stage, and is not a defect.** (i) The whole-scheme increment has no dt→0 limit, because `pigen = max(0, (roqi0/den − q_i)/dtcld)` projects q_i onto the diagnosed equilibrium ice content in ONE sub-step — its increment is 7.78e-8 kg/kg at dtcld = 0.01, 0.02 and 0.04 s alike, so its "rate" is exactly ∝ 1/dtcld (FORTRAN_BUGS N3). The saturation adjustment is identically ZERO in this column, so `pigen`, not `pcond`, is the obstruction. At the model's own dtcld = 60 s `pigen` is only 3.7 % of the total, so the ESM (which prescribes the same dtcld) IS comparable. (ii) What is left is the sequencing. Replaying the kernel at dtcld = 60 s from a synthetic post-sedimentation state (`data/eqwefic/dumps/wsm6/syn_postsed_120.flat`) moves `pidep` from 3.29972e-8 to 4.45250e-8, so R(x1) − R(x0) = **1.145e-8 kg/kg/s** — *exactly* mp_dqv_dt's residual to four significant figures, and 91 % of mp_dtheta_dt's once carried through xl/cpm/pi. The backward estimate 2R(x1) − R(x2) = 2.1469e-8 matches the directly measured R(x0) = 2.15477e-8 to 0.4 %, so the sensitivity is linear and the estimate is sound. (iii) mp_dqi_dt is the exception: it also carries the fallout stage, where this document's donor-cell flux divergence gives 5.304e-8 against WRF's own PLM increment 3.637e-8 at dtcld = 60 s (1.46x; 1.52x against the dtcld→0 fallout 3.4935e-8). The earlier claim that "sedimentation alone is 2.4x WRF's whole ice tendency" compared ONE process against the SUM of all of them and is withdrawn. Closing that 1.46x needs a PLM-equivalent ESD flux rule; it is the ONLY assertion in this document whose residual exceeds its own reference amplitude for a model-side reason. Everything else at step 60 lands inside the step-1410 tolerances unchanged; only pbl_dqidt, cldfra (the real32 Randall cancellation, live for the first time), lsm_dTs_dt and lsm_hfx moved. **COST: 7 m 58 s for both tests** on the EarthSciAST PR #439 binary (3 m 47 s for the clear-sky test alone, up from 2 m 31 s before WSM6). On `main`, which still builds an implicit solver for a run that cannot advance (**#438**), the state count rose 479 → 656 so the same document now costs 2·656+1 = 1313 full RHS evaluations per run instead of 959; a run was killed unfinished at 15 minutes. Merging #439 is now the difference between a 4-minute test and a multi-hour one. Re-time this row and PLAN.md 3.1's acceptance question when it merges |
| Subassembly: diurnal solar clock | `xtime = xtime0 + t/60`, `julian = julian0 + t/86400` driving `WRFSolarGeometry` over one 58 h integration | radconst/calc_coszen dumps, 13 WRF steps incl. 3 sunrise/sunset crossings | `couplings/` | stub with tests (2026-09-17): `couplings/solar_diurnal.esm`, 26/26 at the component's own tolerances, not loosened. With tolerances at abs 0, 20 of 26 agree to <1e-6 relative; the 6 crossings' largest residual is 8.3e-7, i.e. WRF's REAL*4 coszen (N69), not the clock. Carried a TOA-insolation integral because the inline runner could not integrate an algebraic-only model (PLAN 3.1 gap (w)); that gap is FIXED upstream (EarthSciAST #406 -> #412, merged 2026-09-19), so the integral is now optional and could be dropped |
| use_theta_m tendency conversion | physics dθ/dt, dqv/dt → dθm/dt | `dyn_em/module_big_step_utilities_em.F:6674-6707` | — | physics (2026-09-15): `components/atmospheric_dynamics/wrf_arw/moist_theta_tendency.esm`, 28/28 `./esm test`, 7 em_scm_xy steps from the `theta_m_conv` hook (fork 1b4b556); all 59 layers converted (k_end = kde); float64 replay ≤ 7e-11 K/s; conversion effect up to 5.8e-4 K/s. **Wired 2026-09-17** into `couplings/scm_physics_column.esm` (50 → 52), driven by that assembly's own `rth_phys_sum` and YSU `rqvblten`; the assembly now exposes `dtheta_m_dt_phys`, the quantity D(θm) must take (the physics suite's own output is a DRY θ tendency). `scm_physics_column_night.esm` not yet wired. |
| `grids/wrf_arw_c/*` | C-grid mass-coordinate grid, 5th/3rd-order upwind advection with PD limiter, divergence damping, metric terms | `dyn_em/module_advect_em.F`, Ch. 3 | EarthSciDiscretizations | not started |

## Pairwise coupling libraries (`couplings/lib/`)

Adopted 2026-09-15 (strategy (e) in `data/eqwefic/design/assembly_composition.md`): each
interacting pair of schemes has its wiring authored once as an importable coupling library
(esm-spec §10.9, `coupling_roles` + `coupling_import`), and the assemblies that contain the
pair import it instead of repeating the edges. This is the compatibility matrix WRF keeps in
`module_check_a_mundo.F`: a row exists only for a pair that has actually been wired and tested.
Swapping a scheme changes one mount and the imports naming it.

| Library | Roles | Edges | Wires | Tested by |
|---|---|---|---|---|
| `sfclayrev_ysu.esm` | Sfc, Pbl | 8 | sfclayrev → YSU: `ust`, `br`, `wspd`, `u10`, `v10`, `znt`, and `fm`/`fh` → YSU's `psim`/`psih` (the profile functions, WRF passes FM as PSIM) | `surface_pbl_column`, `physics_column{,_night}`, `geometry_surface_pbl_column{,_night}`, `scm_physics_column{,_night}` |
| `sfclayrev_slab.esm` | Sfc, Lsm | 2 | sfclayrev → slab: `flhc`, `flqc`. Scheme-specific — Noah/Noah-MP take a different set and get their own library | `surface_soil_column`, `geometry_surface_pbl_column{,_night}`, `scm_physics_column{,_night}` |
| `slab_ysu.esm` | Lsm, Pbl | 2 | slab → YSU: `hfx`, `qfx`. WRF's sfclayrev → slab → YSU order makes these the values YSU sees | `geometry_surface_pbl_column{,_night}`, `scm_physics_column{,_night}` |
| `dudhia_slab.esm` | Sw, Lsm | 1 | Dudhia → slab: `gsw` (net surface shortwave) | `scm_physics_column{,_night}` |
| `rrtm_lw_slab.esm` | Heat, Lsm | 1 | RRTM → slab: `glw` (downward surface longwave) | `scm_physics_column{,_night}` |
| `rrtm_lw_chain.esm` | Col, Setcoef, GasOpt, Rtrn | 35 | the RRTM longwave internal chain: MM5ATM column → SETCOEF → gas optics → RTRN. An internal chain, not a scheme pair: its external surface is the same however the stages are split, which is what makes bundling it safe | `rrtm_lw_column{,_call1,_call240,_call1440,_call2400}`, `radiation_column{,_night}`, `physics_column{,_night}`, `scm_physics_column{,_night}` |

Not yet factored: the column-state ↔ scheme edges (`ScmPhysicsColumn` ↔ Sfc 52, ↔ Lsm 30,
↔ Pbl 16, ↔ RRTM 12, ↔ Sw 8). These pin a naming convention for the state carrier rather than
wiring two schemes, so they are a second family of libraries (`column_state_<scheme>`).

Verification: a library's edges are proved load-bearing by deleting one and confirming the
importing assembly's Fortran-dump tests fail — `esm diff` and `esm coupling-analysis` compare
documents, not flattened forms, so neither can show that an import and the edges it expands to
are equivalent.
