This repo is an implementation of the WRF-(Chem) https://github.com/wrf-model/wrf and WRF-SFIRE (https://github.com/openwfm/WRF-SFIRE) models in .esm files. (Decided 2026-09-04: the fire code is NCAR's Community Fire Behavior Model, https://github.com/NCAR/fire_behavior, the `phys/fire_behavior` submodule of WRF 4.8 and the more recent, actively developed successor of the WRF-Fire/SFIRE line; WRF-SFIRE-only physics are optional later additions. See PLAN.md §1.)

WRF-... models are made up of process components that are composed together into the large-scale model. Implementation will take place in three stages.

In the first stage, the original fortran codes will be instrumented to output inputs and outputs from each model component during simulations. Outputs can either be instantaneous derivatives (preferred) or integrated trajectories over a time period. The code will also be instrumented to output inputs and outputs of subassemblies of components as appropriate. A select subset of these inputs and outputs will be used as tests in .esm stub files for each component or subassembly which will be added as pull requests to https://github.com/EarthSciML/EarthSciModels.

In stage 2, The stub .esm files will be filled in with the actual physics of each process so that the tests pass, then each PR will be reviewed by a human and merged. Then, the indvidual .esm components will be imported by reference into the subassembly .esm files and coupled together so that the subassembly .esm tests pass.

In stage 3, the components that have been merged into EarthSciModels will be imported by reference and coupled into top-level EqWeather.esm, EqAtmChem.esm, and EqAtmFire.esm models and tested against simulations of the full fortran models.

All model logic should be contained in .esm files. Tests and examples should be in the "tests" and "analysis" sections of .esm files, respectively. All local tests should be conducted using the EarthSciAST rust CLI binary, a copy of which should be kept untracked in the root directory of this repo.

The .esm files should be authored compositionally, with separate components, subcomponents, and expression templates used liberally and imported by reference into other components to keep the .esm code succint, simple, and human-interpretable. Avoid repeating logic or calculations, instead factor reused pieces into their own files/components/expression templates and import them by reference. Avoid the use of scripts to mechanically generate .esm files, as they often end up producing expressios that are not properly factored or succinct. (Scripts MAY be used to extract numeric test tuples from Fortran dumps into an existing hand-authored `tests` block, and to translate tabular data such as KPP `.eqn` reaction lists or lookup tables into `reaction_systems`, `function_tables`, or `data_sources` entries; the equations, structure, and factoring of a component are always authored by hand.)

Always, the equations in the .esm files should like like math rather than einsum operations whenever possible. For example, if a component includes a aggregate operation that is a numerical representation of a mathematical integral, the .esm equivalent should be written as an integral instead of a aggregate operation. Likewise, PDEs that are discretized in the fortran could should be written as PDEs, with discretization templates either used from or submitted as pull requests to https://github.com/EarthSciML/EarthSciDiscretizations.

Some components may already have implementations in EarthSciModels. If so, add additional tests from the instrumented FORTRAN to ensure the existing .esm implementations match their fortran counterparts.

Keep a centralized list of any bugs or errors that are found in the fortran code.

Do not store anything large in /tmp, as it is backed by RAM rather than hard drive and can cause an OOM.

Do not put "Co-Authored-By: Claude ..." in any commit messages. Claude is an LLM and LLMs cannot take responsibility for outputs, therefore they cannot be authors.

## Working conventions (added 2026-09-04 after the de-risking review; see PLAN.md for details)

- **Plan and inventory.** `PLAN.md` is the implementation plan. `INVENTORY.md` tracks every WRF scheme and sub-process with its stage status (`not started` / `instrumented` / `stub PR` / `physics PR` / `merged`). Update it whenever a component changes state. `FORTRAN_BUGS.md` is the centralized list of Fortran bugs required above.
- **Sibling repos.** Sibling checkouts in `../` are the working copies: `../EarthSciAST` (spec + Rust CLI source), `../EarthSciModels`, `../EarthSciDiscretizations`, `../WRF` (v4.8, shallow clone with `phys/physics_mmm`, `phys/noahmp`, and `phys/fire_behavior` submodules; remote `fork` = `ctessum-claude/WRF`, branch `earthsciml-instrumented`), `../WRF-SFIRE` (reference only). `.esm` `ref`s in this repo point at these sibling paths (relative), matching the convention used by `../simpleclimate.esm` and `../wildlandfire.esm`.
- **Committing from a shared checkout: use `git commit -- <explicit paths>`, not `git add` then `git commit`** (decided 2026-09-23, after three agents working in this checkout swept each other's work into three separate commits). The index is shared by every agent in the checkout, so ANY content another agent has staged is committed by whoever runs `git commit` next, no matter which paths that agent names on the `add`. `git commit -- <paths>` takes the WORKING-TREE version of exactly those paths, ignores the rest of the index, and leaves it staged — there is no window. Verified both halves of that behaviour on 2026-09-23 in a scratch repo. **The caveat, which matters for `CLAUDE.md`, `PLAN.md`, `FORTRAN_BUGS.md` and `INVENTORY.md`:** because it takes the working tree, `git commit -- <shared file>` also commits another agent's *unstaged* in-flight edits to that same file, and it ignores a hunk you isolated with `git apply --cached`. There is no mechanism that fully protects a file two agents are editing at once. So on those four files, check `git diff` first, keep the edit small and commit it immediately, and if a sweep happens anyway, leave it: the content is intact and only the attribution is wrong, which is not worth rewriting shared history over. Numbering in `FORTRAN_BUGS.md` collides the same way — three agents numbering concurrently on 2026-09-23 produced a duplicate and a skipped number — so re-read the file immediately before claiming an N or B number.
- **EarthSciModels commit trailers.** That repo's own `CLAUDE.md` (upstream commit b7912bc) forbids `Co-Authored-By: Claude ...` trailers, so commits on the `eqwefic/*` branches must not carry one (keep `Claude-Session:`). The same rule applies to commits in this repo (see the top of this file). Branches opened before 2026-09-07 still carry the trailer and should have it dropped the next time they are touched.
- **Fortran instrumentation lives outside this repo.** Kernel drivers and instrumented Fortran go in the forks `ctessum-claude/WRF` and `ctessum-claude/fire_behavior` (branch `earthsciml-instrumented`), not here. Push to those forks with `git -c credential.helper= -c 'credential.helper=!gh auth git-credential' push fork <branch>` over an `https://ctessum-claude@github.com/...` remote URL, because `~/.gitconfig` rewrites plain https GitHub URLs to ssh under the `ctessum` account. This repo holds only `.esm` files, the plan/inventory docs, and small extraction scripts under `tools/`. Large Fortran dumps go under `/projects/illinois/eng/cee/ctessum/ctessum/data/eqwefic/` (disk-backed), never in git and never in `/tmp`.
- **Test shape.** A component test is an esm §6.6 inline test. Inputs are `parameter_overrides` / `initial_conditions`; expected outputs are assertions on *observed* variables at `time: 0.0` (this is the "instantaneous derivative" form: tendencies are observeds, so asserting them at t=0 evaluates the RHS at the given state). Use a short `time_span` (e.g. `{start: 0, end: 1}`) for algebraic components, and an EMPTY one (`{start: 0, end: 0}`) when the document has ODE states but every assertion is at t = 0: esm-spec §6.6.2 names that shape, and the Rust runner answers it from the initial state without integrating anything, which on a large coupled document is the difference between one right-hand-side evaluation and a Jacobian over every state. Only use trajectory assertions when the Fortran component is itself an in-place time step that cannot be exposed as a rate. For Fortran schemes that do an implicit solve (e.g. PBL tridiagonal diffusion), generate the reference tendency with a small `dt` (≈0.01 s) so the implicit tendency approximates the instantaneous derivative, and say so in the test `description`.
- **A component does not own state a consumer will also drive.** Write the consumer-supplied column as a plain algebraic read (`x = input_x`) and expose the tendency as an *observed*; do not write `ic(x) = input_x` with `D(x) = dxdt`. A component that carries its own copy of the state binds only the INITIAL condition when a coupling document lowers `input_x` to a coupling-target parameter: it then integrates that copy under its own tendency alone while the document integrates the shared state under the sum of every scheme's, and the two diverge silently over a long run. YSU (theta, u, v, qv, qc, qi), Dudhia SW (T) and RRTM LW heating (T) were written the first way and converted on 2026-09-20; no count moved, because every assertion is at `time: 0.0` on an observed either way. The heating case shows the second cost: nothing in that component ever READ its `T`, so the only thing the ownership did was add 59 spurious ODE states to every document that mounted it. A component with no structural equation at all is fine — EarthSciAST #412 added the `static_evaluation_assertions` conformance category and Rust, Julia and Python all evaluate such a document once instead of integrating it.
- **An operator-split scheme joins an ODE document by exposing `increment / dt_substep` as an observed.** WSM6 is not a rate: it applies in-place increments over its own sub-step (melting/freezing, the budget rescaling, the saturation adjustment), and there is no rate hiding inside it to extract. The way in is to add, beside the existing `<x>_upd`/`<x>_out` state variables, an observed `d<x>_dt = (<x>_out - <x>_in)/dtcld` — which is exactly the tendency a consumer integrating the column must add, converges to the continuous process rates as the sub-step shrinks, and carries the scheme's own limiters at finite sub-step. It is a pure addition: the `_out` equations are untouched, so no existing assertion moves (790/790 for WSM6). Two things must be said out loud in the mounting document. The sub-step is a **parameter of the tendency, not the integrator's step** — prescribe it in the document and couple it into every stage, because a parent test cannot set a mounted component's parameter. And the result differs from the Fortran by O(dt_substep), because the Fortran stages its processes sequentially (each sees the state the previous one left) while a continuous formulation evaluates them all at one instantaneous state and sums; measure that difference, do not assume it is small. Done 2026-09-20 for `melting_freezing.esm`, `mass_conservation.esm` and `saturation_adjustment.esm`.
- **A component with no `index_sets` of its own still needs a bindable metaparameter.** Five WSM6 stages take `lev` entirely from a nested `sd`/`sat` subsystem and declared no `metaparameters` block, so a mount-edge `bindings` had nothing to bind and the leaf's default size won — `subsystem_index_set_conflict` the moment such a stage is mounted beside a column of a different size. Give the component an `NLEV` metaparameter whose only job is to be bound at the mount edge and passed down (`"sd": {"ref": ..., "bindings": {"NLEV": "NLEV"}}`). And check the `${ESD_ROOT}` grid imports at the same time: `sedimentation.esm` had `"bindings": {"NLEV": 40}` hard-coded on both of its ESD rule imports, which pins the grid regardless of the mount.
- **Column components.** Column physics is written over a vertical index set (`lev`, cell-based, N cells with N+1 edges) with consumer-supplied geometry (heights/pressures as shaped parameters), following the EarthSciDiscretizations convention that integer counts are metaparameters and real geometry is supplied by the consumer. Vertical diffusion, vertical flux divergence, and column integrals are written as continuous operators (`D` wrt `lev`, `integral`) and lowered by rules imported from EarthSciDiscretizations; missing rules are added there by PR first. Discrete recurrences (esm §4.3.1.1) are reserved for genuinely sequential sweeps (e.g. two-stream radiative transfer up/down passes) and must be justified in the component description.
- **A Fortran loop that reads the cell it wrote last iteration is usually NOT a recurrence, and the test is cheap** (three cases on 2026-09-23, each of which changed what got written; run all three before accepting a sweep). **(a) Unroll it.** If the terms turn out to be the ORIGINAL values rather than the running answer, it is a prefix reduction — New Tiedtke's `cuinin` writes `s[k] ← max(s[k], s[k+1])` descending, which unrolls to `max over j ≥ k of the original s[j]`. Then check the operator is exactly associative before reassociating (`max` is; floating-point `+` is not, and §4.3.1 cares). **(b) Rearrange it.** A sweep whose coefficients look like `(1−c)/(1+c)` is an ODE integrator, not a loop: `cutypen`'s plume ascent rearranges exactly into the trapezoidal rule for `dh/dz = λ(h_env − h)`, which the routine's own comment already writes down, so it belongs in a lowering rule (ESD PR #45) and not in a recurrence. **(c) Look for a conserved sum.** *A cycle between two quantities whose sum the coupling conserves breaks by carrying the sum and a partition of it.* `cuflxn`'s rain and snow fluxes each read both at the level below and look like a two-variable cycle that §4.3.1.1 forbids at any lag — but melting only moves mass between them, so carrying the total and the rain fraction gives two single-variable recurrences in a DAG (a read of another variable at the SAME cell is not a cycle), verified bit-exact over 475 levels. Only when all three fail is it a real recurrence: the honest example is GWDO's wave-saturation sweep, where the amplitude at an interface is reconstructed from the stress the level below passed up. Say in the description which test failed and why, in both directions — a declined recurrence and an accepted one are both worth the sentence.
- **Constants.** Every WRF constant comes from `lib/wrf_constants.esm`, mounted as a subsystem (`"subsystems": {"wrf": {"ref": "../../lib/wrf_constants.esm"}}`) and referenced by the bare mount name (`"wrf.cp"`) in both equations and test assertions (decided 2026-09-15). Do not use the model-qualified form (`"<Model>.wrf.cp"`): it is not rewritten when the component is mounted under a different key, so a component written that way can only be mounted under its own model name. It is (transcribed from `share/module_model_constants.F`, plus per-scheme parameter files where a scheme redefines a constant). Never write a physical constant inline in a component. **A conversion factor carries the units of the conversion, never `"1"`** — `degrad` is `rad/deg`, not dimensionless — even when the Fortran it is transcribed from carries no units. Since EarthSciAST #413 (2026-09-19) the transcendentals apply the declared *scale* of their argument, so a `deg`-typed expression handed to `sin`/`cos` is converted again: declaring the factor dimensionless silently double-converts every angle built with it (this broke 17 assertions in the solar geometry; see PLAN.md §3.1). Hand a trig function either a quantity declared `deg` (unscaled) or one declared `rad`.
- **Recapturing a dump invalidates everything derived from it, and the failure does not look like staleness.** When a reference run is re-run -- a Fortran bug fixed, a namelist changed -- the JSON dumps are rewritten in place, but the `.flat` driver inputs, the `o_*` replays and the filled test values derived from them are NOT. A replay of a stale flat against a fresh dump differences two different column states, and what surfaces is a small, *dt-independent* offset in one quantity that looks exactly like a defect in whatever was rebuilt most recently. It cost a false bug report against an innocent kernel-driver splice on 2026-09-22: the B20 recapture rewrote `dumps/chem170_wrf` at 19:58 while the flats dated from 14:30, 45 `noah` inputs at that step had moved by up to 1e-3, and the resulting `cmc` difference was the reported +1.5e-9. **So: after any recapture, regenerate every flat and replay from the new dumps and refill every test that cites them, in the same session**, and check `grep -rl <dump-dir> components --include=*.esm` for who else cites that directory. **That grep is necessary and not sufficient, because it only finds consumers that already exist** (learned 2026-09-23): the 17:04 recapture of `dumps/chem170_wrf` left the New Tiedtke replays under `chem170_scm/replay/` stale for a day, and no `.esm` cited them because Tiedtke had not reached stage 2 yet. The stale replays put `pt_out` out by 0.32 K, moved a cloud base three kernel levels and changed the convection type at three steps — all of which would have been transcribed as physics by whoever picked the scheme up. So also list the derived artifacts in the data directory itself (`find <data-dir> -newer <a file from the old capture>` or just compare mtimes against the dump directory) and regenerate every one, whether or not anything cites it yet. A stage-1 reference nobody has consumed is exactly the one that will be trusted blindly later. A test can be green, self-consistent and still pinned to a reference state that no longer exists. The regeneration recipe for the chem170 set is in `data/eqwefic/notes/chem170_scm_reference_run.md`.
- **Tape a stage at the routine's OWN exit, not at the next stage's entry — the stage boundary is often not the routine boundary** (N113, 2026-09-23). New Tiedtke's `cuflxn` computes a rain flux, and then `cumastrn` rescales the downdraught mass flux, adds the remainder back into the precipitation and *recomputes* the generation term — deliberately and conservatively, with nothing in `cuflxn`'s argument list to say so. A reconstruction validated against the later dump was off by up to 0.31 of the flux amplitude on the columns whose downdraught had been rescaled and **bit-exact on the rest**, which reads as a bug in the reconstruction and is not: it is a comparison against a state that has moved on, the same family as the stale-dump trap above. Against a tape at the routine's own exit the same reformulation was exact to 4.3e-16. **The diagnostic signature is worth memorising: exact on some columns and badly wrong on others, split by whether some later stage touched them.** When that appears, suspect the reference before the transcription, and add the earlier tape rather than tuning the component.
- **Tolerances.** Reference values from single-precision WRF builds use `rel: 1e-5`; from double-precision kernel builds (`kind_phys` = real64, preferred when the harness allows it) use `rel: 1e-9`. State which build produced each test's values in its `description`.
- **An `abs`-only tolerance DOES NOT BIND: always write `"rel": 0.0` beside it** (found 2026-09-23, verified independently by me the same day). The runner ORs the two bounds and supplies a **default `rel` of ~1e-6** when none is given, so `{"abs": 1e-14}` is satisfied by anything within 1e-6 relative, and **`{"abs": 0.0}` is not zero tolerance**. Probe, in the scratchpad: one assertion perturbed to 5e-7 *relative* from its expectation **passes** `{"abs": 1e-12}` and **fails** `{"abs": 1e-12, "rel": 0.0}`. This bites hardest exactly where an absolute bound is the right instrument — a quantity spanning orders inside one column, where the small cells are the ones the default silently stops checking (a per-layer soil tendency running 1.7e-11 to 4.0e-6 /s was bounded by the default, not by the number written). Any claim of "exact", "bitwise" or "zero tolerance" resting on a bare `{"abs": 0.0}` is unenforced. `components/land_surface` and the five frozen-column couplings were repaired on 2026-09-23 and still pass, so those claims were true but untested; **repo-wide there were 3049 `abs`-only assertions and 2256 carrying neither bound**, and each directory needs its own re-run when repaired, because a claim may only ever have been true at 1e-6.
- **Do not merge red tests.** EarthSciModels CI runs every inline test on every push and the test schema has no skip/xfail marker. Stage-1 stub files therefore live on a branch of this repo (`components/` mirrored to the EarthSciModels layout) and are opened as EarthSciModels PRs only once their tests pass in stage 2. Stage-1 PRs to EarthSciModels are limited to additional tests for components that already exist there.
- **Coupled assemblies live in `couplings/`** (EarthSciModels' directory for coupled documents), not under `components/`, and are run as `./esm test couplings/<file>.esm` or over the whole directory. The `--model <AssemblyModel>` selector this rule used to require is obsolete since EarthSciAST #214 (2026-09-08): a mounted component's inline tests no longer run under the coupling, so a whole-directory run no longer goes red.
- **Local test command.** `./esm test <file-or-dir>` (Rust CLI). The current binary was built 2026-09-21 from EarthSciAST `main` at `be1fafe6d`, which carries **#439** (the fix for #438) and **#440**. **Do NOT rebuild it (decided 2026-09-23): EarthSciAST `main` is not expected to work for this repo at the moment, so stay on the `be1fafe6d` binary until the user says otherwise.** If something appears to need a newer binding — a spec field the CLI drops, a rule that will not load — report that as a finding rather than rebuilding to make it pass; "ESD/the spec is ready and we are not" is a different situation from "the rules moved", and which one it is matters. Check `./esm`'s mtime against `../EarthSciAST/pkg/earthsci-ast-rs/target/release/esm` before timing anything, and say which binary produced any number you report.
- **Measured cost (2026-09-21, `be1fafe6d` binary).** `couplings/eqweather_scm.esm` was **7 m 16 s for two column tests, 134/134, peak 491 MB**; it now carries **FOUR regimes and 278 assertions** and ran **278/278 in 15 m 32 s (932 s), peak 491 MB** on a quiet host (re-measured independently 2026-09-21; an earlier contended run of the same document reported 27 m 48 s at load average 40-260, so a number from a loaded machine can be off by 1.8x — say which you had). Peak RSS is flat in the test count, so the per-test cost is the whole of it. **When only some tests matter, split the document**: writing each test into its own single-test copy and running the four in parallel finished all 278 in about 10 minutes of wall clock under the same load, because on an oversubscribed host four processes get four times the share of one. History, for calibration: 1 h 51 m on pre-#439 `main` — **EarthSciAST #438** built a diffsol implicit solver for every run including one that cannot advance (an empty `time_span`, or a non-empty one whose whole output grid sits at `t0`), and materialising its matrix-free FD Jacobian costs **2*n_states + 1 full RHS evaluations** to return the untouched initial state; then 7 m 58 s on the #439-only binary once WSM6 took the document to 656 states. Repros for the old defect: `data/eqwefic/phase3_probes/mwe_state_leaf_{static,ode}.esm`.
- **Run the suite by naming directories, never as `./esm test .`** — and re-measure with `tools/cost_sweep.sh` rather than trusting the snapshot below, because this repo grows by hundreds of assertions a day. `./esm test .` still dies in the LOADING phase (**EarthSciAST #442**, re-measured 2026-09-21/22 on `be1fafe6d`): **peak 32.67 GB, SIGKILL at 232 s, zero bytes of stdout** (38.4 GB before #440). The RSS ramps linearly at ~0.30 GB/s from 30 s and plateaus near 32.7 GB. **The killer is the Slurm job's 40 GB cgroup (`mem=40992M`), shared by every agent in the allocation — the node had 113 GB free** — so a `./esm test .` here takes other agents' runs down with it. **It is not discovery breadth: `./esm test components couplings lib` covers the SAME 368 files and plateaus at 573 MB.** Every multi-directory grouping peaks at about the maximum of its parts; only the single `.` root blows up (~57x). Name the directories.
  **Snapshot, 2026-09-22 08:34-08:59, pinned at `c768b5b`, load average 16.9-20.0 on 20 cores** (i.e. at or under one runnable process per core, where wall ≈ CPU; `couplings` agreed with a load-3 run the night before to 1 %). All green, **10,345 assertions** (lib 12 + components 8014 + couplings 2319):

  | directory | assertions | wall s | CPU s | peak MB |
  |---|---|---|---|---|
  | `lib` | 12 | 0.02 | 0.0 | 14 |
  | `components/land_surface` | 84 | 0.1 | 0.1 | 31 |
  | `components/atmospheric_deposition/wrf_wesely` | 2048 | 13.0 | 12.9 | 32 |
  | `components/gaschem` | 1394 | 2.7 | 2.4 | 51 |
  | `components/wildland_fire` | 919 | 14.8 | 14.5 | 93 |
  | `components/atmospheric_dynamics/ysu` | 157 | 0.4 | 0.3 | 49 |
  | `components/atmospheric_dynamics/sfclayrev` | 610 | 21.4 | 21.2 | 256 |
  | `components/atmospheric_dynamics/wsm6` | 790 | 1.4 | 1.3 | 36 |
  | `components/atmospheric_dynamics/wrf_arw` | 213 | 0.4 | 0.3 | 35 |
  | `components/atmospheric_radiation/rrtm_lw` | 1451 | 58.4 | 58.0 | 526 |
  | `components/atmospheric_radiation/dudhia_sw` | 222 | 0.4 | 0.4 | 41 |
  | `components/atmospheric_radiation/wrf_solar` | 63 | 0.05 | 0.0 | 15 |
  | `components/atmospheric_radiation/cloud_fraction` | 63 | 0.1 | 0.1 | 21 |
  | `components` (whole tree, = sum of the rows above) | 8014 | 112.8 | 111.8 | 574 |
  | `couplings` | 2319 | 1230.9 | 1214.2 | 486 |

  So the whole suite is **~22 minutes**, and **`couplings` is 92 % of it** (`eqweather_scm.esm` alone ~932 s, `madronich_column.esm` 1323 assertions). Every component directory together is under two minutes; no directory exceeds 574 MB. **Wall time is only trustworthy when the load is at or below the core count (20):** the 1.8x spread recorded for `eqweather_scm.esm` came from load 40-260, while two sweeps at load 3 and load 18 agreed to 1 %. Record the load with any timing; trust the CPU column. **Any single document that runs for hours is a regression, not a big suite.** Always run line-buffered (`stdbuf -oL ./esm test ... | grep ...`) or piping through `grep` block-buffers and hides all progress until the run ends.
- **`ESD_ROOT` must be exported first, and since 2026-09-22 there is ONE worktree for the whole repo:** `export ESD_ROOT=/projects/illinois/eng/cee/ctessum/ctessum/code/EarthSciDiscretizations-integ`. It is local branch `integ/fire-column-sedplm` at `a4540d5`: `integ/fire-column-rules` (the `-fire` worktree, `966ff14`) merged with ESD branch `column-sedimentation-jh2010-plm` (**PR #42**, at `14b5db5` since 2026-09-22, which added the upwind-side face flux; first merged at `bd0634c`), clean. **It is required, not optional:** `components/atmospheric_dynamics/wsm6/sedimentation.esm` now imports `column_nonuniform_1d/rules/sedimentation_jh2010_flux_D_lev.esm` (#42), which no other worktree has, so under `-column` or `-fire` that component and `couplings/eqweather_scm.esm` report `template_import_unresolved`. It is a STRICT SUPERSET of both older worktrees (checked 2026-09-22: `diff -r` of `grids/` against `-fire` shows only the five #42 files, one rule and four stencils, under `column_nonuniform_1d/`), and `tools/cost_sweep.sh` defaults to it. The older two, kept pinned for history: `EarthSciDiscretizations-fire` (`integ/fire-column-rules`) merged `cartesian2d-levelset-weno` (PR #37, the WENO5/ENO1 and Godunov-upwind 2-D rules `level_set_tendency_wrffire.esm` needs) with `column-sedimentation-flux` (`face_flux_D_lev_supplied_faces.esm`, needed by `fire_flux_to_atmosphere_wrffire.esm`), because no ESD branch has both; it is a strict superset of `EarthSciDiscretizations-column` (detached at `f80069a`). EarthSciDiscretizations rules are referenced as `${ESD_ROOT}/grids/...` (esm-spec §4.7 env-var ref expansion, EarthSciAST #400); when the variable is unset the loader deliberately leaves the literal token in place, so every file importing an ESD rule reports `template_import_unresolved` rather than a test failure. **Trap, hit 2026-09-21:** the column rules this repo depends on — `grids/column_nonuniform_1d/grid.esm` and its `rules/*` — exist ONLY on the unmerged ESD branches `column-nonuniform-1d`, `column-diffusion-face-flux-rule` and `column-sedimentation-flux`. They were **not on ESD `main`** when that trap was hit; most have since merged (see the next bullet, which supersedes this sentence — the pin still stands, for different reasons). `../EarthSciDiscretizations` is a shared checkout that other sessions move between branches, and when one did, every SCM test went red in 0.09 s with `template_import_unresolved` on `grid.esm` — which looks like a broken `./esm` and is not. `../EarthSciDiscretizations-integ` (and the older `-column`/`-fire`) are worktrees no other session moves; leave them pinned until those branches merge to ESD `main`, and do NOT fix this by switching the shared checkout's branch.
- **Do NOT switch `ESD_ROOT` to ESD `main` yet (measured 2026-09-23 at ESD `16a84f5`, and re-measure before trying again).** The merges landed — `column_nonuniform_1d` entire, `sedimentation_jh2010_flux_D_lev` (#42) and the WENO5/Godunov 2-D rules (#37, #40) are all on `main` now — and where main's rules still apply they are **numerically identical**: every `.esm` that loads produced the exact same pass count under both pins, including the 853-line-different sedimentation rule (790/790). Main also clears all 233 `deprecated_op_alias` warnings, since `-integ`'s rules are still `"op": "aggregate"`. **The blocker is the rule API, not the numbers and not the binding.** ESD `main` loads and evaluates cleanly under the `be1fafe6d` binary — zero version or parse complaints on the esm 1.2.0 documents — so this is "main moved the rules out from under us", NOT "main is ready and we are not". Three causes, 1051 + 471 + 31 errors: (1) **#39 made `integral`'s `lower`/`upper` GROUND names** (`z_sfc`/`z_top`, or `lev`/`lev_nodes` for the cumulative forms) where they used to be template parameters, and this repo spells its bounds as expressions (`ze[1]`, `ze[NLEV+1]`, `zl[NLEV]`), so 17 integral nodes across 10 components stop matching and hit the `unlowered_operator` gate — main's `grid.esm` does not define those names, so each consumer must declare them, and `gwdo/blocking_drag.esm` integrates to the top layer CENTRE, which `z_top` would misname; (2) **ESD PR #35 is CLOSED unmerged** (2026-09-22, no comment), and main's `varcoeff_face_laplacian_lev_flux_bc.esm` matches `D(K·D(u,lev), lev)` while `noah/soil_heat.esm`, `noah/soil_moisture.esm` and `slab/slab.esm` need the FLUX form `D(K·D(u,lev) − F, lev)` — a different operator, not a rename — which kills 471 land-surface assertions and every coupling mounting slab, `eqweather_scm.esm` included; (3) **ESD PR #41 is still OPEN**, so the three `weno3_eno1_*` rules are absent and `level_set_tendency_wrffire.esm` loses its 31 `fire_upwinding = 8` tests. Order to fix: merge #41; settle what replaces #35; do the `z_sfc`/`z_top` migration in one pass between tranches; re-run the comparison; only then rewrite these bullets. A fifth worktree `EarthSciDiscretizations-esdmain` (detached at `16a84f5`) is left in place, unreferenced, so the re-check is cheap.
- **Run tests with the Rust CLI only.** The EarthSciModels CI gate (`tools/run_esm_inline_tests.py`, Python binding) is a condition for *merging* an EarthSciModels PR, not for creating one, and its slowness on array operations is a Python-binding performance issue handled separately; do not run it as part of this work. Run a test in the Julia or Python binding only to debug a case where the Rust CLI itself appears to be wrong.
- **Existing components take precedence.** Before authoring a component, search `../EarthSciModels/components` (and `lib/`) for an existing implementation (e.g. Monin-Obukhov surface layer, Holtslag-Boville PBL, saturation vapor pressure, Rothermel/level-set fire spread). Reuse by reference and add WRF-derived tests rather than duplicating.
